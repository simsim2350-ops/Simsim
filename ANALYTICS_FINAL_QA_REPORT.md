# SimSim Analytics — Final QA & Data Consistency Report

## الحكم التنفيذي

تم تنفيذ تدقيق الحسابات والاتساق وتحسينات Mobile UX على صفحة Analytics الحالية دون تغيير Database Schema أو Supabase architecture أو Authentication أو Routing. **الحكم الحالي: NEEDS FIXES / QA BLOCKED**؛ السبب هو عدم توفر جلسة مستخدم موثقة لإتمام Manual Visual QA الفعلي على 320 و360 و390 و430 وDesktop، وليس فشل Build أو Tests.

## Bugs والإصلاحات

| المشكلة | الحالة | الإصلاح |
|---|---|---|
| ظهور مقارنة `100%` عندما تكون الفترة السابقة صفرًا | تم الإصلاح سابقًا والتحقق منه | تعرض الصفحة «بيانات جديدة» أو «لا توجد مقارنة سابقة» بدل نسبة نمو غير ذات معنى. |
| متوسط وقت التحضير يظهر بوحدة مختصرة أو قيمة خام غير منطقية | تم الإصلاح | المصدر هو `avg_prep_minutes` الناتج من `extract(epoch)/60` للطلبات المكتملة. تُقبل القيم من 0 إلى 24 ساعة، وتُعرض بصيغة `دقيقة`. القيم غير الصالحة تعرض «بيانات غير كافية». |
| نسبة أعلى يوم لا تتبع مقياس الرسم | تم الإصلاح | أصبح أعلى يوم يعتمد على المقياس المختار: المبيعات أو الطلبات أو متوسط الطلب. |
| جدول الفروع يُقص على Mobile | تم الإصلاح | يعرض Compact Branch Cards على Mobile، وجدولًا كاملًا على Desktop. |
| حالات الطلبات غير المعروفة لا تظهر | تم الإصلاح | تُعرض جميع الحالات القادمة من البيانات مع تسمية احتياطية، بدل حصرها في قائمة ثابتة. |
| نسب المبيعات حسب النوع بلا معنى ظاهر | تم الإصلاح | تُحسب من إجمالي إيراد الأنواع الفعلي، وتُعرض بصيغة `% من المبيعات`. |
| أسباب الإلغاء غير متطابقة مع العدد الكلي | تم الإصلاح | يظهر تحذير Data Quality إذا لم يساوِ مجموع الأسباب إجمالي الإلغاءات. |
| الشكاوى غير المصنفة تعرض كسبب عادي | تم الإصلاح | يظهر تنبيه يذكر العدد ويشرح أن التصنيف يساعد على اكتشاف المشاكل المتكررة. |

## مصادر ومعادلات Metrics

| Metric | المصدر والمعادلة | الوحدة والـFallback |
|---|---|---|
| إجمالي المبيعات | `get_analytics_summary.revenue` من الطلبات المكتملة، `sum(total)` | `ر.س`، صفر عند عدم وجود بيانات |
| صافي المبيعات والضريبة | `vatBreakdown(completed_gross_sum)` بعد طرح رسوم التوصيل من إجمالي الأساس بحسب منطق التسعير الحالي | `ر.س`، `—` للصافي عند عدم وجود مبيعات |
| عدد الطلبات | `total_orders` من الفترة والفرع المحددين | عدد، صفر |
| متوسط الطلب | `revenue / completed_count` | `ر.س`، `—` دون طلبات مكتملة |
| معدل الإلغاء | `cancelled_count / total_orders * 100` | نسبة، صفر عند عدم وجود طلبات |
| أعلى يوم | أعلى قيمة من `daily` بعد تطبيق مقياس الرسم الحالي | يتغير مع Tab الرسم |
| أكثر الأصناف طلبًا | `products` من عناصر الطلبات؛ الترتيب حسب `count`، ونسبة العرض من إجمالي إيراد المنتجات | طلبات و`ر.س` ونسبة موصوفة |
| المبيعات حسب النوع | `type_revenue` من الطلبات المكتملة؛ النسبة من مجموع إيرادات الأنواع | `ر.س` و`% من المبيعات` |
| الولاء | `member_aov` و`nonmember_aov` و`member_revenue_share` من دالة التحليلات المتقدمة؛ المقارنة على الطلبات المكتملة | `ر.س` ونسبة، بلا ادعاء سببي |
| معدل العودة | `count(customers with n > 1) / count(customers)`، حيث العملاء مجمعون حسب الهاتف في الفترة | نسبة، لا يظهر Insight دون 3 عملاء |
| رضا العملاء | متوسط وعدد التقييمات ونسب `rating >= 4` و`rating <= 2`؛ المحايدون = 100 − الراضين − المنخفضين | من 5 ونسب مجموعها منطقيًا |
| متوسط وقت التحضير | متوسط `updated_at - created_at` للطلبات المكتملة بعد تحويل الثواني إلى دقائق | دقيقة، «بيانات غير كافية» للقيم غير الصالحة |
| ساعة الذروة | الساعة ذات أعلى عدد من الطلبات المكتملة من `hours` | نطاق ساعة، `—` عند غياب البيانات |

## اتساق الفلاتر

تُستخدم الفترة والفرع المختاران في استدعائي `get_analytics_summary` و`get_advanced_analytics` معًا، وتنعكس على KPIs والرسم والمنتجات والأنواع والحالات والفروع والولاء والرضا والإلغاء وملخص الفترة. لا توجد قراءة مستقلة من Dataset مختلف داخل الصفحة.

## Automated QA

| الفحص | النتيجة |
|---|---|
| `npm run build` | ناجح |
| `npm test -- --run` | ناجح: 9 ملفات و231 اختبارًا |
| `npm run check:registry` | ناجح: 34 قدرة متطابقة |
| `git diff --check` | ناجح |
| `npm run typecheck` | غير متاح؛ لا يوجد Script بهذا الاسم |
| `npm run lint` | غير متاح؛ لا يوجد Script بهذا الاسم |

## Visual وManual QA

تمت مراجعة كود Mobile Toolbar والرسم وCards الفروع للتأكد من عدم فرض جدول ضيق على الشاشات الصغيرة، لكن Manual Visual QA authenticated لم يُنفذ لأن جلسة مستخدم موثقة غير متاحة. لذلك لم يتم الادعاء بفحص الفلاتر فعليًا لكل فرع أو تأكيد عدم وجود Overflow في المتصفح على المقاسات المطلوبة.

## Data Quality المتبقية

قد تظهر تنبيهات جودة البيانات للمستخدم عندما تكون أسباب الإلغاء أو الشكاوى غير مصنفة أو لا يتطابق مجموع الأسباب مع العدد الكلي. كما أن قيم وقت التحضير غير الصالحة تُحجب وتظهر كبيانات غير كافية بدل عرض رقم خام. هذه ليست بيانات وهمية ولا تم إصلاحها بتخمينات.

> **النتيجة: NEEDS FIXES / QA BLOCKED**

يلزم توفير جلسة مستخدم موثقة لإكمال Manual Visual QA على المقاسات المطلوبة وتحديد ما إذا كانت الصفحة تستحق `READY FOR PRODUCTION` نهائيًا.
