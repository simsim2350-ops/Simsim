# تقرير صقل إدارة الموظفين — SimSim

## نطاق التحسين

تم صقل `src/pages/Staff.jsx` فوق النظام المدمج سابقًا، دون تغيير أي Migration أو API أو سياسة RLS. يرتكز التحديث على متطلبات قابلية الاستخدام والإدارة اليومية للمالك، مع إبقاء نموذج الفروع المتعددة والتفويض الخادمي القائم كما هو.

| المجال | التحسين المنفذ |
|---|---|
| الإحصاءات | إضافة إجمالي الموظفين، المفعلين، المعطلين، المديرين، الكاشير، والموظفين كإحصاءات ديناميكية مختصرة |
| رابط الدخول | زر لفتح رابط دخول الموظفين وزر نسخ مع Toast موجود، دون عرض معلومات حساسة |
| البحث والفلاتر | البحث الآن يشمل اسم المستخدم والدور والفرع والحالة، والفلاتر تبقى قابلة للتمرير أفقيًا على الهاتف |
| بطاقة الموظف | إضافة «عرض الصلاحيات»، تأكيد قبل التعطيل، وعبارة صادقة «لا يوجد نشاط مسجل» عند غياب المصدر الحقيقي |
| الأخطاء | حالة خطأ مرئية قابلة لإعادة المحاولة بدل تحويل فشل التحميل إلى Empty State مضلل |
| Modal | Tabs للمعلومات والصلاحيات والفروع والحساب والأمان، مع شريط إجراءات ثابت على الشاشات الصغيرة |
| الصلاحيات | ملخص يفرق بين الصلاحيات الافتراضية والتخصيصات اليدوية، تحديد على مستوى المجموعة أو الكل، وحالة indeterminate بلون SimSim |
| الفروع | عداد `X من Y`، تحديد/إلغاء الكل، وحالة واضحة لنطاق كل الفروع أو الفروع المحددة |
| الحساب والأمان | إظهار/إخفاء كلمة المرور، مؤشر قوة، وتحقيق إنشاء يطلب كلمة مرور قوية فعلًا |

## الضمانات

لا تزال الصلاحيات والفروع مفروضة خادميًا عبر نموذج `branch_scope` و`branch_ids` وسياسات RLS السابقة. لم يُخفف أي تحقق أو يفتح وصولًا إضافيًا إلى الموظفين أو الفروع أو الطلبات أو الطاولات.

> لا توجد بيانات حقيقية لآخر تسجيل دخول أو آخر نشاط في مصدر عضوية الموظفين حاليًا. تعرض الواجهة حالة عدم التوفر بدل اختراع قيمة، وهي مهيأة لعرض الحقول الفعلية متى أضيف مصدرها.

## التحقق

| الفحص | النتيجة |
|---|---|
| `npm run build` | ناجح |
| `npm test -- --run` | 231 اختبارًا ناجحًا |
| `npm run check:registry` | ناجح، 34 قدرة متطابقة |
| `git diff --check` | ناجح |
| تغييرات قاعدة البيانات | لا توجد؛ يقتصر هذا الصقل على `Staff.jsx` |
| Visual QA موثق | يحتاج جلسة مالك موثقة؛ لم تُستخدم بيانات اعتماد أو تجاوز للمصادقة |
