{
  "name": "simsim-print-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Print Agent — polls print_jobs for a configured restaurant/branch and sends thermal-formatted documents to a configured printer. Runs on a machine inside the restaurant, never in a browser or on Vercel.",
  "main": "src/index.mjs",
  "scripts": {
    "start": "node src/index.mjs",
    "test": "node --test",
    "test:printer": "node scripts/smoke-test-printer.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.8",
    "dotenv": "^16.4.5",
    "pngjs": "^7.0.0"
  },
  "optionalDependencies": {
    "playwright": "^1.62.1"
  }
}
