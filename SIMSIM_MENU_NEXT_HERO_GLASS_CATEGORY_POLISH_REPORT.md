# تقرير: المرحلة التالية من تحسين Public Menu — نظام الأقسام المزدوج + Hero/Glass Sheet التفاعلي
## Public Menu Polish Round — Dual Category System + Scroll-Driven Hero/Glass — Final Report

**التاريخ:** 2026-09-06
**النطاق:** `menu-next` فقط — بلا أي تعديل على Dashboard أو قاعدة البيانات أو الدفع.
**الحالة:** تم النشر على production (`simsimmenu.com`) للاختبار الحي فقط، بموافقتك الصريحة — **لم يتم أي commit/push/PR/merge**. بانتظار مراجعتك واعتمادك.

---

## 1) Executive Summary

هذه الجولة نفّذت طلبين رئيسيين دون المساس بأي شيء يعمل حاليًا:

1. **نظام أقسام مزدوج:** الشريط الأفقي الموجود من قبل (Phase السابقة) بقي كما هو تمامًا، وأُضيف له زر "☰" يفتح قائمة رأسية (Vertical Drawer) تعرض كل الأقسام الحقيقية — **كلاهما يشتركان في نفس الـ state ونفس مصدر البيانات** (لا يوجد نظامان منفصلان).
2. **إعادة بناء سلوك Hero + Glass Sheet:** بدلاً من بطاقة زجاجية ثابتة، أصبح الآن: Hero مثبّت (`fixed`) يتلاشى تدريجيًا مع التمرير، بطاقة زجاجية عادية (`relative`، غير مثبّتة) تنزلق بعيدًا بشكل طبيعي، وHeader مصغّر دائم (`fixed`، شعار + بحث فقط) يظهر بتلاشٍ تدريجي ليحل محلها — بنفس الآلية والمنطق الحقيقيين من المينيو القديم (`src/features/menu/MenuHeader.jsx`)، تم مراجعتهما سطرًا بسطر قبل البدء.

تم اكتشاف وإصلاح خلل حقيقي أثناء الاختبار (race condition في توقيت بدء الاستماع للتمرير) — مفصّل في القسم 20 أدناه، مع الدليل الفعلي الذي كشفه.

**66-67 اختبارًا حيًا** (النظام القديم + الجديد معًا) نجحت **100%**، بما فيها **3 تشغيلات متتالية كاملة** لملف الاختبار الجديد تحديدًا للتأكد من عدم وجود Flakiness، بالإضافة إلى مراجعة بصرية فعلية (Visual QA) عبر لقطات شاشة حقيقية على 3 أحجام شاشة مختلفة.

---

## 2) المشاكل التي تم اكتشافها

| # | المشكلة | الوصف |
|---|---|---|
| 1 | لا توجد قائمة رأسية للأقسام | الشريط الأفقي فقط — لا وسيلة سريعة لرؤية كل الأقسام دفعة واحدة |
| 2 | الـGlass Sheet ثابتة تمامًا | لا تفاعل مع التمرير إطلاقًا — تختفي فجأة بمجرد خروجها من الشاشة، ولا يوجد Header مصغّر بديل |
| 3 (مكتشفة أثناء الاختبار) | **Race condition حقيقي في كود التمرير** | إذا حدث تمرير سريع (برمجي أو من متصفح يعيد موضع التمرير) قبل اكتمال الـhydration، يبقى الـheader "يظن" أن الصفحة في الأعلى حتى يحدث تمرير آخر — مفصّل بالكامل في القسم 20 |

---

## 3) التعديلات التي تم تنفيذها

- `CategoryNav.tsx`: أُعيد بناؤه ليعرض الشريط الأفقي (دون أي تغيير في منطقه) + زر "☰" + قائمة رأسية جديدة، كلاهما يستخدمان نفس `categories` prop ونفس `activeId` state.
- `RestaurantHeader.tsx`: أُضيف منطق تمرير مطابق تمامًا للمينيو القديم (rAF-throttled scroll listener، قيمتان مستمرتان `heroOpacity`/`compactT`)، مع إصلاح إضافي (قراءة موضع التمرير الحقيقي فور التركيب، وليس فقط عند حدث تمرير لاحق).
- `globals.css`: Hero أصبح `position: fixed` (مع Spacer يحجز مكانه في التدفق الطبيعي)، البطاقة الزجاجية بقيت `position: relative` كما هي (لم تُجعل `fixed` إطلاقًا — طبقًا لتعليمتك الصريحة)، Header مصغّر جديد `fixed` بارتفاع 56px (نفس ارتفاع المينيو القديم بالضبط)، شريط الأقسام أصبح `top: 56px` بدل `top: 0` ليجلس أسفل الـHeader المصغّر مباشرة. أُضيفت متغيّرة `--menu-frame-w` لضمان تطابق عرض العناصر الثابتة مع عرض الإطار المتجاوب على Tablet/Desktop (كانت ستبقى 480px فقط بدون هذا الإصلاح).
- `i18n.ts`: مفتاحان جديدان (`allCategories`, `openAllCategories`) بالعربي والإنجليزي.
- `page.tsx`: تمرير `lang` إلى `CategoryNav`.

---

## 4) تفاصيل Category System

مصدر بيانات واحد (`categories` prop مُمرَّر من `page.tsx`، نفس القائمة المفلترة بـ `is_visible` والمرتبة بـ `sort_order` من Dashboard — لا Hardcoding، لا تكرار). كلا الواجهتين (الشريط والقائمة) تستدعيان نفس دالة `goToCategory` الموجودة داخل نفس المكوّن.

## 5) Horizontal Category Bar

لم يتغيّر منطقه (تمرير أفقي، لا التفاف لسطر ثانٍ، تبويب نشط بخط عريض + خط سفلي متحرك) — فقط أُضيف بجانبه زر "☰" (في بداية الشريط بحسب اتجاه اللغة تلقائيًا عبر Flexbox + `dir`, بلا أي كود خاص بـRTL منفصل).

## 6) Vertical Category Menu

زر "☰" يفتح Bottom Sheet (نفس نمط `.allergens-modal`/`.options-modal` الموجود مسبقًا للاتساق البصري) يعرض كل الأقسام الحقيقية بنفس الترتيب. الضغط على قسم: يُمرّر إليه، يُحدّث `activeId` (فينعكس تلقائيًا على الشريط الأفقي أيضًا لأنه نفس الـstate)، ثم يُغلق القائمة. يُغلق أيضًا بالضغط خارج الورقة أو بمفتاح Escape (نفس نمط `AllergensModal.tsx`).

## 7) Scroll Spy

لم يتغيّر إطلاقًا — نفس `IntersectionObserver` بنفس `rootMargin`/`threshold` من الجولة السابقة، يعمل بشكل طبيعي مع كلا واجهتي الأقسام لأنهما يتشاركان نفس الـstate.

## 8) Hero + Glass Sheet behavior

- **الحالة الابتدائية:** Hero (`position:fixed`, مثبّت على أعلى الشاشة) + بطاقة زجاجية (`position:relative`، تتراكب معه بهامش سالب محسوب ليعطي تداخلًا بصريًا واضحًا وليس مجرد لمسًا للحافة).
- **أثناء التمرير:** قيمتان مستمرتان (وليس Boolean) تُحسبان من نفس `scrollY`: `heroOpacity` (يتلاشى Hero بالكامل خلال نافذة 130px)، و`compactT` (يظهر الـHeader المصغّر خلال نافذة 42px، مع حركة `translateY` خفيفة إضافية). لا `display:none` ولا `opacity` مفاجئة — انتقال متدرّج حقيقي، تم التحقق منه فعليًا (قيم بينية حقيقية بين 0 و1 مقاسة من متصفح حقيقي، وليس افتراضًا).
- **البطاقة الزجاجية نفسها لم تُجعل أبدًا `position:fixed`** — تم التحقق من ذلك تلقائيًا عبر اختبار مخصص يقرأ `getComputedStyle(...).position` فعليًا.

## 9) Sticky Header behavior

عنصر واحد دائم التركيب (لا يُحذف/يُعاد إنشاؤه أبدًا، فلا يحدث وميض)، يحتوي فقط على الشعار وزر البحث (بحسب طلبك الصريح هذه المرة — أبسط من هيدر المينيو القديم المصغّر الغني بالمعلومات). يصبح ظاهرًا وتفاعليًا (`pointer-events`) فقط بعد تجاوز نافذة الظهور، ويبقى ملتصقًا (`fixed`) أعلى الشاشة طوال التصفح، مع شريط الأقسام ملتصقًا مباشرة أسفله (`top: 56px`).

## 10) Responsive behavior

تم اكتشاف وإصلاح فجوة حقيقية: كانت العناصر الثابتة (Hero والHeader المصغّر) ستبقى محصورة بعرض 480px حتى على الشاشات الكبيرة (Tablet/Desktop)، رغم أن باقي الصفحة تتسع إلى 720px/1100px حسب حجم الشاشة — تم إصلاحها عبر متغيّر CSS (`--menu-frame-w`) يتزامن تلقائيًا مع نفس نقاط الكسر (`@media`) المستخدمة أصلاً للإطار. تم التحقق بصريًا فعليًا على Mobile (390px)، Tablet (768px)، وDesktop (1440px) — انظر القسم 18.

## 11) Accessibility

- زر "☰": `aria-label` واضح (فتح كل الأقسام) + `aria-haspopup="dialog"`.
- القائمة الرأسية: `role="dialog"` + `aria-modal="true"` + `aria-label`، تُغلق بـEscape، تُغلق بالضغط خارجها، كل عنصر فيها زر `<button>` حقيقي (وليس `<div>` بمعالج نقر فقط) بارتفاع لمس ≥44px.
- زر البحث (في الهيدر المصغّر): يعيد استخدام نفس `.menu-header__search-btn` الموجود مسبقًا (مُختبر سابقًا بالوصول عبر لوحة المفاتيح).
- التركيز (Focus) لا يُنقل قسرًا عند الفتح/الإغلاق — يبقى حيث كان، سلوك متوقّع وغير مربك.

## 12) Performance

لا يوجد أي معالج تمرير خام (raw per-pixel listener) — الآلية الوحيدة المستخدمة هي نفس نمط المينيو القديم المُثبَت في الإنتاج سابقًا: مستمع `scroll` واحد (`{ passive: true }`) مقترن بعلم `ticking` + `requestAnimationFrame`، بحيث لا يُعاد حساب/رسم أكثر من مرة واحدة لكل إطار رسم كحد أقصى — لا Layout Thrashing، لا إعادة رسم مفرطة. `IntersectionObserver` (لScroll Spy) لم يتغيّر ولا علاقة له بهذا التمرير الجديد.

---

## 13) الملفات التي تم تعديلها

```
menu-next/app/globals.css
menu-next/app/menu/[slug]/page.tsx
menu-next/components/CategoryNav.tsx
menu-next/components/RestaurantHeader.tsx
menu-next/lib/i18n.ts
menu-next/tests/e2e/public-menu-audit.spec.ts   (تحديث اختبار واحد ليعكس إضافة زر "☰" المطلوبة الآن صراحةً)
```

## 14) الملفات الجديدة

```
menu-next/tests/e2e/hero-glass-category-drawer.spec.ts
```

---

## 15) الاختبارات التي تم تشغيلها

- `npx tsc --noEmit` — نظيف، بلا أخطاء، بعد كل تعديل.
- `npx next build` — نجح، بلا تحذيرات جديدة، بعد كل تعديل.
- **Live Playwright** على `https://simsimmenu.com` (نفس نمط الجولات السابقة، إذ تعذّر الاختبار المحلي/الـPreview لأسباب بيئية موثّقة أدناه في القسم 21):
  - تشغيل كامل أول: 66 اختبارًا (64 نجح، فشل اختباران بسبب مشكلة توقيت — انظر القسم 20).
  - إصلاح الاختبارات + إصلاح Race Condition الحقيقي في الكود + إعادة نشر.
  - تشغيل ملف الاختبار الجديد بمفرده **3 مرات متتالية** بعد الإصلاح — 12/12 في كل مرة (36/36 إجمالاً)، للتأكد من زوال الـFlakiness فعليًا لا افتراضًا.
  - تشغيل كامل نهائي: **67 اختبارًا، 67 نجح، صفر فشل**.

## 16) عدد الاختبارات

- 67 اختبارًا حيًا (55 من الجولات السابقة + 12 جديدة لهذه الجولة).

## 17) Passed / Failed

- **النتيجة النهائية: 67/67 ناجح (100%).**
- (شفافية): التشغيل الأول لهذه الجولة تحديدًا سجّل فشلين، أُصلحا جذريًا (وليس تجاوزًا) — مفصّل بالكامل في القسم 20.

## 18) Visual QA results

تم التقاط لقطات شاشة حقيقية من `https://simsimmenu.com` (وليس افتراضًا من الكود) على:
- **Mobile (390px):** أعلى الصفحة (Hero+Glass متداخلان بوضوح)، منتصف التمرير، بعد تجاوز الـHero بالكامل (Header مصغّر واضح: شعار + بحث، ملتصق، وشريط الأقسام أسفله مباشرة بلا فجوة)، القائمة الرأسية مفتوحة (كل الأقسام تظهر، القسم النشط مميّز، زر إغلاق واضح).
- **Tablet (768px):** الإطار يتسع إلى 720px بشكل صحيح، لا Overflow.
- **Desktop (1440px):** الإطار يتسع إلى 1100px بشكل صحيح، العناصر الثابتة (Hero/Header المصغّر) تتبع نفس العرض بالضبط (لا عمود ضيق 480px عالق داخل صفحة عريضة).

في جميع الحالات: لا تراكب فوق المنتجات، لا Layout Jump ملحوظ، اتجاه RTL صحيح تلقائيًا في كل مكان (نص عربي، ترتيب الأزرار، اتجاه القائمة).

## 19) Regression results

**صفر انحدار.** كل الميزات المحمية صراحةً في هذه المهمة أُعيد اختبارها حيًا ونجحت: Table QR (القفل التلقائي، منع التلاعب بالفرع)، Branch URL + Table Dropdown، Table → Order → Invoice، Branch Isolation، Hero configuration (menu_layout، cover_url)، Offers/Discounts، Checkout/Order flow الكامل، Loyalty/Coupons (عبر اختبارات My Orders وCoupon الموجودة مسبقًا).

---

## 20) المشاكل المتبقية

لا توجد مشاكل وظيفية معروفة متبقية. **تفصيل الخلل المُكتشَف والمُصلَح فعليًا (وليس مجرد تجاوزه بزيادة مهلة الاختبار):**

أول تشغيل حي أظهر فشلين في اختبارات "التلاشي التدريجي" — بدل افتراض أنها "بطء شبكة" وزيادة مهلة الانتظار فقط، تم التحقيق الفعلي: سكربت تشخيصي مباشر (خارج إطار الاختبار) أثبت أن التمرير والـopacity يعملان بشكل صحيح تمامًا عند إعطاء الصفحة وقتًا كافيًا للـhydration قبل التمرير (500ms) — لكن عند التمرير فورًا بعد التنقل (وهو بالضبط ما يفعله اختبار Playwright)، القيمة تبقى عالقة عند "غير ممرَّرة إطلاقًا" لخمس ثوانٍ كاملة رغم Polling حقيقي — مما استبعد "توقيت بطيء" وأثبت وجود خلل حقيقي: **دالة الاستماع للتمرير في `RestaurantHeader.tsx` كانت تعتمد فقط على أحداث `scroll` المستقبلية، دون قراءة موضع التمرير الفعلي عند التركيب (mount)** — أي أن أي تمرير برمجي سريع يحدث قبل اكتمال الـhydration (أو حتى استعادة المتصفح لموضع تمرير سابق عند التنقل للخلف) يمكن أن يُفوَّت تمامًا، تاركًا الـHeader "يظن" أن الصفحة في الأعلى حتى يحدث تمرير آخر. تم إصلاحه بإضافة قراءة فورية واحدة لموضع التمرير الحقيقي عند التركيب، بنفس الآلية المُقيَّدة بـ`requestAnimationFrame`. أُعيد النشر، وأُعيد تشغيل الاختبار **3 مرات متتالية كاملة** (36 اختبارًا) لتأكيد الإصلاح فعليًا لا افتراضًا، ثم أُكِّد التشغيل الكامل النهائي (67/67).

## 21) المخاطر المحتملة

- **لا يوجد خطر على أي منطق أعمال أو قاعدة بيانات أو دفع** — هذه الجولة بصرية/سلوكية بالكامل داخل `menu-next`، بلا أي migration أو تعديل RPC.
- الاختبار المحلي (`next build && next start`) والـPreview (Vercel Deployment Protection) كلاهما غير متاحين فعليًا في بيئة هذه الجلسة (موثّق سابقًا في `SIMSIM_VERCEL_PLAYWRIGHT_PROTECTION_TEST_REPORT.md` وتم التحقق من استمرار المشكلة) — لذلك تم النشر مباشرة على production للاختبار الحي، **بعد أخذ موافقتك الصريحة على هذا الخيار تحديدًا** بدلاً من الخيارين الآخرين (توفير Bypass Secret، أو الاكتفاء بمراجعة يدوية بلا اختبار آلي).
- القيم الرقمية للتلاشي (86-216px للـHero، 78-120px للHeader المصغّر) مأخوذة كما هي من المينيو القديم دون إعادة اشتقاق لارتفاع Hero الخاص بـmenu-next (140px، قريب من ~132px في القديم) — تجربة بصرية فعلية (القسم 18) أكّدت أن الشعور العام سليم، لكن لو رغبت لاحقًا بتخصيص هذه القيم بدقة أكبر فهذا قرار تصميمي منفصل يمكن ضبطه بسهولة (كل القيم في مكان واحد بالكود).

## 22) الخطوات التالية

- بانتظار مراجعتك البصرية والوظيفية المباشرة على `simsimmenu.com` (التغييرات منشورة فعليًا هناك للاختبار فقط، كما وافقت).
- بانتظار موافقتك الصريحة على commit/push/PR/merge — لم يُنفَّذ أي منها.

---

## READY FOR REVIEW: **YES**
## READY FOR COMMIT: **YES** (بانتظار موافقتك الصريحة فقط — الكود جاهز، الاختبارات كاملة، البناء نظيف)
## READY FOR DEPLOY: **YES — تم النشر بالفعل على production للاختبار الحي بموافقتك، ويعمل بشكل صحيح ومُتحقَّق منه**
