# تنفيذ Dashboard 2.0 في SimSim

تمت إعادة بناء صفحة Dashboard الرئيسية في `src/pages/Dashboard.jsx` مباشرة داخل مستودع SimSim.

## ما تم تنفيذه

أصبحت الصفحة مركز قيادة تشغيليًا لصاحب المطعم، مع الحفاظ على RTL وهوية SimSim البرتقالية والتكامل الحالي مع `AppShell` و`useAuthStore` وSupabase. يتضمن البناء الجديد رأسًا واضحًا يعرّف المستخدم بحالة المطعم ويوفر معاينة المنيو وQR Code ومشاركة المنيو كإجراء أساسي.

تمت إضافة أربع بطاقات KPI لمبيعات اليوم والطلبات ومتوسط قيمة الطلب والعملاء، مع المقارنة بالفترة السابقة، ووصف صريح للحالات التي لا تتوفر فيها بيانات بدل عرض أرقام مضللة. كما تمت إضافة مركز إجراءات يقود مباشرة إلى الطلبات أو الأصناف التي تحتاج صورًا أو أوصافًا.

تتضمن الصفحة الآن قسم مبيعات تفاعليًا بفلاتر اليوم و7 أيام و30 يومًا و3 أشهر، ونقاط الرسم تعرض التاريخ والمبيعات وعدد الطلبات عند التفاعل. كما تمت إعادة بناء عرض آخر الطلبات، والطلبات المباشرة بتحديث لحظي، والأكثر مبيعًا، وصحة المنيو، والإجراءات السريعة.

تمت إضافة حالات Skeleton للـKPIs والرسم وقائمة الطلبات والأصناف، وحالات Empty State واضحة، وحالة خطأ مع زر إعادة المحاولة. ويجري تحميل بيانات صحة المنيو من `categories` و`products` الموجودة فعليًا في Supabase دون تعديل مخطط قاعدة البيانات أو اختلاق بيانات. عندما لا توفر الدالة الحالية إيرادًا مفصلًا لكل صنف، يظهر ذلك كـ«غير متاح» بدل عرض قيمة صفرية.

تم استخدام SVG Icons موحدة داخل الصفحة بدل استخدام الرموز التعبيرية كأيقونات أساسية، مع تصميم Mobile-first ومنع قص جدول الطلبات عبر تمرير أفقي داخلي على الشاشات الصغيرة. تمت المحافظة على مسارات التنقل الحالية مثل `/orders` و`/menu` و`/analytics` و`/qr`.

## التحقق

- `npm run build` نجح.
- `npm test -- --run` نجح: **9 ملفات اختبار، 231 اختبارًا ناجحًا**.
- `git diff --check` نجح دون أخطاء تنسيق.
- الملف المعدل الوحيد هو `src/pages/Dashboard.jsx`.

## ملاحظة تشغيلية

تم تشغيل نسخة التطوير محليًا للتأكد من عمل التطبيق، لكن صفحة Dashboard محمية بالمصادقة ولذلك أعاد المسار غير الموثق التوجيه إلى صفحة تسجيل الدخول. لم يتم تنفيذ أي تغيير على قاعدة البيانات أو نشر التغييرات إلى GitHub تلقائيًا.
