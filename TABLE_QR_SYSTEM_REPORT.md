# تقرير تنفيذ نظام QR للطاولات

**المشروع:** SimSim  
**النطاق:** QR فريد وآمن لكل طاولة، وربط طلبات المنيو بالسياق الموثق للطاولة والفرع.  
**الحالة:** مكتمل محليًا، وMigration قاعدة البيانات مطبّق على مشروع Supabase المرتبط بـSimSim.

## الملخص التنفيذي

تم بناء مسار QR يعتمد على **رمز UUID مستقل** لكل طاولة، وليس على رقم أو اسم الطاولة. عند مسح الرابط، تتحقق دالة قاعدة البيانات من الرمز، و`slug` المطعم، وحالة المطعم، وحالة الفرع، وحالة الطاولة قبل تحميل المنيو. عند إنشاء طلب من هذا المسار، لا يستقبل الخادم `restaurant_id` أو `branch_id` أو `table_id` من المتصفح؛ بل يستخرجها من رمز QR الموثق ثم ينشئ طلبًا من نوع `dine_in` ويثبّت مصدره على `qr`.

> تغيير اسم الطاولة لا يغير رمزها. إعادة إنشاء QR تبطل الرابط السابق فورًا، بينما تعطيل QR يجعل رمز الطاولة غير قابل للاستخدام إلى أن يُفعّل مجددًا.

| المجال | ما تم تنفيذه |
|---|---|
| تعريف الطاولة | إضافة `branch_id` و`qr_token` و`qr_enabled` و`qr_last_used_at` إلى `restaurant_tables` |
| عزل الفروع | Trigger يمنع ربط طاولة بفرع تابع لمطعم مختلف، مع Backfill للطاولات القائمة إلى الفرع الأساسي |
| توليد QR | UUID افتراضي فريد لكل طاولة وفهرس فريد على `qr_token` |
| التحقق العام | RPC `resolve_table_qr` تتحقق من token وslug وحالات المطعم/الفرع/الطاولة دون كشف token من الجدول |
| الطلب من QR | RPC `create_order_from_table_qr` تثبت الطاولة والفرع والمطعم خادميًا وتستدعي محرك التسعير والتحقق القائم |
| سجل الطلب | إضافة `table_id` و`table_name` و`source` إلى `orders`؛ مصدر QR هو `qr` والطلبات العادية تبقى `manual` |
| حماية الإدخال | سياسة إدخال الطلبات المباشر تقبل فقط الطلبات اليدوية بلا `table_id`، بينما طلبات QR تمر حصريًا عبر RPC الموثقة |
| إدارة المالك | شاشة QR لكل طاولة: معاينة، نسخ رابط، تنزيل PNG، طباعة، تفعيل/تعطيل، وإعادة إنشاء بتأكيد صريح |
| تجربة العميل | رابط QR يختار الفرع الصحيح تلقائيًا، ويقفل الطاولة ونوع الطلب على `dine_in` داخل السلة |
| لوحة الطلبات | عرض اسم الطاولة المحفوظ وشارة مصدر `QR` في البطاقات والتنبيهات والتفاصيل |

## ملفات التنفيذ

| الملف | التغيير |
|---|---|
| `sql/table_qr_system.sql` | Migration كاملة للمخطط والفهارس والـTriggers والسياسات ودوال QR |
| `src/pages/Tables.jsx` | إدارة QR داخل بطاقة الطاولة، مع الطباعة والتنزيل والنسخ والتفعيل وإعادة الإنشاء |
| `src/lib/tablesApi.js` | جلب طاولات الفرع وإعادة إنشاء QR عبر RPC |
| `src/pages/PublicMenu.jsx` | حل token من الرابط قبل استخدام المنيو وتحميل الفرع الموثوق |
| `src/features/menu/hooks/useCheckout.js` | تحويل طلب QR إلى RPC خادمية وتثبيت تجربة الطلب على الطاولة |
| `src/features/menu/CartDrawer.jsx` | إظهار سياق «طلب داخل المطعم عبر QR» ومنع تعديل الطاولة أو نوع الطلب |
| `src/features/menu/hooks/useTables.js` | قصر قائمة الطاولات اليدوية على الفرع الفعال |
| `src/pages/Orders.jsx` | إظهار مصدر QR واسم الطاولة المحفوظ تشغيليًا |
| `qa/table_qr_local_visual_findings.md` | سجل فحص المسار العام للمنيو عبر QR |

## التحقق المنجز

تم تطبيق Migration بنجاح على Supabase. أظهر التحقق الحي أن **عدد الطاولات بلا فرع أو token يساوي صفرًا**، وأنه لا توجد طاولات مرتبطة بفرع تابع لمطعم مختلف، مع وجود سبع طاولات نشطة قابلة لاستخدام QR وقت التحقق. كما تحقق استدعاء القراءة أن token صالح يحل إلى سياق طاولة، في حين لا يحل token مزيف إلى أي سجل.

| الاختبار | النتيجة |
|---|---|
| `npm run build` | ناجح |
| `npm test -- --run` | 9 ملفات، 231 اختبارًا ناجحًا |
| `npm run check:registry` | ناجح؛ لا انحراف في Registry |
| `git diff --check` | ناجح؛ لا أخطاء مسافات أو أسطر فارغة |
| تحميل رابط QR بمنيو حقيقي | ناجح من نسخة المعاينة المبنية بعد إصلاح استيراد Supabase |
| رفض token غير صالح | ناجح؛ لا يعيد سياق طاولة |
| QA لواجهة مالك الطاولة | يحتاج جلسة مالك موثقة على المسار `/tables` لفحص الطباعة والتنزيل والتفعيل من المتصفح الحي |
| إنشاء طلب فعلي | لم يُنفذ في QA كي لا يُنشأ طلب تشغيلي أو تُستخدم بيانات عميل غير مصرح بها؛ مسار الإنشاء موثق ومربوط بالتحقق الخادمي |

## ملاحظات النشر

التعديلات ما زالت غير محفوظة في Commit أو Pull Request. Migration قاعدة البيانات نُشرت بالفعل، وهي متوافقة مع الواجهة السابقة لأن إنشاء الطاولة القديم يرث الفرع الأساسي تلقائيًا، بينما ستظهر إدارة QR الجديدة بعد نشر كود الواجهة ضمن Pull Request.

لا تستخدم ميزة QR أي بيانات وهمية أو أسرار في الرابط. المفتاح الظاهر في الرابط هو token خاص بالطاولة، ويخضع للتحقق من حالة المطعم والفرع والطاولة عند فتح المنيو وعند إنشاء الطلب.

## نتيجة فحص أمان Supabase

أظهر مستشار الأمان تحذيرات عامة لدوال `SECURITY DEFINER` القابلة للاستدعاء. بالنسبة إلى دوال QR الجديدة، هذا **مقصود** لأن العميل العابر يحتاج إلى حل رمز الطاولة وإنشاء طلب من دون تسجيل دخول، بينما يحتاج المالك المسجل إلى إعادة إنشاء الرمز. لا تعتمد هذه الدوال على مُعرّفات المطعم أو الفرع القادمة من العميل؛ بل تتحقق من الرمز والحالة ثم تستخرج سياق الطاولة داخل قاعدة البيانات. كما أن جميع دوال QR الجديدة تضبط `search_path` صراحةً وتقيّد الصلاحيات إلى الأدوار المطلوبة فقط.

| الدالة | الدور المسموح | سبب الإتاحة | الحماية الفعلية |
|---|---|---|---|
| `resolve_table_qr` | `anon`, `authenticated` | تحميل منيو طاولة بعد المسح | يعيد حدًا أدنى من بيانات السياق بعد token وslug وحالات التشغيل |
| `create_order_from_table_qr` | `anon`, `authenticated` | إرسال طلب العميل من QR | يفرض `dine_in` ويعيد التحقق من الطاولة والفرع والمطعم والمنتجات والأسعار والكوبون |
| `regenerate_table_qr` | `authenticated` | إعادة إنشاء QR من لوحة المالك | يتحقق من ملكية المطعم قبل توليد token جديد |
