# تقرير Visual + UX Refinement — إدارة الطاولات

**التاريخ:** 18 أغسطس 2026  
**النطاق:** `src/pages/Tables.jsx`

## الهدف

رفع جودة واجهة صفحة **إدارة الطاولات** إلى تجربة SaaS أكثر وضوحًا واتساقًا، مع المحافظة الكاملة على تدفقات إضافة الطاولات وتعديلها وحذفها وتفعيلها أو تعطيلها وترتيبها والبحث فيها.

## التحسينات المنفذة

| المنطقة | التحسين البصري وتجربة الاستخدام |
|---|---|
| Page Header | تحويل العنوان إلى رأس هرمي واضح بأيقونة خطية، عنوان رئيسي، ووصف ثانوي هادئ. بقي زر العودة إجراءً ثانويًا، بينما أصبح زر «طاولة جديدة» هو الـCTA الأساسي. |
| الاستجابة | استخدام Header مكدّس متوافق مع RTL لاحتواء العنوان والإجراءات والهامبرغر دون ازدحام على الشاشات الصغيرة. |
| البحث | ضبط الارتفاع والحدود ونصف القطر ومحاذاة أيقونة البحث في اتجاه RTL، مع إبقائه عنصر أداة صغيرًا لا عنصرًا بصريًا مهيمنًا. |
| بطاقات الطاولات | إعادة تنظيم التسلسل إلى: أيقونة الطاولة، الاسم، الحالة، بيانات الإنشاء/التعديل، التحكم بالترتيب، ثم الإجراءات. |
| الحالة | إظهار Badge صغيرة بنقطة لونية: أخضر هادئ للطاولة النشطة ورمادي محايد للمعطلة. |
| الإجراءات | تجميع التفعيل/التعطيل والتعديل والحذف داخل Footer موحد وواضح، مع عناوين وصول لأزرار الأيقونات. |
| الترتيب | تحسين عرض أزرار تحريك الطاولة لأعلى/أسفل دون تغيير دالة تبديل `sort_order`. |
| Empty States | تحسين حالة عدم وجود طاولات وحالة عدم وجود نتائج بحث، مع CTA واضح لأول طاولة. |
| إضافة وتعديل الطاولة | تحسين Bottom Sheet بعلامة سحب ورأس واضح وزر إغلاق ومساعدة قصيرة، من دون تغيير التحقق أو الحفظ أو Toasts. |

## الضمانات الوظيفية

لم تتغير طبقة البيانات أو أي من الدوال أو الحقول المستخدمة من `tablesApi.js`. كما بقيت عمليات `fetchAllTables` و`createTable` و`updateTable` و`deleteTable`، والتحقق من التكرار، والتوجيه، وحالة التطبيق، وSupabase، والصلاحيات، ورسائل الإشعار، وسلوك التأكيد قبل الحذف كما كانت.

## التحقق

| الفحص | النتيجة |
|---|---|
| `npm run build` | ناجح |
| `npm test -- --run` | ناجح: 9 ملفات و231 اختبارًا |
| `npm run check:registry` | ناجح: 34 قدرة متطابقة مع قاعدة البيانات |
| `git diff --check` | ناجح: لا توجد مشاكل whitespace |

## حالة Visual QA الحي

تعذر إتمام Visual QA موثق على بيئة الإنتاج في هذه المرحلة، لأن رابط النشر المتاح أرجع `404 DEPLOYMENT_NOT_FOUND`، ولم تكن جلسة تطبيق SimSim موثقة وقابلة للاستخدام في المتصفح. لم تُستخدم بيانات دخول أو أي وسيلة لتجاوز المصادقة. يلزم فتح رابط إنتاج أو Preview صالح ضمن جلسة مستخدم موثقة لفحص 320px و360px و390px و430px وDesktop على بيانات حقيقية.

## الملفات المعدلة

| الملف | الحالة |
|---|---|
| `src/pages/Tables.jsx` | معدل: Visual + UX Refinement فقط |
| `TABLES_VISUAL_UX_REFINEMENT_REPORT.md` | جديد: توثيق النطاق والنتائج |

> التعديلات المحلية جاهزة للمراجعة وإنشاء Pull Request عند الطلب.
