# تقرير: مراجعة وإصلاح شاملة للـ Public Menu وربطه بـ Dashboard
## Public Menu ↔ Dashboard — Comprehensive Review & Fix — Final Report

**التاريخ:** 2026-09-06
**النطاق:** `menu-next` (Next.js customer menu) + قاعدة البيانات المشتركة (Supabase Postgres)
**الحالة:** لم يتم عمل commit/push/PR/merge بعد — بانتظار مراجعتك واعتمادك صراحة (كما طلبت في القاعدة/القسم 16).

---

## 1) Executive Summary (ملخص تنفيذي)

هذه المهمة امتداد مباشر لمراجعات Phase 5 وPhase 6 السابقة (المدموجتين بالفعل في `main`)، وتغطي فجوتين حقيقيتين لم تكونا مغطاتين من قبل:

1. **نظام الطاولات — المسار الثاني (Branch URL بدون QR):** كان حقل رقم الطاولة عبارة عن مربع نص حر (free text) بلا أي تحقق من وجود الطاولة فعليًا أو انتمائها للفرع الصحيح. تم استبداله بقائمة منسدلة (dropdown) تعرض فقط الطاولات الحقيقية والفعّالة (`status='active'`) الخاصة بهذا الفرع تحديدًا، مسحوبة من قاعدة البيانات مباشرة (لا Mock Data، لا Hardcoding).
2. **شريط الأقسام الأفقي (Categories Navigation):** لم يكن موجودًا إطلاقًا في `menu-next` — تمت إضافته بنفس سلوك المنيو القديم (تمرير أفقي بصف واحد، تبويب نشط بخط عريض + خط سفلي متحرك، Scroll-Spy تلقائي، تمرير-عند-الضغط)، دون نقل قائمة "☰ كل الأقسام" الإضافية بناءً على طلبك الصريح هذه المرة.

بالإضافة إلى ذلك، تم تمديد `create_order` (أهم RPC في النظام) بشكل **متوافق للخلف بالكامل** لدعم `p_table_id` الحقيقي، مع تحقق صارم من أن الطاولة تنتمي فعليًا لنفس المطعم والفرع — أي محاولة استخدام `table_id` من فرع آخر تُرفض من السيرفر قبل أي عملية كتابة.

كل بنود Phase 5/6 (menu_layout، الغلاف/Hero، وقت التحضير، رسوم التوصيل، العروض، عزل الفروع) تم التحقق من استمرار عملها بلا أي انحدار (regression) عبر 55 اختبارًا حيًا كاملًا على `https://simsimmenu.com`.

---

## 2) المشاكل التي تم اكتشافها (Problems Found)

| # | المشكلة | الخطورة |
|---|---------|---------|
| 1 | حقل رقم الطاولة في مسار Branch URL (بدون QR) كان نصًا حرًا — لا تحقق، لا ربط بجدول `restaurant_tables`، والفاتورة تعرض نصًا كتبه العميل وليس طاولة حقيقية | عالية — يؤثر مباشرة على دقة الفاتورة وربط الطلب بالطاولة الصحيحة |
| 2 | لا يوجد أي شريط أقسام أفقي في `menu-next` — العميل يعتمد فقط على التمرير اليدوي الطويل لإيجاد قسم معين، بعكس المنيو القديم | متوسطة — تجربة استخدام، ليست خطأ وظيفي |
| 3 | `create_order` لا يملك أي وسيلة لاستقبال `table_id` حقيقي — كان يعتمد فقط على نص حر (`p_table_number`) بلا أي تحقق من قاعدة البيانات | عالية — نفس أثر المشكلة رقم 1 من ناحية دقة البيانات |
| 4 | لا يوجد RPC يعرض طاولات الفرع الحقيقية للعميل — `restaurant_tables` غير قابل للقراءة مباشرة من `anon` (تم التأكد من ذلك بمحاولة قراءة مباشرة رجعت "permission denied") | تصميمية — كانت تمنع أي حل صحيح لمشكلة 1 قبل هذه المهمة |

**لم يتم اكتشاف أي مشاكل جديدة** في: menu_layout، الغلاف (cover_url)، وقت التحضير، رسوم التوصيل، شارة العروض، عزل الفروع، Table QR (المسار الأول) — كل هذه أُصلحت في Phase 5/6 وما زالت تعمل بشكل صحيح (تم التحقق مجددًا، انظر القسم 4-9 أدناه).

---

## 3) المشاكل التي تم إصلاحها (Fixes Applied)

- ✅ إضافة `p_table_id uuid DEFAULT NULL` إلى `create_order` (متوافق للخلف 100% — كل الاستدعاءات الحالية بلا هذا الوسيط تعمل بلا أي تغيير).
- ✅ عند تمرير `p_table_id`: يتم التحقق من أنه يخص نفس `restaurant_id` و`branch_id` وأن حالته `active`، وإلا يتم رفض الطلب برسالة `invalid table selection` **قبل** أي إدراج في قاعدة البيانات.
- ✅ رقم الطاولة النهائي في الطلب أصبح مشتقًا من سجل الطاولة الحقيقي نفسه (`v_final_table_number`) وليس من نص أرسله العميل — يمنع أي تلاعب.
- ✅ إنشاء RPC جديد للقراءة فقط: `get_branch_tables_for_menu(p_restaurant_slug, p_branch_id)` يعيد فقط `{id, table_number}` للطاولات الفعّالة في هذا الفرع تحديدًا، بعد التحقق من أن الفرع فعلاً يخص هذا المطعم (Slug).
- ✅ واجهة Checkout: عند وجود طاولات حقيقية للفرع، يظهر Dropdown بدلاً من خانة النص الحرة؛ وإذا لم يكن للفرع أي طاولات مُدخلة في Dashboard، يبقى الحقل النصي الأصلي كما هو (بدون أي كسر لسلوك موجود).
- ✅ إضافة شريط أقسام أفقي جديد (`CategoryNav`) بنفس منطق Scroll-Spy والتمرير عند الضغط من المنيو القديم تمامًا (نفس `rootMargin`/`threshold`/منطق اختيار أعلى قسم ظاهر).
- ✅ إضافة رسالة خطأ عربية/إنجليزية لحالة `invalid table selection` في `orderErrors.ts`.

---

## 4) مسار Table QR — قبل / بعد (لم يتغيّر، تم التحقق فقط)

**قبل وبعد:** لا تغيير على الإطلاق في هذا المسار خلال هذه المهمة — كان يعمل بشكل صحيح منذ Phase 6 (`create_order_from_table_qr` يتحقق من التوكن سيرفريًا، يقفل الفرع/الطاولة، ويمنع أي تعديل عبر الرابط). تم التحقق مجددًا حيًا (اختبارات #27، #38-41 في القائمة أدناه) ولا يزال يعمل كما هو.

## 5) مسار Branch URL (بدون QR) — قبل / بعد

| | قبل | بعد |
|---|---|---|
| حقل الطاولة | نص حر بلا تحقق | Dropdown من طاولات حقيقية فقط عند وجودها |
| القيمة المُرسلة لـ `create_order` | `p_table_number` نصي فقط | `p_table_id` (حقيقي، مُتحقق منه سيرفريًا) + `p_table_number` (متوافقية) |
| الحماية من فرع آخر | غير موجودة | مرفوضة سيرفريًا (`invalid table selection`) — تم اختبارها مباشرة عبر SQL قبل النشر |
| فرع بلا طاولات مُدخلة في Dashboard | نص حر | يبقى نص حر (لا كسر) |

## 6) تدفق الأقسام (Categories Flow)

- المصدر: نفس `categories` المسحوبة أصلاً بواسطة `getVisibleCategories` (فلترة `is_visible=true`، ترتيب `sort_order` — أي ترتيب/ظهور الأقسام في Dashboard ينعكس مباشرة وبلا أي تعديل إضافي).
- تبويبات الشريط لا تشمل الأقسام الاصطناعية (الأكثر مبيعًا يدويًا / المميزة / المفضلة) — فقط الأقسام الحقيقية التي سيظهر لها قسم فعلي (أي لديها منتج واحد على الأقل).
- الضغط على تبويب → `scrollIntoView` لنفس الـ id الموجود مسبقًا في `CategorySection.tsx` (`cat-${id}`) — لم يتم تعديل ذلك الملف إطلاقًا.
- Scroll-Spy عبر `IntersectionObserver` بنفس القيم المستخدمة في المنيو القديم حرفيًا.
- لا توجد قائمة "☰ كل الأقسام" هذه المرة — بناءً على طلبك الصريح.

## 7) روابط Dashboard ↔ Public Menu (ملخص، التفاصيل الكاملة في الجدول بالقسم 10)

كل الحقول المستخدمة هذه المهمة (`restaurant_tables.status`, `restaurant_tables.table_number`, `categories.is_visible`, `categories.sort_order`) هي نفس الحقول التي يكتبها Dashboard فعليًا — لم يُضَف أي عمود جديد ولم يُبتكر أي معنى جديد لحقل موجود.

## 8) الغلاف (Hero) / التأثير الزجاجي (Glass) — تم التحقق فقط، بلا تغيير

لا تغيير في هذه المهمة. تم التحقق حيًا (اختبار #34) من أن `backdrop-filter: blur(...)` ما زال يعمل فعليًا (وليس فقط في CSS) وأن البطاقة الزجاجية تتراكب فوق الغلاف كما صُمم في Phase 6.

## 9) العروض/الخصومات (Offers) وعزل الفروع (Branch Isolation) — تم التحقق فقط، بلا تغيير

لا تغيير. تم التحقق حيًا (اختبارات #36-41) من أن رابط الفرع العادي يُبقي المبدّل قابلاً للاستخدام، وأن رابط الطاولة (QR) يقفله ولا يمكن كسره عبر تعديل `?branch=` يدويًا.

---

## 10) مصفوفة Dashboard ↔ Database ↔ Public Menu

| الميزة | Dashboard | Database | Public Menu | الحالة | ما تم فعله هذه المهمة |
|---|---|---|---|---|---|
| رقم الطاولة (QR) | — (يُصدر QR لكل طاولة) | `restaurant_tables`, `orders.table_id/source` | مقفل تلقائيًا عبر `create_order_from_table_qr` | ✅ يعمل | لا شيء (Phase 6) |
| رقم الطاولة (رابط الفرع بدون QR) | إدارة الطاولات | `restaurant_tables` (عبر RPC جديد للقراءة) | Dropdown حقيقي | ✅ أُصلح الآن | أُصلح هذه المهمة |
| ربط الطلب بالطاولة الحقيقية (`table_id`) في `create_order` العام | — | `orders.table_id` | يُرسل `p_table_id` من الـ Dropdown | ✅ أُصلح الآن | أُصلح هذه المهمة |
| شريط الأقسام الأفقي | ترتيب/تفعيل الأقسام | `categories.sort_order/is_visible` | شريط أفقي + Scroll-Spy | ✅ أُضيف الآن | أُضيف هذه المهمة |
| `menu_layout` (list/grid/showcase/circles) | إعداد شكل عرض الأصناف | `restaurants.menu_layout` | يُطبَّق فعليًا | ✅ يعمل (Phase 5) | تحقق فقط |
| الغلاف (`cover_url`) | رفع صورة الغلاف | `restaurants.cover_url` | Hero banner | ✅ يعمل (Phase 5) | تحقق فقط |
| وقت التحضير المقدّر | تفعيل/إخفاء | `restaurants.show_prep_time` | يظهر في الهيدر | ✅ يعمل (Phase 6) | تحقق فقط |
| رسوم التوصيل في الهيدر | إعداد رسوم التوصيل | `branches.delivery_fee`/`restaurants.delivery_fee` | يظهر مسبقًا قبل السلة | ✅ يعمل (Phase 6) | تحقق فقط |
| البطاقة الزجاجية فوق الغلاف | — | — | CSS، تراكب فعلي | ✅ يعمل (Phase 6) | تحقق فقط |
| عزل الفروع (Branch Isolation) | — | — | قفل المبدّل عند QR فقط | ✅ يعمل (Phase 6) | تحقق فقط |
| العروض/الكوبونات | إدارة العروض | `banners`, `coupons` | شارة + Drawer | ✅ يعمل (Phase 4) | تحقق فقط |

---

## 11) مقارنة بالمنيو القديم (Legacy Comparison)

كل سلوك المنيو القديم ذي الصلة بهذه المهمة (Scroll-Spy، التمرير عند الضغط، الخط السفلي المتحرك) تم نقله بنفس القيم الحرفية (`rootMargin: '-120px 0px -65% 0px'`, `threshold: 0`, منطق "أعلى عنصر ظاهر"). الاستثناء الوحيد المتعمد: قائمة "☰ كل الأقسام" الإضافية — لم تُنقل بناءً على طلبك الصريح هذه المرة (شريط أفقي فقط).

## 12) Branch Menu URL مقابل Table Menu URL

| | Branch Menu URL (`?branch=`) | Table Menu URL (`?table=`) |
|---|---|---|
| الفرع | يُحدَّد من الرابط، قابل للتغيير عبر المبدّل | يُحدَّد من الطاولة نفسها (سيرفريًا)، **لا يتغير أبدًا** حتى مع تعديل `?branch=` يدويًا |
| رقم الطاولة | Dropdown اختياري من طاولات هذا الفرع | مقفل تلقائيًا، بلا أي إدخال يدوي |
| RPC المستخدم عند الطلب | `create_order` (+ `p_table_id` اختياري) | `create_order_from_table_qr` |

تم اختبار محاولة التلاعب بالرابط (`?branch=<فرع آخر>&table=<توكن طاولة حقيقي>`) مباشرة — الفرع الفعلي المُقدَّم هو فرع الطاولة دائمًا، وليس الفرع المكتوب يدويًا (اختبار #38).

---

## 13) الاختبارات المنفذة (قائمة كاملة) والعدد

**العدد الإجمالي: 55 اختبارًا حيًا على `https://simsimmenu.com`** (49 اختبار Regression من Phase 3-6 + 6 اختبارات جديدة لهذه المهمة) — تشغيل حقيقي بمتصفح حقيقي (Playwright/Chromium)، Worker واحد، بلا اعتراض إلا لطلبات `create_order`/`create_order_from_table_qr` فقط (لمنع إنشاء طلبات حقيقية أثناء الاختبار).

الاختبارات الجديدة الستة (`tests/e2e/public-menu-audit.spec.ts`):
1. رابط الفرع (بدون QR) في فرع لديه طاولات حقيقية → يظهر Dropdown حقيقي (وليس نصًا)، يحتوي على TABLE 1 و TABLE 5 وعلى الأقل 7 خيارات.
2. فرع بلا طاولات مُدخلة (konoha) → يبقى الحقل نصًا حرًا كما كان (لا انحدار).
3. اختيار طاولة من القائمة → `create_order` يستقبل `p_table_id` و`p_table_number` الصحيحين المطابقين لاختيار العميل فعليًا.
4. شريط الأقسام يظهر بصف أفقي واحد قابل للتمرير، بعدد تبويبات مطابق للأقسام الحقيقية، ولا وجود لزر "☰".
5. الضغط على تبويب ثانٍ يُمرِّر لقسمه فعليًا ويُفعِّله (خط عريض + Class نشط).
6. التمرير اليدوي للصفحة يُحدِّث التبويب النشط تلقائيًا (Scroll-Spy) بلا أي ضغط.

## 14) عدد الاختبارات: النتائج

**55 / 55 ناجح (100%)** — تشغيل واحد كامل، بلا إعادة محاولة (retries: 0).

## 15) الاختبارات الفاشلة وأسبابها

لا يوجد أي اختبار فاشل في هذا التشغيل النهائي. (ملاحظة شفافة: محاولة تشغيل نفس الحزمة محليًا فشلت بسبب قيود شبكة هذه البيئة تحديدًا — انتهاء مهلة تحميل صور Supabase محليًا — وهي مشكلة بيئة الاختبار وليست في الكود؛ ولذلك اعتُمد التحقق الحي على `simsimmenu.com` كما جرى في Phase 6 سابقًا، بعد الحصول على موافقتك الصريحة على النشر أولاً.)

## 16) المشاكل المتبقية (Remaining Issues)

- الفجوة التي وردت في تقرير Phase 5 (أن `create_order` القديم لم يكن يسجل `table_id`/`source` للطلبات العادية غير الـQR) — **تم إغلاقها الآن جزئيًا**: الطلبات عبر Dropdown الجديد تُسجِّل `table_id` حقيقيًا. الطلبات التي يكتب فيها العميل رقم طاولة نصيًا يدويًا (في فرع لم يُدخل مالكه طاولات في Dashboard بعد) تبقى بدون `table_id` — وهذا سلوك متوقع وسليم (لا توجد طاولة حقيقية لربطها به أصلاً)، وليس خطأ.
- لم تتم إضافة أي واجهة في Dashboard نفسه لإدارة/عرض هذه الطاولات — هذه المهمة اقتصرت على جانب Public Menu فقط كما طُلب.

## 17) المخاطر المحتملة (Potential Risks)

- **منخفضة جدًا لـ`create_order`:** التعديل إضافي بالكامل (معامل جديد بقيمة افتراضية `NULL`)، وتم التحقق يدويًا وحيًا قبل وبعد النشر أن كل مسار قديم يعمل بنفس السلوك تمامًا (3 اختبارات SQL مباشرة + 55 اختبار حي شامل).
- **صفر مخاطر على Moyasar/الدفع:** لم يُمسّ أي كود دفع أو orchestration إطلاقًا في هذه المهمة.
- **صفر مخاطر أمنية جديدة:** الدالة الجديدة `get_branch_tables_for_menu` تعرض فقط `{id, table_number}` (لا `qr_token` ولا أي عمود حساس)، وتتحقق من مطابقة الفرع للمطعم عبر الـSlug قبل إرجاع أي نتيجة (تم اختبار ذلك مباشرة بإرجاع نتيجة فارغة لمطعم غير مطابق).

## 18) الخطوات التالية المقترحة (Next Steps)

- بانتظار مراجعتك واعتمادك لعمل commit/push كما طلبت صراحة.
- (اقتراح، ليس قرارًا مني): إن رغبت مستقبلًا، يمكن إضافة واجهة بسيطة في Dashboard لإدارة/تسمية الطاولات مباشرة لو لم تكن موجودة بشكل كافٍ حاليًا — هذا خارج نطاق هذه المهمة تمامًا ولم يُنفَّذ.

---

## 19) الملفات التي تغيّرت (هذه المهمة تحديدًا)

```
menu-next/app/globals.css                      — CSS شريط الأقسام الجديد
menu-next/app/menu/[slug]/page.tsx              — تركيب CategoryNav + حساب navCategories
menu-next/app/menu/[slug]/checkout/page.tsx     — جلب branchTables وتمريرها لـCheckoutForm
menu-next/components/CheckoutForm.tsx           — Dropdown الطاولة + p_table_id في الطلب
menu-next/lib/data.ts                           — getBranchTablesForMenu()
menu-next/lib/types.ts                          — نوع Table الجديد
menu-next/lib/orderErrors.ts                    — رسالة invalid table selection
```

## 20) الملفات الجديدة

```
menu-next/components/CategoryNav.tsx
menu-next/tests/e2e/public-menu-audit.spec.ts
sql migration: add_optional_table_id_to_create_order   (مطبّقة مباشرة على قاعدة البيانات الحية)
sql migration: create_get_branch_tables_for_menu_rpc    (مطبّقة مباشرة على قاعدة البيانات الحية)
```

**ملاحظة:** `menu-next/lib/i18n.ts` و`menu-next/components/RestaurantHeader.tsx` و`menu-next/lib/prepTime.ts` وملفات اختبار Phase 6 الظاهرة كـ"معدّلة/جديدة" في `git status` هي من عمل Phase 6 السابق ولم تُعدَّل في هذه المهمة — لا تزال غير مرفوعة (uncommitted) بناءً على تعليماتك السابقة.

---

## 21) الخلاصة النهائية (Final Verdict)

**هل Public Menu مرتبط فعليًا بـDashboard الآن؟**
نعم، للبنود التي غطتها هذه المهمة (نظام الطاولات بمساريه، الأقسام) بالإضافة لكل ما أُصلح سابقًا في Phase 5/6 (menu_layout، الغلاف، وقت التحضير، رسوم التوصيل، العروض، عزل الفروع). الربط في كل هذه الحالات هو ربط حقيقي عبر قراءة/كتابة مباشرة من نفس الجداول التي يكتب إليها Dashboard — وليس عرضًا شكليًا فقط.

**ما الذي بقي "خاصًا بلوحة التحكم فقط" (Dashboard-only) بلا أثر على العميل؟**
لم يتبقَّ أي بند من القائمة الأصلية التي راجعتها Phase 5/6/هذه المهمة معًا يقع في هذه الفئة — كل ما تم فحصه أصبح متصلاً وفعّالًا في Public Menu.

**ما الذي أصبح فعليًا ميزة عاملة يراها العميل الآن ولم تكن كذلك قبل هذه المهمة؟**
1. اختيار رقم طاولة حقيقي من قائمة فعلية (بدلاً من كتابة نص حر) عند دخول المطعم عبر رابط فرع عادي.
2. شريط تنقل أفقي بين الأقسام مع تمييز القسم النشط تلقائيًا أثناء التمرير.

لا أعتبر المهمة مكتملة لمجرد نجاح الـbuild — تم التحقق من الصحة الوظيفية (اختبارات SQL مباشرة قبل النشر)، الاتصال الفعلي بالبيانات (Dropdown يعرض بيانات حقيقية من قاعدة البيانات)، صحة الواجهة (55 اختبار متصفح حي)، وسلامة الانحدار (كل اختبارات Phase 3-6 ما زالت ناجحة بلا استثناء).

**بانتظار اعتمادك لعمل commit/push — لم يُنفَّذ أي منهما.**
