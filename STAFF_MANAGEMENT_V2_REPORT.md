# تقرير تطوير إدارة الموظفين — SimSim

## النطاق المنفذ

تم تطوير صفحة **الموظفين** في `src/pages/Staff.jsx` ضمن نفس بنية SimSim القائمة. لا تستخدم الصفحة بيانات وهمية؛ ملخصاتها، الموظفون، الحالات، الفروع، والصلاحيات تُقرأ من `restaurant_members` و`branches` الحقيقيين.

| المجال | التنفيذ |
|---|---|
| Header والملخص | ملخص ديناميكي لعدد الموظفين وحالاتهم، وCTA واضح لإضافة موظف |
| البحث والفلاتر | بحث بالاسم/الدور وفلاتر: الكل، المديرون، الكاشير، الموظفون، المفعلون، المعطلون |
| رابط الدخول | بطاقة مدمجة لرابط `/staff-login/{restaurant-slug}` مع نسخ وToast موحد |
| بطاقات الموظفين | الدور، حالة الحساب، الفروع، ملخص الصلاحيات، آخر دخول وآخر نشاط بحالة حقيقية أو غير متاحة |
| الإجراءات | قائمة إجراءات ضيقة: تعديل، تفعيل/تعطيل، وحذف ثانوي مع Confirmation Dialog |
| نموذج الموظف | Role presets قابلة للتعديل، مجموعات صلاحيات، كل الصفحات، تحديد/إلغاء الكل، وفروع متعددة |
| الاستجابة | Cards متجاوبة وBottom Sheet للمودال على الهاتف مع محتوى قابل للتمرير وRTL |

## نموذج الفروع والصلاحيات

أضيفت Migration `sql/staff_management_v2.sql`، ثم ترقية صلاحيات في `sql/staff_management_security_grants.sql`، ثم تحصين `SECURITY INVOKER` في `sql/staff_management_security_invoker_hardening.sql`.

| الحقل أو القاعدة | السلوك |
|---|---|
| `branch_scope` | قيمة معيارية: `all` أو `selected` |
| `branch_ids uuid[]` | يحفظ فرعًا واحدًا أو عدة فروع عند اختيار النطاق المحدد |
| Trigger تحقق | يمنع اختيارًا فارغًا، ويمنع ربط الموظف بفرع من مطعم آخر، ويزيل تكرار المعرفات |
| `member_has_branch_access` | دالة مركزية تستخدمها سياسات RLS للطلبات والطاولات |
| Orders RLS | يحصر القراءة والكتابة ضمن فروع الموظف المصرح بها |
| Tables RLS | يمنح الموظف الموثق قراءة طاولات فروعه فقط؛ قراءة العميل العامة النشطة تبقى لمسار العميل |
| وظائف النطاق | حولت إلى `SECURITY INVOKER`، وأُزيل تنفيذ anon؛ Trigger التحقق غير قابل للاستدعاء عبر API |

قوالب الأدوار الآن قابلة للتوسع: المدير يحصل على كل الصفحات التشغيلية (`all`) مع بقاء الصفحات الحصرية للمالك محمية في `canAccess`، والكاشير يحصل على الطلبات والطاولات وQR، والموظف يبدأ بالطلبات. يظل المدير قادرًا على تعديل الصلاحيات يدويًا بعد اختيار الدور.

> لا توجد حاليًا بيانات حقيقية لسجل آخر نشاط أو آخر دخول داخل `restaurant_members`. لذلك تعرض البطاقات «لم يسجل دخولًا بعد» و«غير متوفر بعد» بدلاً من اختلاق بيانات. إذا أضيفت حقول حقيقية مستقبلًا، تعرضها الصفحة تلقائيًا.

## التحقق المنفذ

| الفحص | النتيجة |
|---|---|
| بناء Vite | ناجح |
| اختبارات Vitest | 231 اختبارًا ناجحًا ضمن 9 ملفات |
| فحص Registry | ناجح، 34 قدرة متطابقة مع قاعدة البيانات |
| `git diff --check` | ناجح |
| Migration الحية | تمت إضافة `branch_ids`، ولا توجد عضويات `selected` بلا فروع، وسياسات Orders/Tables موجودة |
| مستشار أمان Supabase | لا توجد تحذيرات تخص `validate_member_branch_selection` أو `member_has_branch_access` أو `member_branch_scope` بعد التحصين |
| Visual QA حي | التطبيق حمّل في بيئة QA لكنه أعاد التوجيه إلى `/login` لعدم توفر جلسة مالك موثقة؛ لم تُدخل بيانات ولم يُتجاوز Authentication |
| Functional QA يغير البيانات | لم يُنشأ موظف تجريبي أو يُحذف أي حساب حقيقي؛ المسارات موجودة وتغطيها الفحوصات التقنية لكن اختبار CRUD التشغيلي يحتاج جلسة مالك موثقة وموافقة على إنشاء/حذف حساب اختبار |

## الملفات الأساسية

- `src/pages/Staff.jsx`
- `src/lib/permissions.js`
- `src/lib/permissions.test.js`
- `sql/staff_management_v2.sql`
- `sql/staff_management_security_grants.sql`
- `sql/staff_management_security_invoker_hardening.sql`
- `qa/staff_architecture_findings.md`
- `qa/staff_visual_qa.md`
