# سمسم — حالة المشروع التقنية (PROJECT_STATE)

> ملف حيّ يوثّق القرارات المعمارية وحالة الملفات. يُحدّث مع كل قرار مهم.
> **اتفاقية مصدر الحقيقة:** آخر نسخة من ملف سلّمها الفريق التقني تُعتبر هي المنشورة، ما لم يُذكر خلاف ذلك.

---

## 1) الـ Stack
- **الواجهة:** React + Vite، عربية RTL، خطوط Tajawal (عربي) + Poppins (لاتيني/الـWordmark)، هوية SIMSIM المعتمدة: برتقالي **#FF6A00** / أسود **#0B0B0F** + رمز S رسمي (`public/simsim-s.svg`) — راجع ADR-43.
- **الخلفية:** Supabase (Postgres + Auth + Realtime + Storage + Edge Functions) + RLS مفعّلة على كل الجداول.
- **النشر:** Vercel. **الدومين الرسمي الأساسي:** `https://simsimmenu.com` (و`www.simsimmenu.com` يُحوَّل إليه). الدومين القديم `simsim50.vercel.app` ما زال مرتبطاً بالمشروع (يُبقى حيّاً لأن رموز QR المطبوعة/المُشارَكة سابقاً تشير إليه). **الدمج في main = النشر** (إنشاء PR وحده لا ينشر).
  - ملاحظة: التطبيق نسبيّ الروابط بالكامل (`window.location.origin`) فيعمل على أي دومين تلقائياً. إعداد خارجي يجب ضبطه في لوحة Supabase (خارج الكود): Authentication → URL Configuration → Site URL = `https://simsimmenu.com` + إضافة `https://simsimmenu.com/**` إلى Redirect URLs (لعمل روابط إعادة تعيين كلمة المرور/تأكيد الإيميل على الدومين الجديد).
- **الحماية:** فرع `main` محمي — CI «Build (Vite)» إلزامي قبل الدمج + منع force push + منع الحذف.
- **الحالة:** `authStore` (Zustand) — يوفّر `user`, `restaurant`, `fetchRestaurant`, `signOut`, `isOwner`, `membership`.

## 2) بنية المجلدات (المعتمدة)
```
src/
  components/AppShell.jsx      ← التخطيط الموحّد (سايدبار + توب-بار + تجاوب)
  hooks/useBreakpoint.js       ← نقطة التحوّل الحيّة المشتركة
  lib/nav.js                   ← مصدر روابط التنقل الوحيد
  lib/pricing.js               ← مصدر منطق التسعير/الضريبة الوحيد (ADR-1)
  lib/permissions.js           ← منطق صلاحيات الموظفين (canAccess, navPage)
  lib/supabase.js , uploadImage.js
  store/authStore
  features/menu/               ← منيو الزبون مفكّك بالكامل:
      MenuHeader, MenuBody, ProductItem, HProductCard (بطاقة أفقية),
      ProductModal, CartDrawer, AllergensModal, MenuSkeleton,
      SocialIcons, ErrBoundary, i18n, whatsapp, helpers
    features/menu/ (شاشة طلباتي — ADR-11): OrdersScreen (تجميع), OrdersHeader (KPIs),
      LoyaltyCard, OrderCardActive, OrderCardCollapsed, OrderItemRow, EmptyOrders
    features/menu/hooks/: useLang, useMenuData, useActiveOrders, useCart,
      useCheckout, useLoyalty, useReviews
  pages/*.jsx                  ← تشمل Staff (إدارة الموظفين) و StaffLogin (دخول الموظف)
                                  Tables.jsx (إدارة الطاولات — ADR-12)
                                  PublicMenu.jsx صفحة تركيب نحيفة (~300 سطر)
  lib/tablesApi.js              ← CRUD موحّد لجدول restaurant_tables (لوحة التحكم + المنيو العام)
  features/menu/TableSelect.jsx + hooks/useTables.js ← قائمة اختيار الطاولة في السلة (ADR-12)
  lib/recommendationsApi.js     ← CRUD + خريطة القواعد المفعّلة لمحرك الاقتراحات (ADR-13)
  features/menu/hooks/useRecommendationRules.js + useSmartSuggestions.js ← محرك الاقتراحات الذكي (ADR-13)
  lib/branchesApi.js            ← CRUD للفروع + نسخ المنيو عند إنشاء فرع جديد (ADR-22)
  pages/Branches.jsx             ← إدارة الفروع (لوحة التحكم) — ADR-22
  admin/                       ← وحدة Super Admin معزولة (تحميل كسول) — ADR-27→33، ADR-41
      AdminShell.jsx (قشرة داكنة/بنفسجية) · AdminLogin.jsx · RequirePlatformAdmin
      theme.js · admin-theme.css (تُوكِنز ألوان + :focus-visible موحّدة — ADR-41)
      adminNav.js · components/CommandPalette.jsx (بحث ⌘K — ADR-32)
      components/ui/: Button · Modal(+Focus Trap/Esc) · Field · Badge · States · Skeleton · Icon — ADR-41
      features/: dashboard · restaurants · billing · admins · audit
                 growth · flags · announcements (كلٌّ صفحة + api كسولان)
  payments/                    ← أساس الدفع (محايد للمزوّد) — ADR-34؛ Moyasar مُفعَّل فعلياً (Payment-First Checkout) — ADR-52
      types · contracts/PaymentAdapter · adapters(فارغ) · services · utils
  integration/                 ← طبقة التكامل (البوابة الموحّدة، خاملة) — ADR-35
      contracts · adapters · providers(فارغ) · registry · factories · config
      errors · events · webhooks · logs · dto · types · utils · tests
  components/NotificationsBell.jsx ← جرس إشعارات المطعم (ADR-33/م2)
  observability/               ← تجريد Logging/ErrorReporting موحّد (خامل) — ADR-36/م1
  config/                      ← مصدر الإعدادات غير السرّية الموحّد (appConfig) — ADR-36/م2
  lib/loyaltyApi.js            ← طبقة وصول بيانات الولاء (صفحة مرجعية) — ADR-36/م3
LAYERS.md                      ← الدستور المعماري (الطبقات + قواعد التواصل) — ADR-36/م0
sql/                           ← ملفات SQL تُحفظ هنا وتُنفَّذ عبر موصّل Supabase بعد موافقة المالك
```
- **خادمية:** Edge Function `delete-staff` (service_role) — حذف كامل لحساب الموظف بعد التحقق من المالك.

## 3) القرارات المعمارية الموثّقة (ADR مختصر)
- **[ADR-1] الضريبة = خيار (أ):** السعر المعروض **شامل ض.ق.م 15%**. تُفكّ للخلف: `net = (total - delivery) / 1.15`, `tax = gross - net`. **المنطق موحّد في `lib/pricing.js`** (`VAT_RATE`, `vatBreakdown`, `orderBreakdown`, `itemsGross`) ويستهلكه: PublicMenu (السلة/الإنشاء)، Orders (الفاتورة + تعليم غير متوفر)، Analytics (صافي/محصّلة). يعمل للطلبات القديمة والجديدة معاً.
- **[ADR-2] الولاء:** النقاط تُحسب حيّاً من الطلبات المكتملة (`earned = floor(Σtotal × rate)`) − الاستبدالات. لا يوجد ledger للنقاط.
- **[ADR-3] صلاحيات الطلبات:** المطعم **ممنوع** يعدّل كمية/يحذف صنفاً (فقط "غير متوفر"). الإلغاء **قبل القبول (pending) فقط**. زر تراجع مؤقت مسموح.
- **[ADR-4] أوقات العمل:** `opening_hours` JSONB على `restaurants` وعلى `branches` (كل فرع ساعاته المستقلة تماماً منذ ADR-22؛ لا يوجد fallback على `restaurants.opening_hours` بعد الآن — كل فرع بما فيه الأساسي له صف حقيقي). `null = مفتوح دائماً`. `is_active` = غلق يدوي فوري منفصل.
- **[ADR-5] التخطيط المشترك (AppShell):** كل صفحات اللوحة تستخدم `<AppShell>`. ممنوع تكرار السايدبار/التوب-بار داخل الصفحات.
- **[ADR-6] المنيو ثنائي اللغة (عربي/إنجليزي):** أعمدة `_en` منفصلة في الجداول (وصف المطعم، الأقسام، الأصناف، الفروع، المسبّبات). اسم المطعم **لا يُترجم** (قرار المؤسس). زر تبديل 🌐 مع حفظ الاختيار في localStorage و**fallback للعربي** عند غياب الترجمة. **الاتجاه ثابت RTL** (قلب LTR يكسر التصميم — مجرّب). المسبّبات data-driven عبر `label_en` مدمجة (وليس خريطة ثوابت منفصلة).
- **[ADR-7] ألوان المنيو المخصّصة:** أعمدة اختيارية `price_color` و`description_color` (NULL = الافتراضي)، تُدار من الإعدادات.
- **[ADR-8] مصادقة الموظفين:** إيميل وهمي حتمي `username.slug@staff.simsim.app` يُنشأ عبر **عميل Supabase ثانوي** (`persistSession:false`) حتى لا يُخرج صاحب المطعم من جلسته — بدل بناء مصادقة مخصّصة أو كشف service_role في المتصفح. يتطلب **تعطيل تأكيد الإيميل** في Supabase.
- **[ADR-9] خصوصية الطلبات:** أُغلقت ثغرة `orders_select_public` (كانت تكشف بيانات العملاء). البديل: دالتا RPC آمنتان `get_active_orders_count` + `get_recent_order_items`، وحُذفت السياسة المكشوفة. أُضيفت لاحقاً `get_restaurant_rating` (متوسط النجوم + العدد فقط) على نفس النمط.
- **[ADR-10] هندسة منيو الزبون (يوليو 2026):** بأسلوب تطبيقات التوصيل — هيرو مصوّر بكامل العرض (170px، تدرّج بلون الهوية لمن بلا غلاف) + أزرار عائمة (لغة/بحث/طلباتي بعداد) + ورقة بيضاء منزلقة تحوي: الهوية + التقييم ★ + الوصف (**يُعرض بحد 105 أحرف**) + الموقع/التواصل/المسبّبات + بطاقة الحالة (مفتوح/تجهيز/توصيل) + بانر الولاء 🎁 للزبون المعروف، ثم تبويبات نصية بخط سفلي + زر ☰ لكل الأقسام. **قرار المالك الثابت: طرق عرض الأصناف الثلاث (قائمة/شبكة/دوائر) يحددها صاحب المطعم — لا تُمس.** الأولوية: الأقسام والأكثر مبيعاً ظاهرة من أول شاشة.
- **[ADR-11] قائمتا صدارة المنيو + إعادة تصميم «طلباتي» (يوليو 2026):**
  - **قائمتان أعلى المنيو:** «يعجب زبائننا 👌» (تلقائية من المبيعات، **شريط أفقي** 4 أصناف عبر `HProductCard`) ثم «الأكثر طلباً 🔥» (يدوية = `is_featured`، **بطاقات مربعة/grid دائماً** بغضّ النظر عن تخطيط المطعم). أي صنف `is_featured` يحمل علامة «🔥 الأكثر طلباً» ويغذّي **اقتراحات السلة**. زر الإدارة أُعيد تسميته «⭐ مميز» → «🔥 الأكثر طلباً».
  - **شاشة طلباتي (مرحلة 1):** حساب عميل احترافي — هيدر KPIs + بطاقة ولاء بريميوم + أزرار للأعلى + فلاتر لاصقة (الكل/الحالية/المكتملة/الملغية) + بحث برقم الطلب. **النشط** مفتوح (Timeline + وقت متوقّع + رسالة للمطعم + إلغاء)، و**السابق منطوٍ** في صفّ مدمج + زر **«اطلب تاني 🔄»** (`reorderToCart`: يطابق كل صنف بالمنتج الحالي، يتجاهل غير المتوفر). أصناف بالصور والإضافات + حالة فارغة + حركة دخول.
  - **مرحلة 2 لطلباتي (مؤجّلة — تحتاج خلفية):** سجلّ كامل + إنفاق مدى الحياة + مستوى عضوية + فاتورة PDF + Skeleton الطلبات. الحالي = تتبّع محلي (localStorage، 12 ساعة).
- **[ADR-12] إدارة الطاولات (يوليو 2026):** علاقة **Restaurant → Tables** (واحد لكثير، بلا ربط بالفرع). جدول `restaurant_tables` (`id, restaurant_id, table_number, status, sort_order, created_at, updated_at`) بقيد تفرّد `(restaurant_id, table_number)` — يمنع تكرار الرقم داخل نفس المطعم، ويسمح به بين مطاعم مختلفة. لوحة تحكم جديدة «🪑 الطاولات» (إضافة/تعديل/حذف/تفعيل/تعطيل/ترتيب يدوي/بحث). في السلة: حقل رقم الطاولة (طلب داخل المطعم فقط) أصبح **Dropdown** (`TableSelect.jsx`، ورقة منزلقة) يعرض فقط الطاولات المفعّلة مرتّبة حسب `sort_order` — بلا كتابة يدوية. **شبكة أمان:** لو المطعم بلا طاولات مُعرَّفة بعد، يتراجع الحقل تلقائياً للنص الحر القديم (صفر انقطاع للمطاعم الحالية). `id` الطاولة الفريد جاهز كأساس لتوسّع مستقبلي (QR لكل طاولة، حالة ديناميكية مشغولة/فارغة، ربط مباشر بالطلب).
- **[ADR-13] محرك الاقتراحات الذكي (يوليو 2026):** استبدال الاعتماد الكامل على `is_featured` بمحرك ذي أولويات متسلسلة (Chain of Responsibility) — **1) قواعد يدوية** (`product_recommendations`: `source_product_id → recommended_product_id`، يربطها المطعم من نافذة تعديل الصنف) **2) نفس القسم 3) الأكثر مبيعاً** (شبكة أمان أخيرة، لا مصدر أول كالسابق). كل مصدر دالة خالصة تلتزم بواجهة `{product, reason}` موحّدة — إضافة مصادر مستقبلية (Frequently Bought Together، AI، شخصنة) تعني مصدراً جديداً بنفس الشكل بلا إعادة كتابة. الحساب بالكامل من بيانات محمَّلة مسبقاً (`useRecommendationRules` تحمّل القواعد مرة واحدة مع فتح المنيو) ومُخزَّن مؤقتاً (`useSmartSuggestions` عبر `useMemo` على معرّفات السلة فقط، لا الكمية) — صفر استعلامات إضافية أثناء التفاعل مع السلة. في السلة: قسم «🍽️ أكمل وجبتك» (بدل «يكمّل طلبك») ببطاقات أكبر (56px) وشارة سبب لكل اقتراح (يُكمّل طلبك/من نفس القسم/الأكثر طلباً)؛ الأصناف ذات الخيارات الإجبارية لم تعد تُستبعَد — الضغط عليها يفتح نافذة الصنف للاختيار بدل الإضافة الفورية. مفتاحان جديدان على `restaurants`: `recommendations_enabled` و`recommendations_count` (تُدار من الإعدادات). **مؤجَّل (يحتاج قراراً منفصلاً):** مرحلة "يُشترى معاً" (تحتاج View محسوبة دورياً فوق `orders.items` JSONB) ومرحلة "الأعلى ربحية" (تحتاج عمود `cost_price` جديد على `products` — بيانات مالية حسّاسة).
- **[ADR-14] مرونة بطاقة هوية المطعم (يوليو 2026):** أول تطبيق لمبدأ «فلسفة تصميم البطاقة» — كل قسم في بطاقة المنيو الهوية (الوصف، وسائل التواصل، مسبّبات الحساسية، حالة الفتح/ساعات العمل، وقت التجهيز) أصبح Component مستقل التفعيل عبر مفتاح boolean على `restaurants` (`show_description`, `show_social_links`, `show_allergens`, `show_hours`, `show_prep_time`) — **بمعزل تام عن وجود بياناته**، خلافاً للسلوك السابق (إظهار/إخفاء تلقائي حسب البيانات فقط). تُدار الخمسة مفاتيح من بطاقة إعدادات جديدة «🎛️ عناصر بطاقة المطعم». كل عمود افتراضه `true` فلا يتأثر أي مطعم قائم. حالة الفتح ووقت التجهيز مفتاحان مستقلان تماماً (قرار صريح من المالك) رغم عرضهما في نفس صفّ الإحصائيات. رسوم التوصيل تبقى محكومة بمفتاحها القديم `delivery_enabled` (مرن أصلاً، لا حاجة لعمود جديد). **مؤجَّل:** المبدأ السادس من الفلسفة (Sticky Header بأسلوب Morph Animation للبطاقة نفسها عند التمرير) — لم يُطلَب تنفيذه بعد.
- **[ADR-15] Sticky Header بأسلوب Morph (يوليو 2026، مُحسَّن):** تطبيق المبدأ السادس من فلسفة تصميم البطاقة. نفس بطاقة الهوية العائمة (وليس هيدر منفصل جديد) تتحوّل **تدريجياً ومستمراً** لنسخة مصغّرة لاصقة أعلى الشاشة (`position: sticky`)، مرتبطة فعلياً بموضع التمرير عبر قيمة `progress` (0..1) محسوبة من `window.scrollY` بمستمع `scroll` مُقيَّد بـ`requestAnimationFrame` (بلا مكتبة رسوم متحركة). **الهيرو** أصبح هو الآخر `sticky` داخل حاوية أطول قليلاً فيبقى ثابتاً بصرياً بينما تنزلق البطاقة فوقه، ثم يتلاشى (`opacity`) فقط قرب اكتمال التحوّل. **الانكماش بالأولوية:** العناصر الثانوية تتلاشى بترتيب متدرّج ومتداخل (بطاقة الولاء ← وقت التجهيز/ساعات العمل + صفّ الفرع ← التواصل/المسبّبات ← الوصف/الموقع) عبر `opacity`/`transform` محسوبة مباشرة من `progress` بلا CSS transition (لتفادي تأخّر ملاحقة القيمة المستمرة). **حالة الفتح/الإغلاق** عنصر مستقل صغير يبقى ظاهراً دائماً (لا يتلاشى مع خلية الإحصائيات). النسخة النهائية المصغّرة (~70-80px): **شعار + اسم + تقييم + حالة الفتح** فقط (قرار صريح من المالك). طيّ المساحة الفعلي (`max-height`) يحدث مرة واحدة فقط بعد اكتمال التلاشي (لا قفز مرئي، لأن كل شيء شفاف بالفعل عندها) — أداء 60fps بلا تغييرات Layout كل إطار. ظل متدرّج خفيف وثابت بين البطاقة ومنطقة المنتجات لانتقال بصري أنعم. كل التعديل داخل `MenuHeader.jsx` فقط — لا تغيير في قاعدة البيانات ولا في مكوّنات أخرى، ولا مكتبة رسوم متحركة جديدة (Framer Motion كانت خياراً بديلاً، رُفض للحفاظ على خفة الحزمة). **تطوّر لاحق (بعد تحليل فيديوهات المالك):** آلية `progress`/`fade`/`collapse` الهشّة استُبدلت أولاً بنمط sticky كلاسيكي (صفّ الهوية `position:sticky` يبقى ظاهراً 100% والمحتوى ينزلق خلفه)، ثم بـ**هيدر مصغّر دائم منفصل** (`fixed`، دائم التركيب، يظهر بتلاشٍ عند التمرير فوق شريط الأقسام `top:56px` في `MenuBody`) — يبقى ثابتاً طوال تصفّح المنتجات بلا وميض، وهو الحل النهائي المستقر.
- **[ADR-16] مكوّن Accordion مشترك + دمج مفاتيح البطاقة مع محتواها (يوليو 2026):** مكوّن `components/Accordion.jsx` عام (`Accordion` + `AccordionItem`) قابل لإعادة الاستخدام في أي قسم كثيف الإعدادات — عنوان + وصف مختصر + مفتاح إظهار/إخفاء اختياري في الرأس + محتوى قابل للطي. طُبّق على «عناصر بطاقة المطعم» فدمج كل مفتاح `show_X` مع محتواه الفعلي (الوصف/التواصل/الحساسية) بدل الفصل بينهما، ودمج 3 بطاقات في واحدة. **هو أساس قابلية التوسع:** أي عنصر بطاقة مستقبلي = صفّ Accordion جديد بلا إطالة الشاشة.
- **[ADR-17] إعادة تنظيم معماريّة لوحة التحكم (يوليو 2026):** بعد مراجعة UX شاملة (تقرير مُعتمد من المالك) على 3 مراحل بلا أي تغيير في قاعدة البيانات: **(0)** توحيد المكرّرات — إعدادات «اقتراحات السلة» (تفعيل+عدد) انتقلت من الإعدادات إلى تبويب الاقتراحات في صفحة المنيو (مصدر واحد)، ودُمج رابطا «الأقسام/الأصناف» في رابط «المنيو». **(1)** تجميع السايدبار: `nav.js` أصبح `NAV_GROUPS` (5 مجموعات: يومي/المنيو/العملاء/الإعداد/تحليل وضبط) بدل قائمة مسطّحة من 12؛ `AppShell` يعرض عناوين المجموعات ويُسقط الفارغة بعد فلترة الصلاحيات. **(2)** مكوّن Accordion (ADR-16). **(3)** إعادة توزيع تبويبات الإعدادات من «المطعم المكدّس (7 بطاقات) + أوقات + ملف + كلمة مرور + متقدّم» إلى 5 تبويبات موضوعية: 🏪 المطعم · 🎨 العرض · 🚦 التشغيل · 👤 الحساب · ⚠️ الخطر. التنفيذ في الإعدادات بأقل حركة (تغيير شرط التبويب لكل بطاقة، بلا نقل كتل)، والتبويبات التي تحرّر `restForm` تتشارك زر الحفظ.
- **[ADR-18] إعادة بناء صفحة العملاء (يوليو 2026):** بعد مراجعة UX شاملة (تقرير مُعتمد من المالك)، تحويل الصفحة من قائمة عرض إلى لوحة قرار. **حقيقة معماريّة حاسمة:** لا يوجد جدول `customers` — كل عميل مُشتقّ حيّاً من `orders` بتجميع حسب رقم الجوال (المفتاح الفعلي لهوية العميل). **إصلاح:** إحصائيات الإنفاق/عدد الطلبات كانت تشمل الطلبات `cancelled` خطأً — استُثنيت الآن من كل الحسابات (تبقى ظاهرة في سجل الطلبات معلَّمة فقط). **إحصائيات:** 6 أساسية + 4 خلف "عرض المزيد". **رؤى ذكية:** شريط بطاقات مستقلة (كل رؤية دالة خالصة تُخفي نفسها بلا بيانات — Modular لإضافة رؤى مستقبلية بلا إعادة تصميم). **شارات:** شارة مستوى واحدة دائماً (VIP/مميز/جديد، نفس عتبات 10/3 التاريخية) + شارة ظرفية اختيارية بالأولوية (أفضل عميل ← قريب من الترقية ← نشط جداً ← لم يطلب منذ فترة) — يسمح بدمج VIP+توقّف النشاط بدل إخفاء إشارة. **البطاقة:** إجراءات سريعة (اتصال/واتساب/الطلبات/المكافآت). **الفلاتر:** بحث موحّد + فرع (بعدد عملائه) + حالة + مستوى + ترتيب، وBottom Sheet لفلتر الحد الأدنى للإنفاق. **لوحة العميل:** Bottom Sheet بتبويبات (نظرة عامة/سجل الطلبات/الولاء) بدل التوسّع المكاني القديم؛ تبويب الولاء يظهر فقط لو `loyalty_programs.enabled`. **مؤجَّل بقرار المالك:** تاريخ الميلاد (لا مكان لجمعه من الزبون) وملاحظات صاحب المطعم (غير مطلوبة، لم يُبنَ لها جدول). **الأداء:** جلب واحد عند الدخول (orders/branches/loyalty_programs/loyalty_redemptions)، وكل بحث/فلتر/فرز/تفاصيل محسوب بـ`useMemo` من نفس البيانات — صفر استعلامات إضافية. **Suggestion مفتوحة:** صفحة العملاء ليست ضمن `SHARED_PAGES` في `lib/permissions.js` فقد يرى موظف مقيَّد بفرع بيانات عملاء كل الفروع (سلوك سابق لهذه المهمة، لم يُلمَس).
- **[ADR-19] قفل تمرير الصفحة الخلفية موحّد (يوليو 2026):** مشكلة متكرّرة عبر التطبيق — التمرير داخل أي نافذة/قائمة منزلقة مفتوحة (Modal/Bottom Sheet) كان يُمرّر الصفحة الخلفية معها. كان الإصلاح مطبَّقاً سابقاً في مكانين فقط بكود مكرّر (`ProductModal`, `CartDrawer`) وغائباً عن 11 موضعاً آخر. الحل: خطّاف مشترك `hooks/useBodyScrollLock.js` بعدّاد مرجعي (`lockCount`) يدعم تراكب أكثر من نافذة مفتوحة معاً (مثال: تأكيد فوق تفاصيل) بلا أن يُعيد إغلاق إحداها فتح التمرير قبل إغلاق البقية. **قاعدة ثابتة من الآن:** أي نافذة/قائمة منزلقة جديدة (`position:'fixed', inset:0`) **يجب** أن تستدعي `useBodyScrollLock()` (لمكوّن يُركَّب فقط عند الفتح) أو `useBodyScrollLock(isOpenState)` (لمكوّن دائم التركيب يعرض النافذة شرطياً) — لا تُكرَّر منطق القفل يدوياً.
- **[ADR-20] إعادة تصميم تجربة البحث في المنيو (يوليو 2026):** بعد مراجعة UX شاملة (تصور مرئي تفاعلي مُعتمد من المالك)، استبدال حقل البحث القديم (داخل بطاقة الهوية، يختفي بالتمرير، مطابقة حرفية بسيطة `.includes()`) بشاشة بحث مستقلة `SearchOverlay.jsx` (`position:fixed, inset:0`، تستخدم `useBodyScrollLock` وفق ADR-19). **نقاط الدخول:** زر 🔍 في الهيرو + زر 🔍 جديد في الهيدر المصغّر الدائم (لم يكن موجوداً هناك من قبل)، فيصبح البحث متاحاً بغضّ النظر عن موضع التمرير. **محرك المطابقة** (`searchUtils.js`, دوال خالصة منفصلة عن الواجهة): تطبيع نص عربي (توحيد الهمزات أ/إ/آ→ا، التاء المربوطة ة→ه، الألف المقصورة ى→ي، إزالة التشكيل) ثم تسجيل نقاط بالأولوية: مطابقة تامة (100) > بداية الاسم (85) > احتواء جزئي (65) > الوصف (50) > تسامح إملائي بمسافة تحرير Levenshtein قصيرة (45/30) — ثم ترتيب النتائج المتساوية بتفضيل `is_featured` فالأكثر مبيعاً. **قبل الكتابة:** اقتراحات (الأكثر طلباً من `is_featured`، عمليات بحث سابقة محفوظة بـ`localStorage` — مفتاح `sm_recent_searches`، والأقسام) بدل شاشة فارغة. **لا نتائج:** رسالة + أزرار أقسام بديلة. **أُزيل بالكامل:** `searchQuery`/`setSearchQuery` من `MenuHeader`/`MenuBody`/`PublicMenu` (كانت تُخفي شريط الأقسام وتعرض نتائج مسطّحة داخل تدفّق الصفحة نفسها؛ البحث الآن مُغلَق تماماً عن تصفّح الأقسام العادي). **مؤجَّل بقرار المالك (نطاق منفصل):** شارة التصنيف داخل بطاقة نتيجة البحث (تتطلب تعديل `ProductItem.jsx` في تخطيطاته الأربعة)، واقتراحات "جديد"/"عروض" قبل الكتابة (لا يوجد عمود "جديد" في قاعدة البيانات حالياً)، والبحث بالمكوّنات/الحساسية الغذائية/النكهات/الصوت/الذكاء الاصطناعي (مستقبلية — محرك المطابقة مصمَّم كطبقة منفصلة تسهّل إضافتها لاحقاً بلا إعادة هيكلة الواجهة).
- **[ADR-21] إزالة ميزة "الفروع المتعددة" بالكامل (يوليو 2026):** بقرار صريح من المالك، بعد تحليل معماري أثبت أن المنيو (أقسام/أصناف) لم يكن أصلاً مفلتراً بالفرع — الفروع أثّرت فقط على أوقات العمل والعنوان وربط الطلب. أزيل نهائياً: صفحة إدارة الفروع، منطق اختيار/عرض الفرع في منيو الزبون، استهداف البانرات/الكوبونات بفرع، فلترة الفرع في لوحة التحكم، ونطاق صلاحية الموظف بالفرع. **⚠️ عُكس بالكامل بـ ADR-22 بعد يومين من التنفيذ** — راجع ADR-22 للنموذج الحالي الفعلي؛ هذا القرار موثَّق للتاريخ فقط.
- **[ADR-22] إعادة بناء الفروع كـ "كيان تشغيلي مستقل" (يوليو 2026):** بعد تحليل معماري عميق (مقارنة 3 خيارات: تعميق سطحي/توأم مستقل بالكامل/هجين)، تبنّي **نموذج هجين**: `restaurants` = العلامة التجارية فقط (الهوية/الشعار/الألوان/برنامج ولاء واحد مشترك)، و`branches` = وحدة تشغيلية حقيقية ومستقلة تماماً لكل فرع (بما فيه الفرع الأساسي — لا حالة استثناء "بلا فرع" بعد الآن). **قرار المالك الحاسم الذي شكّل التصميم:** عند إنشاء فرع جديد يُنسخ منيو الفرع الأساسي إليه بالكامل (`lib/branchesApi.js → cloneMenuToBranch`) ثم يصبح قابلاً للتعديل بحرية تامة بلا أي ربط لاحق بالأصل (لا مزامنة، لا جدول استثناءات) — الفرع الأساسي دائماً مصدر النسخ. **قاعدة البيانات (`sql/create_branches_v2.sql`):** جدول `branches` جديد (`is_primary` بدل الحالة الضمنية القديمة)؛ `branch_id` **إجباري (NOT NULL)** على `categories`/`products`/`orders` (كل صف ينتمي لفرع حقيقي دائماً)؛ اختياري على `banners`/`coupons`/`reviews` (null = عام لكل الفروع). ترحيل تلقائي: كل مطعم قائم يحصل على فرع أساسي يرث عنوانه/ساعاته/منيوه الحالي بلا فقدان بيانات. **الاستقلالية الفعلية لكل فرع:** رابط/QR (نمط `?branch=id` — نفس بنية الرابط، اختيار "أ" من المالك لتفادي تعقيد توجيه جديد)، منيو وأسعار (نسخة كاملة منفصلة)، ساعات عمل، حالة تشغيل، بيانات تواصل/عنوان/خريطة، عروض وكوبونات (اختياري)، تحليلات/طلبات (`orders_access` RLS تُعيد تفعيل فلترة `branch_scope` بلا الحالة الخاصة "main" القديمة). **الوراثة من العلامة:** الشعار/الألوان/برنامج الولاء (مشترك بقرار صريح من المالك — نقاط تُجمع وتُستبدل عبر كل الفروع). **صلاحيات الموظفين:** عادت `branch_scope` على `restaurant_members` (موظف تابع للعلامة، مقيَّد اختيارياً بفرع واحد — قرار صريح من المالك، أقرب للنموذج القديم لا نموذج حسابات مستقلة لكل فرع). **الدفع:** مؤجَّل — لا تكامل دفع إلكتروني حقيقي في سمسم حالياً؛ لم يُبنَ له شيء بعد (قرار المالك: تكامل حقيقي مستقبلاً، خارج نطاق هذه المرحلة). **[تحديث لاحق]** أُنجز تكامل دفع إلكتروني حقيقي (Moyasar، Payment-First Checkout) بالكامل في أغسطس 2026 — انظر ADR-52. **لمسة إضافية اكتُشفت أثناء التنفيذ (لم تكن في الخطة الأصلية):** صفحة إدارة المنيو (`Menu.jsx`) تحتاج منتقي فرع (كل قسم/صنف مملوك لفرع محدد الآن) — أُضيف. **إصلاح أمان اكتُشف أثناء المراجعة:** `useCoupon.js` كان يتحقق من الكوبون بـ`restaurant_id` فقط بلا فحص `branch_id` — كان يسمح لزبون في فرع A باستخدام كوبون حصري لفرع B؛ أُصلح.
- **[ADR-23] إعدادات تشغيلية مستقلة لكل فرع: توصيل/استلام/إغلاق مؤقت + اقتراحات السلة ووقت التجهيز لكل فرع (يوليو 2026):** بعد ADR-22، ظلّت 3 فجوات حقيقية غير منجزة اكتُشفت لاحقاً: **(1) توصيل** — عمودان جديدان على `branches` (`delivery_enabled`, `delivery_fee`) **قابلان للـnull عمداً**: `null` = وراثة إعداد المطعم العام (`effectiveDeliverySettings` في `helpers.js`)، أي قيمة أخرى = تخصيص مستقل للفرع — نمط "وراثة/تخصيص" مختلف عمداً عن نمط "نسخ كامل بلا مزامنة" المستخدم للمنيو في ADR-22. **(2) استلام** — عمود جديد كلياً `takeaway_enabled` (افتراضي `true`، **غير قابل للـnull**، لا مقابل له على مستوى المطعم لأنه لم يكن موجوداً كخيار من الأساس). **(3) إغلاق مؤقت فوري** — عمود `is_paused` (منفصل تماماً عن `is_active` الدائم وعن `opening_hours` الأسبوعي)، `computeBranchOpenStatus` في `helpers.js` يجعله يتفوّق فوراً على الجدول الأسبوعي. **فجوتان إضافيتان من نفس الفئة (اكتُشفتا لاحقاً):** `cart_wide_recommendations` (قائمة "أكمل وجبتك" اليدوية) لم يكن فيها `branch_id` أصلاً فكانت تُدار وتُعرض مختلطة بين كل فروع المطعم في لوحة التحكم رغم أن المنتجات نفسها مستقلة لكل فرع منذ ADR-22 — أُضيف `branch_id` إجباري + قيد فريد لكل فرع بدل المطعم كله. و`get_active_orders_count` (المستخدمة لحساب "وقت التجهيز التقديري" في منيو الزبون) كانت تحسب طلبات المطعم كله بدل الفرع الحالي فقط — أُضيف لها معامل اختياري `p_branch_id` (افتراضي `null` يحافظ على أي استدعاء قديم).
- **[ADR-24] إعادة تصميم صفحة الطلبات كـ"مركز قيادة تشغيلي" (يوليو 2026):** بعد تحليل معماري عميق مطلوب صراحة من المالك (11 محوراً: الأهداف/رحلة كل مستخدم/الطبقات/الأولويات/الذكاء التشغيلي/الكانبان/الجدول/الفلاتر/تعدد الفروع/الاستعداد المستقبلي/تقييم شامل) قبل أي كود، ثم تنفيذ تدريجي بموافقة على كل طبقة. **قرارات حاسمة من المالك شكّلت التنفيذ:** لا أعمدة زمنية جديدة لكل حالة (`accepted_at`/`ready_at`) ولا إسناد طلب↔موظف الآن (مؤجَّل)؛ لا دمج فعلي بين Orders وAnalytics (الفكرة المقترحة — hook حسابات مشترك — لم تُنفَّذ، تبقى الصفحتان منفصلتين كودياً). **المُنجَز:** شريط أولوية عابر للحالة (🔥 متأخر/⚠️ معرّض للتأخير/🎁 كوبون/⭐ VIP، شارات قابلة للنقر كفلتر فوري) + شريط فلاتر موحّد (بحث نصي برقم الطلب/العميل/الجوال + فرع + نوع الطلب + فلتر جاهز "يحتاج تدخل") حلّ محلّ شريط إحصائيات مكرِّر (كان يكرر أرقام أعمدة الكانبان نفسها) وشريط فلتر فرع منفصل؛ تبويبات الفلتر (نشط/الكل/مكتمل/ملغي) أصبحت دائمة الظهور في الكانبان والجدول معاً وتتحكم الآن أيضاً بأعمدة الكانبان الظاهرة (عمود "ملغي" كان مخفياً بالكامل من الكانبان، الآن يظهر فقط عند اختيار تبويب "الكل"؛ عمود "مكتمل" لم يعد ظاهراً افتراضياً في تبويب "النشطة" — تغيير سلوك متعمّد). **⭐ VIP:** مؤشر تقريبي خفيف (عميل تكرر رقم جواله 3 مرات فأكثر ضمن آخر 100 طلب محمَّلة فقط، بلا استعلام إضافي لقاعدة البيانات ولا عمود جديد) — قرار المالك الصريح (بدل حساب دقيق لكامل تاريخ العميل). **جُرِّب ثم أُلغي بالكامل بقرار المالك:** نظام "وضع عرض" (كاشير/مطبخ/توصيل) — كان فلتر عرض محلي بحت (يُظهر/يخفي أعمدة وتفاصيل) **بلا أي فرض صلاحيات حقيقي** (أي موظف يقدر يقبل/يلغي أي طلب بغض النظر عن الوضع)، فاعتُبر مضلِّلاً وأُزيل بالكامل. **خطة ب مؤجَّلة (توثيق فقط، لم تُنفَّذ):** نظام أدوار حقيقي (عمود دور في `restaurant_members` + مصفوفة صلاحيات لكل انتقال حالة + تطبيق Soft بالواجهة أو Hard عبر RLS) لتقييد فعلي لمن يقبل/يجهّز/يسلّم الطلب — قرار مستقبلي منفصل. **طباعة تذكرة المطبخ:** صُمِّمت (طباعة تلقائية عند القبول عبر نافذة طباعة المتصفح، بلا سعر/دفع) لكن **مؤجَّلة بقرار المالك** لحين تحديد عرض ورق الطابعة. **تنبيه صوتي جديد:** نغمة مختلفة (صفّارتان بنفس النغمة) تُنبّه الكاشير عند تحوّل الطلب لـ"جاهز" (لم يكن موجوداً — الصوت كان يعمل فقط عند وصول طلب جديد)؛ كلا النغمتين طُوِّلتا لاحقاً (تكرار 3 مرات، ~1.5 ثانية) بعد اختبار حقيقي على جهاز المالك كشف أن السبب الفعلي لعدم سماعها كان سلوك "إلغاء كتم" الصوت بالمتصفح (يحتاج تفاعلاً مباشراً)، وليس خللاً بالكود — أُصلح بإعادة استخدام آلية `playTone` المُثبَتة فعلاً بدل نغمة مفردة جديدة.
- **[ADR-25] حادثة حرجة: الزبون لا يقدر ينشئ طلباً (RLS على RETURNING) + Realtime Broadcast للتحديث اللحظي (يوليو 2026):** **السبب الجذري** (تأكَّد بمعاملات SQL تجريبية مباشرة على قاعدة البيانات الحية بـrollback): `useCheckout.js` كان يستخدم `.insert().select()` — و`.select()` يتطلّب قراءة الصف بعد إدخاله (RETURNING)، وهذه القراءة تخضع لسياسة SELECT المغلقة عمداً أمام الزبون العابر (ADR-9)، فيفشل الإدخال **كاملاً** برسالة "violates row-level security policy" رغم أن الإدخال نفسه مسموح فعلياً. **الحل:** دالة آمنة جديدة `create_order` (`SECURITY DEFINER`، بنفس نمط `get_active_orders_count`/`get_orders_status`) تُدخل الطلب داخلياً بصلاحياتها وترجع فقط `id`/`order_number` — بلا أي كشف بيانات، بلا إلغاء أي سياسة RLS موجودة. **اكتشاف بالامتداد:** نفس السبب الجذري كان يعطّل بصمت (بلا خطأ ظاهر) تتبّع حالة الطلب عند الزبون (`useActiveOrders.js`) وتحديث "وقت التجهيز التقديري" الحي في المنيو العام (`useMenuData.js`) — كلاهما كان يشترك بـ`postgres_changes` مباشرة على `orders`، وهذا لا يعمل إطلاقاً للزبون لنفس سبب RLS. **الحل لكليهما:** Realtime Broadcast بدل الاشتراك المباشر — Trigger على `orders` يبثّ لقناة خاصة (`order-status:<uuid>` للطلب الواحد، ثقة قائمة على عشوائية الـUUID نفسه؛ `restaurant-orders:<restaurant_id>` لعدّاد المطعم كله، بلا أي قيد خصوصية لأن `restaurant_id` علني أصلاً بالرابط)، مع سياسة على `realtime.messages` (جدول تفويض Broadcast الخاص بـSupabase — **RLS مفعّل عليه مسبقاً من Supabase نفسه**؛ محاولة `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` عليه تفشل بـ"must be owner of table messages"، الحل: تنفيذ `CREATE POLICY` وحدها بلا `ALTER TABLE`). التحديث الاحتياطي الدوري المسبق (شبكة أمان، لم يُحذف) قُصِّر من 20 إلى 5 ثوانٍ كسقف أقصى مضمون للتأخير، بعد ملاحظة تأخير متفاوت (~ثوانٍ إلى ~10 ثوانٍ) في البث اللحظي على الجوال حتى والشاشة مفتوحة طول الوقت — الأرجح سلوك طبيعي لإيقاظ اتصال الشبكة الخلوية بعد خمول، وليس خللاً بالكود؛ يُعتبر قريباً من الحد العملي لتطبيق ويب بلا إشعارات Push حقيقية.

- **[ADR-26] نقل التجميع إلى قاعدة البيانات — طبقة بيانات قابلة للتوسع (M1) (يوليو 2026):** بعد مراجعة معمارية شاملة (خطة C متوازنة)، نُقل تجميع الصفحات الثقيلة من المتصفح إلى Postgres عبر دوال `SECURITY INVOKER` (تخضع لـ RLS فتحترم نطاق فرع الموظف تلقائياً — **تغلق Suggestion ADR-18**). كل دالة تُرجع مجاميع مضغوطة فقط (لا صفوفاً خام)، والتوقيت الشهري/اليومي **بتوقيت الرياض**، وتحقّق تطابق بيانات **100%** مع الحساب السابق على البيانات الحيّة قبل كل ربط. **(1) العملاء:** `get_customers_summary` (صف لكل عميل: إنفاق/عدد/مستوى/فروع) بدل تنزيل كل الطلبات الخام (كان `select('*')` بلا حدّ — الأخطر)؛ تفاصيل العميل (سجل الطلبات + المنتجات المفضّلة) تُحمَّل **كسولاً** عند فتح لوحته فقط؛ و`get_customers_insights` للرؤى العامة (أكثر منتج + اتجاه المتوسط الشهري). **(2) التحليلات:** `get_analytics_summary` (استدعاء واحد يرجع كل المجاميع: KPIs/يومي/أصناف/حالات/أنواع/فروع/أسباب إلغاء/ساعة ذروة) بدل نافذتين `select('*')`؛ الضريبة تبقى تُفكّ في `lib/pricing.js` عبر `completed_gross_sum` (خطّية الحساب تحفظ ADR-1)؛ فلتر الفرع أصبح **معامل خادمي** (إعادة جلب بدل فلترة كل الطلبات محلياً). **(3) لوحة التحكم:** فُصلت إلى **تشغيلي** (طلبات اليوم+الأمس + الاشتراك اللحظي: جدول اليوم/البطاقات المباشرة/العدّادات) و**تحليلي** (`get_dashboard_summary`: رسم مبيعات غير الملغاة يومياً + أكثر أصناف + عدد عملاء الفترة) بدل سقف `.limit(2000)` غير الدقيق على النوافذ الطويلة — الرسم/الأصناف/عملاء الفترة يتحدّثون عند التحميل/تغيير الفترة (لا لحظياً)، وإحصاءات اليوم تبقى لحظية. **مؤجَّل:** Pagination خادمي للعملاء (A3 — غير عاجل بحجم البيانات الحالي؛ التجميع الخادمي أزال الخطر الأساسي).

- **[ADR-27] أساس Super Admin (Platform OS) — مرحلة M1 (يوليو 2026):** بعد مراجعة نقدية للمعمارية (تقليص Over-Engineering)، بناء أساس لوحة تحكّم المنصّة كـ**وحدة معزولة** `src/admin/` (تحميل كسول — لا بايتة تُحمَّل لأي صاحب مطعم/زبون؛ قابلة للفصل مستقبلاً إلى `admin.simsim.com` بلا إعادة بناء). **قرارات معتمدة:** **(الأمان)** لا مساس بـRLS المستأجرين إطلاقاً — المشرف يصل للبيانات **حصراً** عبر دوال `SECURITY DEFINER` مبوّبة بـ`is_platform_admin()` (تبدأ بـ`raise` عند غير المشرف، `search_path` مضبوط). **(الأدوار)** طبقة أدوار منصّة منفصلة عن أدوار المطعم — `platform_admins` بدورين الآن (`super_admin`/`read_only`) + `capabilities` JSONB للتوسّع، بلا Over-Engineering لـ11 دوراً. **(Feature Flags)** مستويان (`feature_flags` عام + `restaurant_feature_overrides` لكل مطعم) خلف واجهة `has_feature()` وحيدة قابلة للتوسّع. **(Audit)** `platform_audit_logs` + `log_platform_action()` (كتابة ذرّية داخل معاملة الإجراء، إدارية فقط) — بلا IP/Correlation سابقة لأوانها. **المُنجَز:** M1.1 أساس قاعدة البيانات · M1.2 كشف المشرف في `authStore` + حارس `RequirePlatformAdmin` + مسار `/admin` · M1.3 قشرة `AdminShell` مستقلة (هوية داكنة/بنفسجية «وضع المنصّة») + `adminNav` · M1.4 أول Admin Service (`admin_list_restaurants`) + وحدة «المطاعم» للقراءة (Walking Skeleton كامل: حارس→قشرة→خدمة→RPC→DB، قالب لكل وحدة لاحقة). **التالي:** M2 لوحة مؤشّرات المنصّة، M3 إدارة المطاعم، M4 الفوترة (الأعلى قيمة تجارية).

- **[ADR-28] تحصين أساس Super Admin: حارس بوابة آلي + ترقيم/بحث خادمي لقائمة المطاعم (يوليو 2026):** بعد مراجعة معمارية نقدية لقرارات أساس المنصّة (ADR-27)، تحسينان: **(1) حارس بوابة المشرف الآلي** (`src/lib/adminGate.test.js`): اختبار Vitest **ثابت (Offline، بلا شبكة)** يقرأ ملفات `sql/` ويتأكد أن جسم كل دالة اسمها `admin_*` يستدعي `is_platform_admin()` أو `platform_admin_can(...)` — أي دالة إدارية جديدة بلا بوابة تُسقط CI قبل الدمج مع ذكر اسمها وملفها. يغطّي الدوال السبع الحالية ويلتقط الجديد تلقائياً (بلا قائمة يدوية)، مع فحص «فقدان صامت» يضمن سلامة المُحلِّل؛ أُثبت أنه يحمرّ فعلاً عند غياب البوابة. **إلزامي في CI عبر `npm test` القائم** (بلا تعديل إعدادات CI). **(2) ترقيم/بحث خادمي لـ `admin_list_restaurants`:** كانت تُرجع كل المطاعم وتُجمّع جدولي `orders`/`branches` **كاملين** في كل استدعاء (يتدهور مع نمو الطلبات لا المطاعم). البديل: معاملات `p_search`/`p_limit` (مقيّد 1..100)/`p_offset` + `total_count` عبر `count(*) over()`، وعدّ الطلبات/الفروع **لصفوف الصفحة فقط** (بعد الترقيم داخل CTE، يستفيد من فهرس `orders(restaurant_id,...)`) بدل مسح الجدولين. أُعيد عمود `platform_suspended` للمُخرجات (**إصلاح انحراف** بين ملف SQL والنسخة المنشورة). الواجهة (`RestaurantsList.jsx`): بحث خادمي بـ`debounce` 300ms + «تحميل المزيد» + رمز طلب (`reqRef`) يتجاهل الردود القديمة — بطاقة العرض بلا تغيير. `restaurantsApi.js`: `listRestaurants({search,limit,offset}) → {rows,total}`. **نُفِّذ في Supabase ✅** (بوابة تُختبر بالرفض، ترقيم يُرجع `total_count` صحيحاً). البيانات الحالية 4 مطاعم فقط (الترقيم استباقي؛ القيمة الأساسية إصلاح التجميع المكلف). PR #163 مُدمج. **(3) `admin_get_restaurant`** (فحص لاحق): النمط المكلف الخطير **غائب** أصلاً (كل استعلاماتها مُقيَّدة بمطعم واحد عبر فهرس `restaurant_id`، لا تجميع عابر لكل المطاعم)؛ ومع ذلك وُحِّدت مسحاتها الـ~6 المتكرّرة في CTE `ro` (materialized) واحد لتخفيف الحمل لمطعم ضخم — بتحقّق تطابق مخرجات 100% على المطاعم الحيّة قبل النشر. نُفِّذ في Supabase ✅. **(4) تقوية الحارس:** أُضيف فحص ثانٍ يتأكد أن البوابة هي **أول بيان بعد `begin`** (لا استعلام قبلها) — يُطبَّق على دوال plpgsql ذات `begin`، وأُثبت أنه يلتقط بوابةً موضوعةً بعد استعلام. الحارس الآن 15 اختباراً (وجود + أول بيان لكل دالة).

- **[ADR-29] أساس الفوترة M4.1 (خلفية فقط) — Super Admin/Billing (يوليو 2026):** بعد تخطيط معماري بموافقة المالك على كل قرار: **النموذج** اشتراك SaaS لكل مطعم (علامة، لا لكل فرع)، **تحصيل يدوي** مصمَّم للتوسّع للدفع الإلكتروني (`payments.method` يشمل `gateway` مسبقاً — البوابة لاحقاً = دالة webhook تُدرج دفعاً بلا تغيير جدول). **الباقات (Plans)** يُنشئها الأدمن يدوياً ويسعّرها: كل باقة = اسم + دورة (شهرية/سنوية) + سعر واحد، **الاسم والسعر قابلان للتعديل** دائماً. **الجداول (4):** `plans` · `subscriptions` (اشتراك واحد لكل مطعم، `plan_id`+`amount` قابل للتعديل فوق سعر الباقة+`plan_label` لقطة) · `invoices` (الأدمن يُدخل الإجمالي الشامل، الصافي/الضريبة يُشتقّان `round(total/1.15,2)` وفق ADR-1، رقم تسلسلي، حالات draft/open/paid/void) · `payments`. كلها **RLS: قراءة مشرف فقط، صفر وصول للمستأجرين**، والكتابة حصراً عبر **9 دوال `admin_*`** (بوابة أول بيان + `search_path` + Audit ذرّي؛ القراءة `is_platform_admin`، الكتابة `platform_admin_can('manage_billing')` — يمرّها super_admin). **تحقّق تدفّق حقيقي كامل** بمعاملة rollback (باقة→اشتراك ورث السعر→فاتورة 115 صافي100/ضريبة15→دفع→paid، صفر تسرّب) + مستشار أمان Supabase بلا تحذير جديد. **الحارس الآلي يغطّي الدوال التسع تلقائياً.** **مؤجَّل:** M4.2 واجهة المطعم لفواتيره (سياسات RLS للمالك)، M4.3 بوابة دفع، توافق ZATCA الرسمي، التعليق التلقائي عند التأخّر. **الحالة:** الخلفية منشورة؛ **واجهة الأدمن «💳 الفوترة» (الخطوة 3) مبنية ومدموجة** — وحدة كسولة معزولة `src/admin/features/billing/` (`Billing.jsx` + `billingApi.js`) بثلاثة تبويبات (الباقات: إنشاء/تعديل/تفعيل · الاشتراكات: ربط مطعم بباقة + سعر قابل للتعديل · الفواتير: إنشاء بمعاينة الضريبة حيّة + تعليم مدفوعة + إلغاء + فلتر بمطعم)، الكتابة لـ`super_admin` فقط، `adminNav` صار `ready:true`، مسار `/admin/billing`. **[M4.2 مُنجز] واجهة المطعم لفواتيره:** سياستا RLS **قراءة فقط** (`subs_owner_select`/`inv_owner_select` مقيّدتان بـ`is_restaurant_owner`؛ `payments`/`plans` تبقيان للمشرف — عزل مُتحقَّق بمعاملة rollback: مالك يرى نفسه/غريب لا شيء/مشرف الكل)، وصفحة `src/pages/Billing.jsx` (قراءة فقط: بطاقة الاشتراك + قائمة الفواتير، تقرأ عبر RLS مباشرةً) كصفحة **للمالك فقط** (`OWNER_ONLY` + `nav.js` مجموعة «تحليل وضبط» + مسار `/billing`). **مؤجَّل:** M4.3 بوابة دفع، توافق ZATCA الرسمي، التعليق التلقائي عند التأخّر.

- **[ADR-30] RBAC مرن لمشرفي المنصّة + دخول منصّة مستقل (يوليو 2026):** فصل هوية المنصّة عن المطاعم بالكامل + نظام أدوار تديره أنت. **(الدخول)** باب مستقل `/admin/login` (هوية داكنة) لا يمرّ بأي شاشة مطعم/Onboarding — يتحقق `is_platform_admin()` ثم يوجّه، ومن ليس مشرفاً يُسجَّل خروجه (لا يدخل تطبيق المطعم)؛ `RequirePlatformAdmin` يوجّه غير المسجّل لـ`/admin/login`؛ زر «🛡️ لوحة المنصّة» في `AppShell` للمشرف فقط. يعمل لحساب منصّة **بلا مطعم**. **(RBAC — الأدوار بيانات لا كود)** جدول `platform_roles` (اسم/وصف/`capabilities[]`/`is_system`) يديره المشرف (CRUD)؛ `super_admin` **دور نظام محمي** (صلاحية `*`)؛ `platform_admins.role_id` (رُحِّل المشرف الحالي، وحُذف عمودا `role`/`capabilities` القديمان). الصلاحيات: `manage_restaurants`·`manage_billing`·`manage_admins`·`manage_roles` (والقراءة لكل مشرف). `platform_admin_can` يحلّها من الدور (`*`=شامل). **(إنشاء من الصفر)** Edge Function `create-platform-admin` (نمط `delete-staff`): تتحقق أن الطالب `manage_admins` ثم تُنشئ حساب Supabase بلا مطعم وتُسنده لدور، مع تراجع لو فشل الإسناد. **الحمايات (مُختبَرة rollback):** دور النظام غير قابل للتعديل/الحذف · لا حذف دور مُسنَد · **يُمنع أي إجراء يُبقي النظام بلا super_admin فعّال**. **الواجهة:** وحدة كسولة `src/admin/features/admins/` بتبويبين (المشرفون: إضافة/تغيير دور/تعطيل/إلغاء · الأدوار: إنشاء/تعديل/حذف + تأشير الصلاحيات)، الإجراءات مبوّبة بفحص `platform_admin_can` في الواجهة **والخادم يفرضها**. الملفات: `sql/platform_roles_rbac.sql` · `sql/billing_owner_read.sql`(سابق) · `supabase/functions/create-platform-admin/` · `src/admin/AdminLogin.jsx`. **مؤجَّل:** تخصيص صلاحيات أدقّ (مثل فصل إنشاء/حذف الفاتورة) — النموذج يسمح بإضافة مفاتيح جديدة بلا إعادة بناء.

- **[ADR-31] طبقة توسّع Super Admin (F1–F5): Event Ledger + Rollup + لوحة قرار + Restaurant 360 + Audit (يوليو 2026):** بعد تصحيح المالك «حلّل على أساس منصّة تتوسّع لآلاف المطاعم لا 4 مطاعم تجريبية»، بُنيت البنية التي **تمنع إعادة البناء لاحقاً** (هي الـMVP الحقيقي، لا الشاشات الفارغة): **(F1.1 دفتر أحداث)** `subscription_events`+`invoice_events` **append-only** (Triggers + backfill idempotent) — يحفظ تاريخ MRR/Churn الذي «لا يمكن إعادة بناؤه لاحقاً» من الحالة الراهنة. **(F1.2 تجميع مسبق)** `platform_daily_metrics` + `restaurant_stats` (إحصاءات denormalized لكل مطعم + **Health Score**: نشاط40/فوترة30/تفاعل20/نمو10 → أخضر≥70/أصفر≥40/أحمر) تُحدَّث عبر `refresh_platform_metrics`+`admin_refresh_metrics` يدوياً و`pg_cron` كل 10 دقائق — القراءات الثقيلة تصير من جداول مجمّعة لا مسحاً حياً. **(F3 لوحة قرار)** `admin_dashboard()` (jsonb: KPIs+deltas + سلاسل + تنبيهات متأخر/قرب انتهاء/churn + الأكثر نمواً/الأقل نشاطاً)؛ أُعيد بناء `Overview.jsx` كلوحة **قرار** لا أرقام. **(F4 Restaurant 360)** وُسّعت `admin_list_restaurants` (join `restaurant_stats` + فرز/فلترة خادمية آمنة عبر CASE) و`admin_get_restaurant` (تُرجع stats/subscription/invoices/activity)؛ الواجهة: جدول كثيف قابل للفرز بشريط صحّة + ملف مطعم بتبويبات. **(F5 Audit Viewer)** `admin_list_audit_logs`+`admin_audit_actions` + `AuditLog.jsx` (بحث/فلتر/ترقيم + عارض فروقات). MRR: شهري=المبلغ، سنوي=المبلغ/12، حالات مساهِمة active+past_due؛ كل التجميع بتوقيت الرياض. كل الدوال DEFINER مبوّبة (الحارس يغطّيها تلقائياً).

- **[ADR-32] Phase 2 لأدوات المشرف: المزايا + النمو + «عرض كمطعم» + بحث ⌘K (يوليو 2026):** أربع وحدات كسولة معزولة فوق الأساس، كلٌّ بموافقة وخيارات منفصلة: **(المزايا)** `admin_feature_flags.sql` (5 دوال فوق `feature_flags`/`restaurant_feature_overrides` القائمة، صلاحية جديدة `manage_flags`) + صفحة `flags/` — تحكّم عام لكل ميزة + تخصيص لكل مطعم (override يتجاوز العام، `null`=إزالة/وراثة). **(النمو)** `admin_growth.sql` (summary + سلسلة MRR 90 يوماً + تفصيل شهري 6 أشهر **من دفتر الأحداث** ADR-31) + صفحة `growth/`. **(«عرض كمطعم» — الخيار A، قراءة فقط)** بلا PII وبلا لمس RLS (يحترم قرار الخصوصية «أ» + قرار «لا وصول مباشر لجداول المستأجرين»): `admin_restaurant_operational` (جاهزية المنيو من `products`/`categories` + جاهزية الإعداد) و`admin_restaurant_features` (لكل ميزة: حالتها الفعلية على المطعم + هل مُخصّصة) — تبويب «🩺 الحالة التشغيلية» بتحميل كسول في ملف المطعم. **(بحث ⌘K)** `src/admin/components/CommandPalette.jsx` في `AdminShell` — بحث مطاعم **خادمي** (إعادة استخدام `admin_list_restaurants`، debounce 250ms، حد أدنى حرفان) + قفز لأقسام اللوحة، تنقّل كامل بالكيبورد، **بلا خلفية جديدة**. `manage_flags`/`manage_announcements` أُضيفتا لقائمة الصلاحيات القابلة للإسناد للأدوار.

- **[ADR-33] الإعلانات (Announcements) — منصّة + جرس المطعم (يوليو 2026):** بثّ إعلانات من المنصّة لأصحاب المطاعم، على مرحلتين بموافقة منفصلة. **(م1 خلفية+إدارة منصّة)** جدول `announcements` (نوع info/warning/maintenance · استهداف all/plan + `target_plans[]` · نافذة زمنية `starts_at`/`ends_at` · `is_active`) + 3 دوال `admin_*` (بوابة+Audit، صلاحية `manage_announcements`) + صفحة إدارة `announcements/`. **(م2 جهة المطعم — أول لمسة لواجهة المطعم في هذه السلسلة)** `announcement_reads` (حالة «مقروء» لكل مطعم×إعلان، **RLS مغلق — وصول عبر الدوال فقط**) + دالتان **جهة-مطعم آمنتان** (`restaurant_announcements`/`mark_announcements_read`، DEFINER **تحلّان مطعم المتّصل خادمياً عبر `auth.uid()` — بلا `restaurant_id` كوسيط، فلا يرى أحد إعلانات مطعم آخر**، مع استهداف الخطة والنافذة الزمنية) + `NotificationsBell.jsx` في توب-بار `AppShell` (شارة غير مقروء + قائمة، يعلّم كمقروء عند الفتح). تُحقّق كله بمعاملات rollback (عزل المطاعم + الاستهداف بالخطة صحيحان).

- **[ADR-34] أساس نظام الدفع (Payment Foundation) — بنية محايدة للمزوّد، خاملة (يوليو 2026):** بقرار المالك «ضع الأساس بلا بناء أي تكامل، قابل للتوسّع لأي مزوّد دون إعادة تصميم». `sql/payments_gateway_foundation.sql`: 3 جداول — `payment_providers` (سجل manual/moyasar/tap/hyperpay/stripe، **كلهم معطّلون**؛ الاختيار قرار **بيانات** لا كود) · `payment_transactions` (دورة حياة **المحاولة**: initiated/pending/succeeded/failed/cancelled/refunded + `provider_ref` + `idempotency_key` + `raw`، **منفصلة عمداً** عن `payments`=السجل المُسوّى) · `payment_webhook_events` (فهرس فريد `(provider,event_id)` للإتقان). RLS إداري. **طبقة كود `src/payments/`:** `types` (Enums مصدر الحقيقة) · `contracts/PaymentAdapter` (عقد مجرّد: createCharge/verifyPayment/parseWebhook/refundPayment/mapStatus) · `adapters` (فارغ) · `services` (تنسيق خامل) · `utils` + `docs/PAYMENTS.md`. **ممنوعات محترمة:** لا مزوّد مربوط · لا Edge Functions · لا مفاتيح (تعيش في بيئة الخادم) · لا واجهة · لا تحصيل فعلي.

- **[ADR-35] طبقة التكامل (Integration Layer) — البوابة الموحّدة لكل خدمة خارجية، تأسيس معماري خامل (يوليو 2026):** بقرار المالك، `src/integration/` كبوابة **وحيدة** لأي API خارجي مستقبلي (دفع/واتساب/SMS/إيميل/خرائط/إشعارات/تخزين/AI/توصيل/ERP/CRM) وفق Clean Architecture · SOLID/ISP · Dependency Inversion · Adapter · Strategy · Interface First · **Open/Closed** (إضافة مزوّد = مُهايئ + تسجيله، **بلا تعديل كود قائم**). المجلدات: `contracts` (IntegrationAdapter + عقد لكل قدرة) · `adapters` (BaseAdapter) · `providers` (فارغ) · `registry` (Strategy، **بلا if/else**) · `factories` (نقطة الدخول الوحيدة) · `config` (اختيار المزوّد، **بلا أسرار**) · `errors` (سلسلة External→Integration→Application) · `events` (ناقل عام) · `webhooks` (عقد+موجِّه) · `logs` (NullLogger افتراضي) · `dto`/`types`/`utils`/`tests` (8 اختبارات معمارية)/`README`. **القاعدة الذهبية:** ممنوع على أي Module استدعاء API خارجي مباشرة. `src/payments` **لم يُعدَّل** — يُربط معمارياً كقدرة `payment` عبر `PaymentContract`. خامل تماماً: لا مزوّد/تكامل/منطق أعمال/واجهة/مفاتيح.

- **[ADR-36] إعادة تنظيم المشروع إلى طبقات احترافية — الخيار A (التدريجي الآمن) (يوليو 2026):** بعد مراجعة معمارية شاملة للطبقات (تقرير مُعتمد من المالك) كشفت **«لهجتين» معماريتين** (تطبيق المطعم: صفحات ↔ Supabase مباشرة · Super Admin/التكامل/الدفع: طبقات نظيفة)، اعتُمد **الخيار A**: تنظيم الطبقات **منطقياً** وسدّ الفجوات بالحد الأدنى فوق البنية القائمة (Feature-Sliced) — **لا** إعادة رصف فيزيائية (رُفض B لأنه Cargo-Cult عالي الخطر لـSPA). نُفِّذ على 5 مراحل، كلٌّ PR منفصل بموافقة: **(م0)** `LAYERS.md` — الدستور المعماري (اتجاه الاعتماد + خريطة الطبقات + قواعد التواصل + المؤجَّلة + دليل «أين أضع X؟»). **(م1)** طبقة `src/observability/` — تجريد Logging/ErrorReporting موحّد خامل (`Logger`/`ErrorReporter`/`NullLogger`/`observability.configure`)؛ `integration/logs` يعيد التصدير منها (بلا تكرار)، و`RootErrorBoundary` مربوط بنداء لا-أثر (سلوك مطابق). **(م2)** طبقة `src/config/` — مصدر واحد للإعدادات غير السرّية (`appConfig`)؛ وُحِّدت قراءات `import.meta.env` (كانت في `supabase.js` و`Staff.jsx`) فصارت في مكان واحد فقط، **بلا أسرار** في العميل. **(م3)** إرساء اصطلاح الوصول للبيانات (كل وصول عبر `*Api`) + ترحيل `Loyalty.jsx` كصفحة مرجعية → `lib/loyaltyApi.js` (سلوك مطابق 100%، الصفحة صارت عرضاً فقط بلا `supabase.from`). **(م4)** التوثيق (هذا القرار + تحديث `LAYERS.md`). **مبادئ محفوظة:** Clean Architecture (اتجاه الاعتماد) · SOLID/SRP لكل طبقة · Open/Closed (الطبقات الخاملة تقبل مزوّداً بلا لمس المتصلين). **صفر كسر ميزة، صفر تغيير سلوك، صفر لمس RLS/SQL.** **مؤجَّل بقرار موثّق (بلا بناء):** ترحيل بقية صفحات المطعم لطبقة الوصول (تدريجي، صفحةً صفحة) · ربط مزوّد Observability فعلي (Sentry) · طبقة AI · Domain Models صريحة — كلها جاهزة الأساس، تُبنى عند الحاجة بلا إعادة هيكلة.

- **[ADR-37] تطوير وحدة الولاء إلى نظام احترافي — Phase 1 (جارٍ) (يوليو 2026):** بعد تحليل شامل للوحدة (نقاط قوة/ضعف على 8 محاور) وموافقة المالك على خطة 4 مراحل، اعتُمد **عكس ADR-2**: الانتقال من «حساب النقاط حيّاً» إلى **دفتر نقاط دائم (append-only ledger)** — لأنه الأساس الوحيد الذي يفتح: سجل حركة النقاط (كشف حساب)، صلاحية النقاط، مستويات العضوية، الحملات، الحدود، والتدقيق، بلا إعادة بناء لاحقاً. **قرارات المالك المعتمدة:** هوية العميل = (`restaurant_id` + `customer_phone`) [الولاء مشترك بين الفروع، ADR-22] · احتساب النقاط على **صافي الأصناف** `(total − delivery_fee)/1.15` [ADR-1] · **بذر تاريخي** (كل عميل يبدأ برصيده الحالي) · كسب النقاط عبر **Trigger** على `orders` عند الاكتمال · طلب مكتمل ثم مُلغى → حركة `adjust` عكسية · إضافة أعمدة `reply/status` للتقييمات الآن. **نطاق Phase 1 (6 بنود):** دفتر (accounts+transactions) · سجل حركة · كتالوج مكافآت · ترحيل صفحة الولاء لتجميع خادمي (إصلاح خطر أداء: كانت تُنزّل كل الطلبات للمتصفح) · تحصين نزاهة التقييمات (سدّ `reviews_insert_public` المفتوحة بدالة `submit_review` مُتحقِّقة) · توثيق كائنات SQL الغائبة. **التنفيذ بخطوات صغيرة، كلٌّ بموافقة منفصلة:** **(خطوة 1 ✅)** توثيق `loyalty_tables.sql`/`reviews_table.sql`/`get_customer_loyalty.sql` (كانت في Supabase فقط). **(خطوة 2 ✅)** مخطط الدفتر: `loyalty_accounts` (رصيد denormalized + `lifetime_*` + `tier` جاهز لـPhase 2) + `loyalty_transactions` (append-only، `balance_after`، `source`/`staff_id`/`reward_id`) + Triggers صيانة رصيد ذرّية (قفل صفّ + منع رصيد سالب) + دوال كتابة داخلية `loyalty_post`/`loyalty_ensure_account` (نقطة الكتابة الوحيدة) + RLS قراءة فقط. **إصلاح أمني أثناء التنفيذ:** المستشار كشف أن `revoke from public` وحده لا يكفي — Supabase يمنح `anon`/`authenticated` تنفيذاً مباشراً، فأُلغيت الصلاحية عنهما صراحةً على الدوال الأربع (وإلا استطاع عميل مجهول منح نفسه نقاطاً). تحقّق وظيفي بـrollback (رصيد/مجاميع/حاجز السالب) ✅. **(خطوة 3 ✅)** البذر التاريخي (`loyalty_backfill()`، idempotent، DEFINER): حركة `earn` افتتاحية واحدة لكل عميل بأساس صافي الأصناف. النتيجة: 57 حساباً/979 نقطة/0 استبدال. **تحقّق تطابق قبل الاعتماد:** رصيد كل عميل = الحساب الصافي المستقل (0 اختلاف)، 0 رصيد سالب، الإجمالي مطابق. الفرق المقصود عن الأساس القديم (إجمالي 1158 → صافي 979 = −179، ~15.5% = التوصيل+الضريبة المستبعدان). **(خطوة 4 ✅)** Trigger `trg_loyalty_earn` على `orders` (`after insert or update of status`، DEFINER): يمنح `earn` عند الاكتمال ويعكسه (`adjust` سالب) عند الإلغاء بعد الاكتمال، بنفس أساس الصافي. **غير كاسر لتدفّق الطلب** (كل المنطق داخل `exception→warning` — درس ADR-25)، idempotent عبر صافي حركات الطلب (يدعم دورة اكتمال→إلغاء→اكتمال)، حاجز عكس آمن `least(المكتسب, الرصيد)` (لا رصيد سالب/لا فشل إلغاء). تحقّق بـrollback: كسب/لا-مضاعفة/عكس→صفر/إعادة-كسب + **اختبار عدم الكسر** (تعطيل `loyalty_post` مؤقتاً → الطلب يكتمل والخطأ يُبتلع) ✅. **(خطوة 5 ✅)** كتالوج المكافآت `loyalty_rewards` (6 أنواع + `points_cost` + `value` + `product_id` + `conditions` jsonb جاهز لـPhase 2 + `is_active`/`sort_order`) + FK `loyalty_transactions.reward_id→loyalty_rewards` (المؤجَّل من خطوة 2) + RLS `has_restaurant_access` + بذر المكافأة النصية الحالية كصفّ `gift` واحد (idempotent، توافق خلفي). إدارة الكتالوج = CRUD مباشر عبر RLS؛ الاستبدال الفعلي = دالة DEFINER (خطوة 6). **(خطوة 6 — ربط الواجهة، مقسّمة 6أ/6ب/6ج):** **(6أ ✅)** دالة `redeem_reward(restaurant,reward,phone)` DEFINER ببوابة `has_restaurant_access`: تتحقق من المكافأة (موجودة/تخص المطعم/مفعّلة) والرصيد، ثم تخصم عبر `loyalty_post` (`source='reward'`، `reward_id`، `staff_id=auth.uid()`)؛ لـ`authenticated` فقط. تحقّق بـrollback (بوابة ترفض غير المصادَق + مسار ناجح بمحاكاة جلسة المالك عبر `request.jwt.claims` + حاجز رصيد غير كافٍ) ✅. **قراران معتمدان:** محاذاة `Customers.jsx` للدفتر (1أ)؛ كرت الزبون يبقى على `reward_threshold` في Phase 1 (2نعم). **(6ب ✅)** كود التطبيق (أول لمسة للواجهة): **(SQL تمهيدي)** عمود `customer_name` على `loyalty_accounts` (الخيار A — الحساب مكتفٍ ذاتياً) يُزامَن تلقائياً من Trigger الكسب + بذرة أسماء لمرة واحدة (16/57 لهم اسم). **`loyaltyApi.js`:** `fetchLoyaltyData` تقرأ accounts/rewards بدل الطلبات (إصلاح خطر الأداء 🔴) + دوال جديدة (`fetchCustomerLedger`/`redeemReward`/`saveReward`/`toggleReward`/`deleteReward`؛ أُزيلت `createRedemption`). **`Loyalty.jsx`:** 3 تبويبات (النقاط+المكافآت+التقييمات)؛ لوحة صدارة من الدفتر + ورقة عميل منزلقة (كشف حساب + استبدال من الكتالوج) + إدارة كتالوج المكافآت؛ بطاقة الإعدادات والتقييمات بلا تغيير. **`Customers.jsx`:** رصيد الولاء من `loyalty_accounts` بدل الحساب الحيّ (يزيل التناقض — قرار 1أ). يلتزم LAYERS (لا `supabase.from` جديد في `Loyalty.jsx`، كله عبر `loyaltyApi`). **CI:** build + 105 اختبار خضراء. **(6ج ✅)** إعادة كتابة `get_customer_loyalty` لتقرأ الرصيد/المكتسب/المستبدَل من `loyalty_accounts` بدل الحساب الحيّ على الطلبات (نفس التوقيع، تطبيع الجوال، fallback أصفار). تحقّق: تطابق الدالة مع الدفتر (184=184)، عميل مجهول=0، تطبيع صيغ الجوال ✅. **بهذا يتطابق رصيد الزبون في المنيو مع لوحة المالك (صافي) — اكتملت خطوة 6.** **(خطوة 7 ✅)** تحصين نزاهة التقييمات: دالة `submit_review` (DEFINER) تتحقّق أن الطلب موجود/مكتمل/غير مُقيَّم وتشتقّ الهوية منه ثم تُدرج + قيد فريد `uq_reviews_order_id` (تقييم واحد لكل طلب) + أعمدة `reply`/`reply_at`/`status` خاملة (جاهزة لـPhase 2) + **إسقاط سياسة `reviews_insert_public` المفتوحة** (سدّ ثغرة التزوير/الإغراق). `useReviews.js` تستدعي الدالة بدل الإدراج المباشر (التوقيع باقٍ، لا لمس لـPublicMenu). تحقّق rollback (صحيح ينجح/مكرّر/غير مكتمل/تقييم خارج المدى) + build + 105 اختبار ✅. **🎉 اكتملت Phase 1 بالكامل (خطوات 1→7).** **مؤجَّل لـPhase 2:** المستويات (Bronze→VIP، عمود `tier` جاهز) · صلاحية النقاط/الحدود/قواعد الكسب المتقدمة (عمود `conditions` جاهز) · صفحة العميل الكاملة · تطوير التقييمات (رد/شكاوى/توزيع، أعمدة `reply`/`status` جاهزة) · لوحة تحكم الولاء · توحيد `Customers.jsx` بالكامل.

- **[ADR-38] تطوير الولاء — Phase 2 «التطوير المتوسط» (جارٍ) (يوليو 2026):** بعد اكتمال Phase 1 (الدفتر)، اعتُمدت خطة Phase 2 بأولوية: **أ) المستويات → ب) لوحة تحكم الولاء → ج) تطوير التقييمات → د) صفحة العميل الكاملة → هـ) قواعد الكسب/الحدود → و) صلاحية النقاط** (الأخيرة حسّاسة — تُفقِد نقاطاً). **(أ — المستويات، مقسّمة P2.1أ/ب/ج):** **قرارات معتمدة:** المقياس = `lifetime_earned` (تصاعدي فقط، لا ينزل — D1) · جدول يديره المالك + بذر الخمسة (D2) · مزية = **مضاعف كسب لكل مستوى** (D3) · **توحيد** شارة صفحة العملاء مع مستوى الولاء حيث الولاء مفعّل (D5=ب). **(P2.1أ ✅ الطرف الخلفي)** جدول `loyalty_tiers` (`name`/`min_points`/`earn_multiplier`/`color`/`icon`/`sort_order` + RLS إدارة المالك) + بذر 5 (🥉برونزي0·🥈فضّي200·🥇ذهبي500·💎بلاتيني1000·👑VIP2000، **مضاعفات ×1 خاملة**) + `loyalty_accounts.tier_id` (استبدل `tier` النصّي) + دالة `loyalty_compute_tier` (داخلية، مُشدَّدة عن العميل) + تحديث Trigger صيانة الرصيد (يحسب المستوى لحظياً مع `lifetime_earned`) + Trigger الكسب (`pts = صافي × earn_rate × مضاعف المستوى`) + بذرة مستويات الحسابات الـ57 (كلها برونزي، أعلى نقاط=184<200). **صفر تغيير سلوكي** (المضاعفات ×1). تحقّق rollback: ×1 لا يغيّر · ×3 يضاعف بدقة · العبور فوق 200 يرقّي لفضّي · 0 تنبيه أمني (بعد تشديد `loyalty_compute_tier`) ✅. **(P2.1ب ✅)** دالة `loyalty_recompute_tiers` (DEFINER، بوابة، تُعيد تصنيف الحسابات بعد تعديل/حذف مستوى) + `loyaltyApi` (جلب `tiers`+`tier_id` · `saveTier`/`deleteTier`/`recomputeTiers`) + تبويب رابع «🏅 المستويات» في `Loyalty.jsx` (إدارة السلّم: اسم/عتبة/مضاعف/لون/أيقونة + عدد العملاء) + شارة المستوى في لوحة الصدارة وورقة العميل + **توحيد `Customers.jsx` (D5=ب):** الشارة والفلتر يصيران مستوى الولاء حيث الولاء مفعّل (وإلا الشريحة التشغيلية بعدد الطلبات؛ الشارة الظرفية والفرز يبقيان تشغيليَّين). CI: build + 105 اختبار ✅. **(P2.1ج ✅)** `get_customer_loyalty` (DROP+CREATE) تُرجع أيضاً المستوى الحالي (اسم/أيقونة/لون/عتبة) والتالي (اسم/عتبة)؛ `LoyaltyCard.jsx` يعرض شارة المستوى + سطر «باقي X للترقّي إلى <التالي>». تحقّق: برونزي→فضّي عتبة 200 باقي 16 · العميل الأعلى next صحيح. **🎉 اكتملت ميزة (أ) المستويات بالكامل (P2.1أ/ب/ج).** **(ب — لوحة تحكم الولاء ✅)** دالة `get_loyalty_dashboard` (تجميع jsonb، INVOKER تحترم RLS): ممنوحة/مستبدَلة/منتهية/قائمة · إجمالي+نشطون(30ي) · معدل استبدال (مستخدَمة÷ممنوحة) + احتفاظ (% من كسبوا>مرة) · أفضل 5 عملاء · أكثر 5 مكافآت · توزيع المستويات. تبويب أول «📊 نظرة عامة» في `Loyalty.jsx` (بطاقات KPIs + توزيع المستويات كأشرطة + قائمتا أفضل عملاء/مكافآت) + `loyaltyApi.fetchLoyaltyDashboard`. تحقّق: ممنوحة 979=الدفتر، قائمة 979=الأرصدة، أفضل=علي/184، توزيع=57 برونزي. CI: build+105 اختبار ✅. **(ج — تطوير التقييمات ✅)** قرارات: الرد **داخلي** (D1=أ، لا لمس للمنيو) · حالات جديد/قيد المعالجة/محلول (D2) · الشكوى = ≤نجمتين (D3). دالة `get_reviews_summary` (jsonb، INVOKER): متوسط/عدد/توزيع النجوم/شكاوى/عدّ الحالات/اتجاه الشهر (توقيت الرياض). `loyaltyApi`: `fetchReviewsSummary`/`saveReviewReply`/`setReviewStatus` (الرد/الحالة تحديث مباشر عبر RLS `reviews_access`) + رفع حدّ جلب التقييمات إلى 200. إعادة تصميم تبويب التقييمات: بطاقات إحصائية + أشرطة توزيع النجوم + فلاتر (نجوم/حالة/فرع/بحث) + سطر الاتجاه + لكل تقييم: تمييز شكوى + شارة حالة + صندوق رد داخلي + أزرار حالة + واتساب. تحقّق: تجميع مطابق (متوسط 4.3/58، توزيع، شكاوى 5) + تحديث المالك عبر RLS (rollback) + build+105 اختبار ✅. **(د — صفحة العميل الكاملة 360 ✅)** قرار: **إثراء ورقة `Customers.jsx`** (D1=أ، لا صفحة جديدة/لا تكرار) + عبر التبويبات (D2=أ). **بلا SQL جديد** — إعادة استخدام `fetchCustomerLedger` + استعلام `reviews` بالجوال (RLS). التحميل الكسول للعميل صار يجلب أيضاً: كشف النقاط + تقييماته. الورقة صارت 4 تبويبات: نظرة عامة (+ شارة المستوى الموحّدة) · الطلبات · الولاء (+ **كشف حساب النقاط** كاملاً) · **التقييمات (جديد)** (نجوم/تعليق/تاريخ + الرد الداخلي). build + 105 اختبار ✅. **(هـ — قواعد الكسب المتقدمة + الحدود، مقسّمة هـ-1/هـ-2):** قرارات: الحد الأدنى على مجموع الأصناف · المضاعفات تتضاعف معاً · حدود المكافأة = حد/عميل + فترة. **(هـ-1 ✅ قواعد الكسب)** أعمدة على `loyalty_programs` (`min_order_amount`/`earning_branches[]`/`excluded_product_ids[]`/`campaign_multiplier`/`campaign_starts_at`/`campaign_ends_at`، كلها محايدة افتراضياً) + إعادة كتابة Trigger الكسب: فرع مشمول → حد أدنى (مجموع أصناف) → خصم المنتجات المستثناة (من `items`) → `floor(net × earn_rate × مضاعف المستوى × مضاعف الحملة)`. UI: بطاقة «قواعد الكسب المتقدمة» (حد أدنى/أزرار الفروع/checkboxes المنتجات/حملة بتواريخ) + `saveLoyaltyProgram` موسّعة + جلب `products`. تحقّق rollback شامل (محايد=الأساس · حد أدنى→0 · فرع→0 · استثناء الكل→0 · حملة×3 بالضرب-قبل-التقريب) + build+105 ✅. صفر تغيير سلوكي (افتراضات محايدة). **(هـ-2 ✅ حدود المكافآت)** `redeem_reward` تفحص `conditions` (`per_customer_limit`+`period`: مدى الحياة/يومي/أسبوعي/شهري كنوافذ متدحرجة) قبل الخصم → `reward_limit_reached` عند البلوغ. `loyaltyApi.saveReward` تحفظ `conditions` + نموذج المكافأة (حقلا حد/فترة) + رسالة خطأ. تحقّق rollback (أول ينجح/ثانٍ يُرفض) + build+105 ✅. **🎉 اكتملت (هـ).** **(و — صلاحية النقاط ✅، مقسّمة و-1/و-2)** قرارات: بالخمول (D1=أ) · سماح forward-only (D2=أ) · تنبيه العميل (D3=نعم). **(و-1 SQL)** `points_expiry_months` (0=لا تنتهي) + `expiry_enabled_at` + Trigger ختم وقت التفعيل + دالة `loyalty_expire_points` (تُصفّر رصيد الخامل>المدة بحركة `expire`، مع سماح: لا انتهاء قبل مدة كاملة من التفعيل) + `get_customer_loyalty` يُرجع `expiry_months` + جدولة `pg_cron` يومية. **الأمان:** الانتهاء يصفّر الرصيد فقط (لا يمسّ lifetime_earned فالمستوى لا ينزل). **(و-2 UI)** حقل «صلاحية النقاط» في بطاقة القواعد + `saveLoyaltyProgram` + تنبيه كرت المنيو «اطلب خلال X شهر للحفاظ على نقاطك». تحقّق rollback (سماح=0 · خامل→صفر بحركة expire · نشط سليم) + جدولة قائمة + build+105 ✅. صفر أثر (افتراضي 0). **🎉 اكتملت Phase 2 بالكامل (أ→و).**

- **[ADR-39] تطوير الولاء — Phase 3 «الخصائص المتقدمة» (جارٍ) (يوليو 2026):** بعد اكتمال Phase 2، اعتُمدت خطة Phase 3 بثلاثة بنود مرتّبة: **1) التحليلات الاحترافية → 2) التقارير/التصدير → 3) الحملات الموسمية** (الأخيرة ترقية لا تأسيس — حملة واحدة مبنية أصلاً في ADR-38/هـ-1). **قرارات معتمدة:** التحليلات تُوسّع صفحة `Analytics` القائمة لا صفحة جديدة (D1=أ) · «أسباب الشكاوى» بتصنيف **يدوي** الآن (D2=أ، جاهز ليملأه الـAI في Phase 4) · التصدير **هجين**: CSV وطباعة أصليان بلا مكتبات، وExcel بتحميل كسول عند الضغط فقط (D3=ج — حفاظاً على خفة الحزمة، نفس مبدأ رفض Framer Motion في ADR-15). **(P3.1 — التحليلات الاحترافية ✅)** **تفادي تكرار موثّق:** فحص `get_analytics_summary` كشف أن **أفضل المنتجات ومبيعات الفروع موجودان أصلاً** فلم يُبنَ لهما شيء. دالة **جديدة منفصلة** `get_advanced_analytics(restaurant,from,to,branch)` (jsonb، INVOKER تحترم نطاق فرع الموظف، **بلا لمس `get_analytics_summary`**) تُرجع: **تأثير الولاء على المبيعات** (طلبات/إيراد/متوسط طلب للأعضاء مقابل غيرهم + حصّة الإيراد + `aov_uplift_pct`؛ «عضو» = `lifetime_earned>0`) · **رضا العملاء** (متوسط/عدد/% راضين≥4/% ساخطين≤2) · **معدل العودة** في الفترة · **أداء الفروع** (إيراد+طلبات+تقييم+شكاوى في جدول واحد) · **أسباب الشكاوى** بالتصنيف. عمود `reviews.complaint_category` + قائمة تصنيف في بطاقة الشكوى بصفحة الولاء + `loyaltyApi.setReviewCategory`. الواجهة: قسم «🎯 تحليلات متقدمة» أُضيف لصفحة `Analytics` (بطاقة تأثير الولاء + رضا العملاء + أسباب الشكاوى + جدول أداء الفروع). **تحقّق مطابقة:** إيراد الأعضاء+غيرهم = إيراد الفترة الكلي (1173.95+288.35=1462.30) · عدد الفروع مطابق · الرضا يطابق `get_reviews_summary` (4.3/58). CI: build + 105 اختبار ✅. **ملاحظة تحليلية من البيانات الحيّة:** الأعضاء = 80.3% من الإيراد لكن متوسط طلبهم أقل بـ62% → البرنامج يرفع **التكرار** لا حجم السلّة (تُعرض للمالك كتوصية داخل البطاقة). **(P3.2 — التقارير والتصدير ✅)** قرار المالك بعد اكتشاف موثّق: **CSV مع BOM يفتحه Excel بالعربية صحيحة بلا أي مكتبة**، فاعتُمد **صفر تبعيات** (بدل إضافة xlsx كسولاً) — طبقة التصدير مجرّدة فتقبل مزوّد Excel لاحقاً بلا لمس المتصلين (Open/Closed). **طبقة مشتركة جديدة `src/lib/exportUtils.js`:** `toCSV` (تهريب فواصل/اقتباسات/أسطر) · `downloadCSV` (**BOM إلزامي** وإلا تُشوَّه العربية في Excel) · `exportRows` · `buildTable` (مع تهريب HTML) · `printReport` (iframe مخفي — أوثق من نافذة منبثقة قد يحجبها المتصفح؛ الطباعة تتيح «حفظ كـPDF»). **نقاط التصدير:** العملاء (CSV+طباعة، **يحترم الفلاتر الحالية**) · نقاط العملاء (CSV+طباعة) · التقييمات (CSV+طباعة، يحترم الفلاتر) · تقرير التحليلات (طباعة/PDF شامل: ملخّص الفترة + المبيعات اليومية + أفضل المنتجات + تأثير الولاء + الرضا + أداء الفروع + أسباب الشكاوى). **تفادي تكرار ثانٍ موثّق:** صفحة `Analytics` كان فيها **تصدير CSV يومي بالفعل** — أُبقي كما هو ولم يُكرَّر، وأُضيف زر التقرير فقط. **حارس آلي جديد:** `src/lib/exportUtils.test.js` (9 اختبارات: تهريب الفواصل/الاقتباسات/الأسطر · null لا يصير "null" · تهريب HTML ضد الحقن · مفاتيح دالّية) → **114 اختباراً في CI**. **(P3.3 — الحملات الموسمية ✅)** ترقية «الحملة الواحدة» (أعمدة على `loyalty_programs`) إلى كيان مستقل: جدول `loyalty_campaigns` (اسم/مضاعف/نافذة/مفعّلة + قيد `ends_at>starts_at` + RLS) · **قاعدة التداخل: الأعلى مضاعفاً يفوز** (لا تتضاعف الحملات — يمنع انفجار النقاط) · **قياس أثر حقيقي:** كل حركة كسب أثناء حملة تُختم بـ`campaign_id` في `metadata`، ودالة `get_campaigns_impact` تُرجع (نقاط/طلبات/عملاء) لكل حملة · **حُذفت الأعمدة الثلاثة القديمة** بعد الترحيل (تفادي مصدر حقيقة مزدوج — كانت محايدة فصفر فقدان). الواجهة: قسم «🎉 الحملات الموسمية» في تبويب النقاط (إنشاء/تعديل/حذف + حالة الحملة: جارية/مجدولة/منتهية/معطّلة + بطاقات الأثر). تحقّق rollback خماسي: بلا حملة=الأساس · ×3 مضاعفة+ختم صحيح · **التداخل: الأعلى فقط والأدنى لم يُطبَّق إطلاقاً** · التعطيل=عودة للأساس · تقرير الأثر دقيق (نقاط/طلبات/عملاء). CI: build + 114 اختبار ✅. **🎉 اكتملت Phase 3 بالكامل (P3.1 تحليلات · P3.2 تقارير · P3.3 حملات).** **المتبقّي:** Phase 4 (الذكاء الاصطناعي: تحليل التعليقات · تصنيف الشكاوى آلياً — العمود `complaint_category` جاهز · اقتراح الردود · اكتشاف العملاء المعرّضين للمغادرة · حملات استرجاع) — يُبنى عبر `integration` (AiContract) وفق ADR-35.
- **[ADR-40] إعادة تصميم هيرو منيو الزبون — تقليص الارتفاع + زجاج مصنفر (يوليو 2026):** بعد موافقة المالك على خطة مفصّلة (تحليل قياسات لكل عنصر قبل التنفيذ)، أُعيد تصميم الهيرو في **`MenuHeader.jsx` فقط — بلا أي تغيير في قاعدة البيانات أو مكوّنات أخرى، وبلا مكتبة جديدة**. الهدف: إظهار المنتجات أسرع بأسلوب تطبيقات التوصيل (Uber Eats/Talabat) مع الحفاظ التام على الهوية/الألوان/الأنيميشن/ترتيب العناصر. **التغييرات (نُفّذت تدريجياً عبر PRs #194→#199، كلها مدموجة في `main`):** **(1) تقليص الارتفاع ~27%** (من ≈416px إلى ≈305px حتى شريط الأقسام): `HERO_HEIGHT` 170→128px؛ **التداخل يزداد عبر تقصير الغلاف لا عبر قيمة `margin`** (بقيت `-76px`)، فرأس البطاقة يرتفع على الشاشة من ~94px إلى ~52px. الأزرار العائمة (لغة/بحث/طلباتي) رُفعت `top 18→12` لئلا تُقصّ خلف البطاقة الأعلى. **(2) إعادة معايرة الـ Sticky/Morph (ADR-15) إلزامية:** عتبات `heroOpacity` و`compactT` مربوطة يدوياً بالارتفاعات؛ أُزيحت بمقدار **‑44px** (heroOpacity: `(scrollY-130)/130`→`(scrollY-86)/130`؛ compactT: `(scrollY-122)/42`→`(scrollY-78)/42`) لتطابق موضع الاسم الجديد فلا يظهر مرّتين ولا يتلاشى الغلاف مبكراً — **قاعدة: أي تعديل على ارتفاع الهيرو مستقبلاً يستلزم إعادة حساب هاتين العتبتين**. **(3) الوصف:** استُبدل القصّ بالحروف (`DESC_MAX_CHARS=105`، أُزيل) بـ**سطرين عبر `-webkit-line-clamp:2`** + زر «عرض المزيد/أقل» (حالة `descExpanded` + قياس تجاوز عبر `descRef.scrollHeight>clientHeight` في حالة الطيّ فقط). **(4) ضغط المسافات** داخل البطاقة (paddingTop/مقبض السحب/صفّ الهوية/الوصف/الإحصائيات/الولاء) بلا حذف أي معلومة، وشعار الهوية 56→52px. **(5) روابط التواصل + المسبّبات في صفّ واحد مضمون** (`flexWrap:'nowrap'` + شبكة أمان `overflowX:'auto'` للشاشات الصغيرة جداً)، والأيقونات **تُعرض 3 فقط + زر دائري «+N»** يوسّع/يطوي الباقي (حالة `socialExpanded`، `socialKeys.slice(0,3)`) — بلا حذف رابط. **(6) بطاقة الإحصائيات** (مفتوح الآن/التجهيز/التوصيل) أُحكمت مساحتها، وشريط الولاء نُحّف. **(7) خلفية الورقة الرئيسية = زجاج مصنفر** (قرار المالك بعد عرض 3 خيارات): `white`→`rgba(255,255,255,0.72)` + `backdrop-filter:blur(12px)` (مع بادئة `-webkit-`) — يُظهر الغلاف خلفها بأناقة. **الهيدر المصغّر الدائم يبقى أبيض صلباً عمداً** (يمرّ فوق المنتجات، فالصلابة تحفظ قراءة النص). كل التعديلات Mobile-first (`maxWidth:480px`) وبلا أثر على الأداء (لا مكتبة، لا تغيير في منطق `requestAnimationFrame`).

- **[ADR-40] Platform Capability Registry (PCR) — سجل القدرات المركزي (M0: تثبيت التصميم، بلا كود) (يوليو 2026):** المصدر الوحيد للحقيقة (Single Source of Truth) لكل ما يمكن التحكم فيه داخل سمسم (صفحات/تبويبات/بطاقات/أزرار/نوافذ/نماذج/جداول/تقارير/رسوم/إجراءات/APIs/Webhooks/Integrations/AI/Mobile/POS/Delivery/Payment). **القرار الجوهري:** يُبنى بـ**تطوير بنية `feature_flags` القائمة** (ADR-27/32: `feature_flags` + `restaurant_feature_overrides` + `has_feature`) **لا كنظام موازٍ** — تفادياً لمصدر حقيقة مزدوج (نفس مبدأ الحذف بعد الترحيل في ADR-39/P3.3). الاسم المنتَجي = PCR؛ الأسماء الفيزيائية/الكودية تبقى (`feature_flags`, `has_feature`, `restaurant_feature_overrides`) للتوافق الخلفي 100%.
  - **النموذج المُنمَّط (Typed Catalog):** كتالوج واحد. كل Capability لها **`type`** ∈ {`feature`(boolean) · `limit`(number) · `option`(value) · `mode`(enum من مجموعة مسموحة)} — النوع **جزء من التصميم لا إضافة لاحقة**. القيَم تُحسم عبر دالة موحّدة `feature_value(restaurant,key) → jsonb` مُنمَّطة (**override المطعم ← قيمة الباقة ← default العام** + فحص التبعيات والحالة). `has_feature(restaurant,key)→boolean` يبقى توقيعه (غلاف رفيع فوق `feature_value` لنوع `feature`). `effective_features(restaurant)→jsonb` تُحمَّل **مرة واحدة** عند الدخول في `authStore` فكل الفحوص محلية = صفر استعلام أثناء التفاعل (نمط ADR-13).
  - **`scope`** ∈ {`platform`(عام لكل المنصّة) · `restaurant`(خاص بكل مطعم) · `user`(مؤجَّل تعريفاً)} — يمنع ربط كل الميزات بالمطعم فقط؛ ضروري لـAI/التكاملات/الإدارة العامة. القدرات platform-scope تُحسم على مستوى المنصّة (تتجاهل `restaurant`).
  - **فصل الحالة:** `lifecycle_status` ∈ {`draft`·`active`·`deprecated`·`archived`} (حالة **التعريف** في الكتالوج) منفصلة عن `runtime_status` ∈ {`enabled`·`disabled`·`beta`·`preview`} (حالة **التشغيل** للعملاء) — الميزة قد تكون في الكتالوج (`active`) لكنها غير مفعّلة للعملاء (`disabled`). القابلية للاستخدام = `lifecycle=active` **و** `runtime ∈ {enabled,beta,preview}`.
  - **Versioning (صيغة خفيفة، تفادي Over-Engineering):** تغيير **غير متوافق** لعقد الميزة = **مفتاح جديد** يُشير لسلفه (نمط API versioning: `online_orders_v1`←`online_orders_v2`) بدل تعديل القديم، فالمطاعم القديمة لا تُكسر. حقول: `version`(int) · `compatibility_version` (يوضّح علاقة الإصدارات) · `supersedes`(مفتاح السلف) · `deprecated`(bool). **per-restaurant version pinning مؤجَّل**، والمخطط يقبله لاحقاً بلا إعادة هيكلة.
  - **الحقول الكاملة لكل Capability:** `key`(lowercase snake_case إلزامي) · `name` · `description` · `category_id` · `module` · `parent_key`(هرمي، أي عمق) · `kind`(page/tab/card/button/modal/form/table/report/chart/action/api/webhook/integration/ai/mobile/pos/delivery/payment) · `type` · `scope` · `default_value`(jsonb) · `allowed_values`(jsonb، لـmode) · `metric`/`unit`(لـlimit) · `lifecycle_status` · `runtime_status` · `version`/`compatibility_version`/`supersedes`/`deprecated` · `icon` · `upgrade_message` · `sort_order` · `tags[]` · `required_permissions` · Audit(`created_at`/`updated_at`/`created_by`/`updated_by`).
  - **الجداول (المخطط):** `feature_flags`(مُوسَّع بكل ما سبق) · `feature_categories` · `feature_dependencies`(`key`→`depends_on_key` + **`dependency_type`** ∈ {`required`·`optional`·`conflicts`}؛ مثال: Online Ordering **requires** Menu، Delivery Integration **requires** Online Ordering) · `plan_features`(`plan_id`,`key`,**`is_included`**(bool: ضمن الباقة أم لا) + `value`(jsonb: قيمة الميزة/الحد) — يوحّد ربط الميزات + قيم الحدود، **محايد لنوع الباقة**) · `restaurant_feature_overrides`(يُوسَّع من boolean إلى `value` jsonb مُنمَّط) · `feature_permissions`(`key`↔role). كل الجداول تحمل Audit metadata.
  - **الحدود (Limits):** أي مورد قابل للقياس = صفّ `limit` بـ`metric`/`unit`. الفرض الفعلي **في قاعدة البيانات** (triggers/checks) للموارد القابلة للعدّ (products/branches/users)؛ Storage/Upload/Time/API/AI Usage = تعريفات **جاهزة**، فرضها الفعلي لاحق (بعضها يحتاج metering).
  - **FeatureGate متعدد الأنواع:** يلفّ Page/Component/Button/Action عبر `kind` (بوابة عميل من `feature_value` المحمّلة محلياً)، ولـAPI Endpoints **بوابة خادمية** داخل RPC/Edge (نمط DEFINER). رسالة الترقية موحّدة عبر `UpgradeModal` (upgrade_message + الباقة المناسبة + زر `/billing`).
  - **الحوكمة (قواعد ملزمة):** **(أ) مصدر حقيقة هجين** — التعريفات البنيوية في **Manifest بالكود** (`src/registry/features.manifest.js`، مُراجَع في git) تُزرع للـDB؛ الحالة التشغيلية (runtime_status/overrides/ربط الباقات/قيم الحدود) في DB يديرها الأدمن حيّاً. **(ب) «سجّل أولاً»** — حارس CI (نمط ADR-28) يُسقط أي مفتاح مُستخدَم بلا تسجيل في الـManifest. **(ج) تسمية** — كل `key` lowercase snake_case (يفرضه الحارس بـregex). **(د) طبقة وصول إلزامية** — يُمنع استخدام `key` مباشرة في الواجهة؛ كل فحص عبر Feature API فقط (`features.has("online_orders")` لا `if(feature_flags.online_orders)`) — يفرضه الحارس.
  - **مؤجَّل (حجز تصميم فقط، لا يُبنى الآن):** **Feature Evaluation Log** (لماذا حصل المطعم على الميزة؟ من أي باقة/override/تجربة؟ مثال: `restaurant A · online_orders · source=plan · value=true`) · **per-restaurant version pinning** · **أدوار Manager/Cashier** (M7 — تفعيل RBAC المؤجَّل من ADR-24؛ صلاحيات M1 على النموذج الحالي owner/employee) · **أنواع الباقات** (Free/Trial/Lifetime/Enterprise + Add-ons) = إضافة `plan_type`/كيان `addons` مستقبلاً بلا إعادة هيكلة (`plan_features` محايد لنوع الباقة) · **Integrations/AI** = capabilities من نوعها عبر integration layer (ADR-35).
  - **الطبقات (LAYERS):** Domain(`src/registry/features.manifest.js` + `src/lib/features.js` دوال حسم نقيّة) · Data-Access(`src/lib/featuresApi.js` جهة المطعم + `src/admin/features/catalog/catalogApi.js`) · Presentation(`FeatureGate.jsx` + `UpgradeModal.jsx` + `admin/features/catalog/`) · State(`authStore` يحمّل `effective_features`) · Governance(`src/lib/registryGuard.test.js`).
  - **الأثر:** صفر لمس لجداول المستأجرين أو RLS الحالية · `has_feature` توافق خلفي 100% · كل صفوف `feature_flags` القائمة تُرحَّل بلا فقدان · `plans.features` النصي يبقى للعرض حتى ترحيل `plan_features`.
  - **القرارات المعتمدة من المالك (مقفلة):** مصدر حقيقة هجين · تسجيل ما له قرار تجاري فقط (الكتالوج يدعم أي عمق، بلا انتفاخ) · صلاحيات على النموذج الحالي (أدوار M7 لاحقاً) · فرض الحدود القابلة للعدّ في DB · Versioning بالصيغة الخفيفة · أنواع الباقات باب مفتوح بلا بناء الآن.
  - **خطة التنفيذ (كل مرحلة PR منفصل بموافقة مستقلة، وكل SQL يُعرض ويُتحقّق rollback قبل الاعتماد):** **M0** توثيق (هذا القرار) ✅ · **M1** توسعة `feature_flags` + `feature_categories` + ترحيل + `feature_value`/`effective_features` + `has_feature` مُغلَّفة · **M2** Manifest مُنمَّط + بذر الكتالوج + حارس CI · **M3** `plan_features`(is_included+value) + قواعد الترقية · **M4** FeatureGate + بوابة API خادمية + UpgradeModal + authStore · **M5** حدود مُنمَّطة + فرض DB للموارد القابلة للعدّ · **M6** لوحة Catalog الاحترافية (شجرة/فئات/بحث/فلاتر/تبعيات/باقات مرتبطة/overrides/preview قبل الحفظ؛ change-log مستقبلاً) · **M7** (منفصلة) أدوار Manager/Cashier.
  - **التقدّم — M1 ✅ (منفَّذ على Supabase، يوليو 2026):** `sql/capability_registry_m1.sql` — أُنشئ `feature_categories` + `feature_dependencies` (بـ`dependency_type`)، وُسِّع `feature_flags` بـ21 عموداً (type/scope/parent_key/kind/default_value/allowed_values/metric/unit/lifecycle_status/runtime_status/version/compatibility_version/supersedes/deprecated/name/category_id/module/icon/upgrade_message/sort_order/tags/required_permissions + Audit)، وُسِّع `restaurant_feature_overrides` (`value` jsonb + audit، `enabled` صار NULLable)، وأُضيف الحلّال الموحّد `feature_value(restaurant,key)→jsonb` (override←[الباقة:M3]←default) و`effective_features(restaurant)→jsonb` (مبوّب `has_restaurant_access`/`is_platform_admin`، authenticated فقط). **`has_feature` أُعيد تغليفها فوق `feature_value` بسلوك مطابق 100%** (تحقّق حيّ: 0 اختلاف على كل المطاعم والصفوف). قيد `ff_key_snake_case` أُضيف **`NOT VALID`** (قرار المالك — يفرض على الجديد ويتجاوز الصف القائم). **تحقّق rollback:** limit افتراضي=10/override=3 · mode="self" · feature=true · بوابة `effective_features` ترفض غير المصرّح · anon لا يصل effective_features (يصل has_feature/feature_value) · 0 أثر للاختبار · مستشار الأمان: 0 فئة تحذير جديدة (دوال DEFINER المبوّبة = نمط LAYERS الموثّق). لا بذر للكتالوج (ذلك M2 عبر Manifest + حارس CI).
  - **التقدّم — M2 ✅ (كود + بذر، يوليو 2026):** `src/registry/` (خارج تطبيق العميل، لا يُحمَّل له): **`features.manifest.js`** = مصدر الحقيقة للتعريفات (9 فئات + 31 قدرة + 4 تبعيات: صفحات `nav.js` كـ`page`/`feature` بـ`default_enabled=true` [صفر تغيير سلوكي] + الهيكل الهرمي `menu`/`orders`/`loyalty` + 3 حدود `limit` + وضع `delivery_mode` `mode`). **`buildSeedSQL.js`** = مُولِّد SQL idempotent نقيّ (UPSERT، بلا اتصال DB — يمنع انحراف الكتالوج عن المصدر). **`features.manifest.test.js`** = حارس CI (40 اختباراً، نمط ADR-28): snake_case + تفرّد + صحة type/scope/lifecycle/runtime + مراجع parent/category/dependencies موجودة + mode له allowed_values والافتراضي ضمنها + limit له metric + **منع استخدام مفتاح قدرة مباشرة في الواجهة** (لا `has_feature`/`feature_flags.` خارج طبقة الوصول — يُبنى في M4). **البذر:** `sql/capability_seed_m2.sql` (مُولَّد) طُبِّق على Supabase — تحقّق حيّ: 9 فئات · 32 قدرة (31 + `Simsim` القائم لم يُمَس) · 14 ابناً هرمياً · has_feature سليم (menu=true، orders_refund=false preview) · feature_value مُنمَّط (delivery_mode="self"، products_limit=null بلا حد). CI: build + **154 اختباراً** (114 + 40 حارس). لا لمس لواجهة التطبيق ولا لسلوك أي ميزة.
  - **التقدّم — M3 ✅ (منفَّذ على Supabase، يوليو 2026):** `sql/capability_registry_m3.sql` — **يملأ الفجوة الكبرى** (كان `plans.features` نصاً حرّاً فقط): جدول `plan_features` (`plan_id ↔ feature_key` + **`is_included`** [ضمن الباقة؟] + **`value`** jsonb [قيمة الحد/الوضع] + Audit + RLS إداري). **`feature_value` وُسِّعت** — سلسلة الحسم صارت `override المطعم ← قيمة الباقة ← الافتراضي العام` (الباقة النشطة = `subscriptions.plan_id` حيث `status∈active/trialing/past_due`). **`feature_upgrade_options(key)`** تُرجع الباقات المفعّلة التي تمنح القدرة (الأرخص أولاً) — أساس رسالة الترقية (M4). **دلالة is_included (3 حالات):** لا صفّ→يرث · included=true→(feature:on · limit:value) · included=false→(feature:off صريح · غيره:يرث). **توافق خلفي مضمون:** بلا صفوف `plan_features` (الحالة الآن) طبقة الباقة تُرجع null → السلوك مطابق 100% (has_feature: 0 اختلاف حيّ). محايد لنوع الباقة (Free/Trial/Lifetime/Enterprise مستقبلاً بلا تعديل). **تحقّق rollback:** الباقة تستبعد menu→false · تمنح orders_refund→true · حد=50 من الباقة · **override يفوق الباقة** · upgrade_options صحيحة · صفر أثر · مستشار الأمان: 0 فئة تحذير جديدة. لا بذر ربط باقات (يُدار من لوحة الأدمن M6 أو بقرار منفصل).
  - **التقدّم — M4 ✅ (أول ربط بالواجهة، غير كاسر، يوليو 2026):** **كود فقط (لا SQL).** طبقة **Domain** `src/lib/features.js` (دوال نقيّة: `has`/`value`/`state`/`isRuntimeUsable`؛ **fail-open**: مفتاح غير مسجَّل أو خريطة غير محمّلة → متاح، طرح غير كاسر؛ `preview`/`disabled` = غير مُتاح، `enabled`/`beta` = مُتاح). طبقة **Data-Access** `src/lib/featuresApi.js` (`fetchEffectiveFeatures`/`fetchUpgradeOptions` عبر RPC). **authStore:** حقل `features` يُحمَّل مرة واحدة عند الدخول (`loadFeatures`، fail-safe → {} عند أي خطأ) + محدّدان `hasFeature`/`featureValue` (المرور عبر Domain — حوكمة ADR-40/9) + مسح عند الخروج. **`<FeatureGate feature mode>`** (Page/Component/Button/Action؛ `hide`/`disable`/`upgrade`) يقرأ الخريطة المحمّلة (صفر استعلام — نمط ADR-13). **`<UpgradeModal>`** (يستهلك `feature_upgrade_options` كسولاً، يلتزم `useBodyScrollLock` ADR-19، هوية برتقالية RTL، زر → `/billing`). **الربط الحقيقي غير الكاسر:** فلترة تنقّل `AppShell` تستشير السجل أيضاً (`canAccess && featureHas`) — كل الصفحات `enabled_global=true` → **صفر تغيير مرئي**، لكن الربط حيّ (إخفاء صفحة لباقة مستقبلاً يعمل تلقائياً). اختبار Domain جديد `features.test.js` (17 اختباراً). **الحارس ما زال يفرض** المرور عبر Feature API (لا `has_feature`/`feature_flags` مباشرة في الواجهة). CI: build + **171 اختباراً**. **غير منشور** (فرع فقط؛ الدمج في main = النشر). صفر لمس لقاعدة البيانات ولا كسر لأي سلوك (fail-open + كل القدرات مفعّلة).
  - **التقدّم — M5 ✅ (فرض الحدود في DB، منفَّذ على Supabase، يوليو 2026):** `sql/capability_registry_m5.sql` — فرض حقيقي (لا يُتجاوز من الواجهة) للحدود القابلة للعدّ عبر triggers `before insert`: `products_limit` (products) · `branches_limit` (branches) · `users_limit` (restaurant_members النشطون). دالة عامة `enforce_capability_limit(restaurant,key,count)` تقرأ الحد عبر `feature_value`، **null/غير رقمي → لا فرض** (return مبكر)، وترفع `capability_limit_reached` (errcode check_violation) عند `count >= limit`. الجداول الثلاثة لها `restaurant_id` مباشرة. **التوافق الخلفي مطلق:** كل الحدود null الآن → صفر فرض على أي إدراج قائم؛ الفرض يبدأ حين يُعرّف المالك حدّاً (باقة/override). **تحقّق rollback:** بلا حد=ينجح · بحدّ عند البلوغ=يُرفض · بعد رفع الحد=ينجح · صفر أثر · الـ3 triggers مثبّتة. **أمان:** أُلغيت مِنح PUBLIC عن دالة الفرض ودوال الـtrigger (تُطلَق بصلاحيات المالك بلا حاجة لمنح) — مستشار الأمان 0 خطأ و0 فئة جديدة (الدوال الآن غير قابلة للاستدعاء المباشر). **ملاحظات:** نسخ المنيو عند إنشاء فرع (ADR-22) يخضع لحد المنتجات لو فُعِّل؛ `count(*)` لكل إدراج مقبول بالحجم الحالي (قابل لـdenormalize لاحقاً). **مؤجَّل:** Storage/Upload/Time/API/AI Limits = تعريفات جاهزة (M2)، فرضها الفعلي لاحق (metering لا عدّ صفوف).
  - **التقدّم — M6.1 ✅ (خلفية لوحة الكتالوج، منفَّذ على Supabase، يوليو 2026):** `sql/capability_registry_m6_admin.sql` — **13 دالة `admin_*` مبوّبة** (قراءة `is_platform_admin` · كتابة `platform_admin_can('manage_flags')` كأول بيان + Audit ذرّي): قراءة (`admin_list_capability_categories`/`admin_list_capabilities` بعدّ تبعيات-باقات-تخصيصات/`admin_capability_detail`) · قدرات (upsert/delete) · فئات (upsert/delete مع منع حذف فئة مأهولة) · تبعيات (set/delete) · ربط الباقات (`admin_set_plan_feature`/delete) · تخصيص مطعم مُنمَّط (`admin_set_capability_override`، null=إزالة) · **`admin_sync_capabilities(jsonb)`** مزامنة الـManifest (نقطة المزامنة الوحيدة كود→DB). **الحارس الآلي (ADR-28) يغطّيها تلقائياً** (101 اختبار، البوابة أول بيان). **تحقّق حيّ:** جلسة مشرف محاكاة → كل الدوال تعمل (caps=32·cats=9·detail·upsert→33·plan_feature·override·sync idempotent) ثم تتراجع (0 أثر) · البوابة ترفض غير المصرّح (`forbidden`) · anon/public مُلغى عن الـ13 · مستشار الأمان 0 خطأ و0 فئة جديدة. صفر لمس لجداول المستأجرين. **التالي M6.2:** واجهة كسولة `admin/features/catalog/` (شجرة/بحث/فلاتر/تبعيات/باقات/overrides/preview).
  - **التقدّم — M6.2 ✅ (واجهة لوحة الكتالوج، كود فقط، يوليو 2026):** وحدة Super Admin كسولة معزولة `src/admin/features/catalog/` (`catalogApi.js` + `Catalog.jsx`) — لا تُحمَّل لأي مطعم/زبون. **`catalogApi.js`:** يلفّ دوال M6.1 الـ13 + `manifestSyncPayload()` (تحويل الـManifest → حمولة المزامنة) + إعادة استخدام `listPlans` من billingApi. **`Catalog.jsx`:** لوحة احترافية ضمن `AdminShell` (ثيم داكن/بنفسجي): **شجرة هرمية** مجمّعة بالفئة مع تداخل أب/ابن (`parent_key`) ونقطة حالة ملوّنة (runtime) وشارات عدّ (تبعيات/باقات/تخصيصات) · **بحث + 4 فلاتر** (فئة/نوع/نطاق/حالة تشغيل) · **لوحة تحرير** كاملة (كل حقول القدرة، حقول مشروطة حسب النوع: enabled_global لـfeature · default_value/allowed_values لـmode · metric/unit لـlimit) · **Preview JSON للحمولة قبل الحفظ** · **تبعيات** (إضافة/حذف بنوع) · **باقات مرتبطة** (ربط/فك مع is_included+value) · **تخصيصات المطاعم** (عرض/إزالة) · **مزامنة من Manifest** (زر → `admin_sync_capabilities`) · نوافذ «+ قدرة/+ فئة». أزرار الكتابة مشروطة بـ`can('manage_flags')` (والخادم يفرضها). مربوطة في `adminNav` (🧬 سجل القدرات) + مسار `/admin/catalog` (محميّ `RequirePlatformAdmin`، تحميل كسول في `App.jsx`). **الحارس (ADR-40/9) يمرّ** (لا `has_feature`/`feature_flags` مباشرة — كله عبر دوال admin). CI: build + **197 اختباراً**. **غير منشور** (فرع فقط). **🎉 اكتمل M6 (خلفية + واجهة).** المتبقّي: M7 (أدوار Manager/Cashier — RBAC المؤجَّل ADR-24، قرار منفصل).
  - **التقدّم — M7 ✅ (أدوار Manager/Cashier — تفعيل feature_permissions، UI-soft، يوليو 2026):** **قرارات المالك:** أدوار ثابتة `{manager,cashier,staff}` (owner ضمني عبر `restaurants.owner_id`) · الدور = **قالب افتراضي لـ`allowed_pages` مع تخصيص ممكن (هجين)** · فرض **UI-soft** (لا لمس RLS، متسق مع نموذج `allowed_pages` القائم) · صلاحيات إجراءات الطلبات (من يقبل/يجهّز/يسلّم) **خارج النطاق** (ADR-24، قرار منفصل). **اكتشاف:** `restaurant_members.role` كان موجوداً خاملاً (كله `'staff'`) و`required_permissions` مبذور خامل — M7 يفعّلهما. **الخلفية (`sql/capability_registry_m7_roles.sql`):** قيد `rm_role_valid` (NOT VALID) على قيم الدور + توسعة `effective_features` لتُرجع `required_permissions` لكل قدرة (إضافة حقل — has_feature/القيَم مطابقة، مُتحقَّق حيّاً: staff=["owner"]، menu=[]). **العميل:** `permissions.js` (`MEMBER_ROLES`/`ROLE_LABELS`/`ROLE_PRESETS` [manager=كل غير-owner · cashier=orders · staff=مخصّص] + `canAccess` يفرض `required_permissions` عبر خريطة القدرات: **fail-open** بلا قيود، owner يتجاوز) · `AppShell`+`RequirePage` يمرّران `role`+`capabilities` · `Staff.jsx` (منتقي دور يطبّق القالب على الصفحات ويبقى قابلاً للتعديل + شارة دور في القائمة + حفظ الدور في insert/update). **توافق خلفي:** الموظفون الحاليون يبقون `staff` بـ`allowed_pages` الحالية؛ `required_permissions` المبذورة (`['owner']` على staff/billing/settings) متطابقة مع OWNER_ONLY فصفر تغيير سلوكي. اختبار `permissions.test.js` (12 اختباراً: الأساس + الأدوار + القوالب). CI: build + **209 اختباراً**. **غير منشور** (فرع). **🎉 اكتمل Platform Capability Registry بالكامل (M0→M7).** **Suggestion:** توحيد OWNER_ONLY مع required_permissions (تعميم مستقبلي) · إضافة تحرير `required_permissions` لنموذج Catalog (M6.2 لا يحرّره حالياً).
  - **[ADR-40 / تفعيل الفرض الفعلي] (يوليو 2026):** بعد اكتمال بناء PCR، فُعِّل **الفرض الحقيقي** للقدرات (كان الربط يُخفي روابط السايدبار فقط): **(1) بوابة المسار** — `RequirePage` يفحص `featureHas` بعد `canAccess` (تُطبَّق على الجميع بما فيهم المالك؛ الفصل: `canAccess`=صلاحية الموظف · `featureHas`=بوابة المنصّة/الباقة)، القدرة المُطفأة تعرض شاشة `FeatureUnavailable` (رسالة لا توجيه، تفادي الحلقات)؛ fail-open. + إعادة تحميل القدرات عند عودة تركيز النافذة (`App` focus→`loadFeatures`). **(2) عُدّة FeatureGate كاملة** (Pages=RequirePage/nav · Components=`<FeatureGate>` · Actions=`useFeature` hook جديد · API=`has_feature`/`feature_value` في RPC/Edge) — أي قدرة قابلة للتوصيل مستقبلاً بلا إعادة بناء. **(3) قدرات منيو الزبون** (قرار المالك: توصيل القدرات التجارية المبنية فقط، إخفاء تام): دالة **آمنة لـanon** `menu_capabilities(restaurant_id)` (online_orders→menu_checkout · reviews→menu_reviews · loyalty→loyalty)؛ `useMenuData` تجلبها (fail-open كلها مفعّلة) وتطويها في `loyaltyEnabled`؛ `PublicMenu` وأبناؤه (MenuBody/ProductItem/HProductCard/ProductModal/SearchOverlay/OrdersScreen/OrderCardCollapsed) يُخفون السلة/الإضافة/إعادة الطلب عند إطفاء الطلبات أونلاين، والتقييمات عند إطفائها. **مؤجَّل تعريفاً (غير مبنيّ — قاعدة المالك: لا يُوصَّل حتى يُبنى):** الدفع الإلكتروني · التكاملات · AI · الحجوزات · API · الإشعارات. build + 211 اختباراً. **صفر تغيير سلوكي** (كل القدرات مفعّلة افتراضياً + fail-open).
  - **Suggestion (خارج نطاق المهمة — لم يُلمَس):** يوجد عَلَم اختبار قائم في `feature_flags` مفتاحه `Simsim` (بلا وصف، `enabled_global=true`) + تخصيص واحد لمطعم «كونوها»، غير مستخدَم في أي كود. يُقترح تنظيفه لاحقاً (حذف أو إعادة تسمية snake_case) بقرار منفصل من المالك.
- **[ADR-41] توحيد نظام تصميم Super Admin + إتاحة الوصول (يوليو 2026):** بعد مراجعة UI/UX شاملة عبر 7 محاور (معمارية/UX/واجهة/تنقّل/نظام تصميم/إتاحة/جاهزية توسّع) كشفت أن كل شاشة من شاشات `src/admin/` الثماني كانت تعيد اختراع نفس المكوّنات (Modal/زر/حقل/شارة) محلياً بلا مصدر مشترك، نُفِّذت إعادة تصميم تدريجية على 5 مراحل بموافقة منفصلة لكل مرحلة، **بلا أي تغيير في منطق العمل أو استدعاءات الـAPI أو الهوية البصرية الداكنة/البنفسجية — وبلا لمس SQL إطلاقاً.** **(M1 الأساس)** `src/admin/theme.js` مصدر تُوكِنز ألوان واحد؛ **إصلاح خلل حرج**: `RestaurantDetail.jsx` كان يتحقّق من صلاحية الإدارة بمطابقة `platformRole === 'super_admin'` حرفياً بدل استدعاء `can('manage_restaurants')` الفعلي كبقية الشاشات — كان يُخفي أزرار الإدارة عن أي دور RBAC مخصّص (ADR-30) يملك الصلاحية فعلياً. **(M2 مكوّنات مشتركة)** `src/admin/components/ui/`: `Button`(variant دلالي بدل hex خام)·`Modal`+`ModalActions`(`role="dialog"`+`aria-modal`+Focus Trap+إغلاق `Esc`، لم يكن أيٌّ منها موجوداً)·`Field`·`Badge`·`Loading`/`EmptyState`/`ErrorState` — تستبدل 5 نسخ محلية مكرّرة تقريباً حرفياً في Billing/Flags/Announcements/Admins/RestaurantDetail؛ `admin-theme.css` (نطاق `.admin-root` عبر `AdminShell`) يمنح أول مؤشر `:focus-visible` في اللوحة كلها (مستحيل التعبير عنه بأنماط Inline خالصة). **(M3 جداول)** قائمة المطاعم: عرض بطاقات تحت 768px بدل أعمدة CSS Grid ثابتة العرض كانت تُقصّ على الموبايل + أدوار ARIA (`table`/`row`/`columnheader`) + تشغيل بلوحة المفاتيح للصفوف ورؤوس الفرز (لم يكن موجوداً)؛ سجلّ التدقيق: زر إظهار التغيير أصبح `<button aria-expanded>` حقيقياً بدل `div onClick`. **(M4 تحميل/خطأ)** `Skeleton`(`Tiles`/`Rows`/`Chart`) بدل نص "جارٍ التحميل…" الثابت في كل الشاشات + زر «إعادة المحاولة» في كل حالة خطأ؛ **إصلاح خلل مصاحب اكتُشف أثناء التنفيذ:** دوال `load()` (بما فيها الخمس من M2) لم تكن تُصفِّر `error` عند إعادة النداء — لو نجحت إعادة المحاولة فعلياً كانت شاشة الخطأ القديمة تبقى ظاهرة. **(M5 أيقونات)** 15 أيقونة SVG (`components/ui/Icon.jsx`، `currentColor`، بلا تبعية جديدة) تستبدل Emoji في القشرة الدائمة (التنقّل عبر `NAV_ICON_MAP` مصدر واحد يغذّي `AdminShell` و`CommandPalette` معاً، الترويسة، صفحة الدخول) — **نطاق مقصود:** اقتصر على القشرة عالية الظهور لا كل Emoji زخرفي داخل محتوى الصفحات (خطر رسم يدوي غير دقيق لعناصر ثانوية). تحقّق بصري فعلي (Playwright + لقطات شاشة لكل الأيقونات) قبل الدمج. **مؤجَّل (يحتاج قراراً منفصلاً):** تعميم مؤشر «آخر تحديث» على Growth/Billing/قائمة المطاعم — تحقّق من `sql/admin_growth.sql`/`admin_list_restaurants.sql`/دوال الفوترة أنها **لا تُعيد أي timestamp حداثة أصلاً** (فقط `admin_dashboard()` يفعل)؛ إضافته بصدق تتطلّب تعديل SQL خلفي، مختلف الطبيعة والمخاطر عن هذه المرحلة الواجهية البحتة. **مؤجَّل بقرار (غير مجدول، ليست عاجلة بحجم الاستخدام الحالي):** إجراءات جماعية (Bulk)، تصدير CSV، تحذير انتهاء الجلسة. **PR #193** (5 كوميتات، فرع `claude/super-admin-review-e9o5hv`) — بناء + 114 اختبار ناجحة بعد كل مرحلة (نفس عدد اختبارات ADR-39، لم تُضَف اختبارات جديدة لأن التغيير واجهي بصري لا منطقي).

- **[ADR-42] Analytics Core Layer — طبقة تحليلات أساسية قائمة على الأحداث (M0: تثبيت التصميم، بلا كود) (يوليو 2026):** بقرار المالك تُعتبر التحليلات **طبقة أساسية (Core Layer)** لا مجرد صفحة تقارير — مصدر مركزي تعتمد عليه كل الوحدات (حالية ومستقبلية: Menu·Orders·Loyalty·Reviews·Payments·Delivery·PCR·Integrations·CRM·Marketing·AI·Notifications) عبر **Event-Driven Architecture**: الوحدات **منتِجة (Producers)** للأحداث، وطبقة التحليلات **مستهلِكة (Consumer)** تحوّلها لمؤشرات — **بلا اعتماد عكسي** (المنتِج لا يعرف التحليلات إطلاقاً). **القرار الجوهري — تصميم هجين (Hybrid) يبني على ADR-31 ولا يكرّره:** الحقائق التي لها «سجل مصدر» (طلبات/إيرادات/MRR/صحّة المطاعم عبر `platform_daily_metrics`/`restaurant_stats`/`refresh_platform_metrics`+pg_cron، و`subscription_events`/`invoice_events`) **تبقى في مكانها ولا يُعاد بناؤها كأحداث**؛ ويُضاف **ناقل أحداث عام** فقط لِما **لا سجل مصدر له اليوم** (سلوك العميل: QR scans·product views·cart events·funnel·customer journey) وكعمود فقري قابل للتوسّع للمنتجين المستقبليين. يخدم التصميم **ثلاثة مستويات دون إعادة تصميم**: Platform (Super Admin)·Restaurant (Owner)·Customer — عبر حقل `scope`+`restaurant_id`. **المكوّنات الأربعة:** **(1) Event Store** `analytics_events` (append-only، مُهيّأ للتقسيم الشهري لأن أحداث العميل كثيرة): `event_id·occurred_at·ingested_at·scope(platform/restaurant/customer)·restaurant_id·branch_id·actor_type(system/owner/staff/customer/anon)·session_id·event_type(مُنمَّط مثل menu.product_viewed·order.placed)·entity_type·entity_id·props(jsonb)·source_module·dedupe_key`؛ مفهرس `(scope,event_type,occurred_at)` و`(restaurant_id,occurred_at)`. **(2) عقد المنتِج (Decoupling)** مسار إدخال واحد: `emit_event()` للمنتجين داخل القاعدة (triggers/RPC)، و RPC آمن لـanon `track_event()` (محدود المعدّل + قائمة أنواع مسموحة allowlist + RLS بحسب scope) + مساعد واجهة صغير `track()` (batching، إطلاق-وانسَ). **ضمانات:** غير كاسر ولا معطِّل (فشل حدث لا يكسر فعل المستخدم أبداً)، fail-open، non-blocking. **(3) المستهلك/التجميع** تعميم نمط `refresh_*`+pg_cron الموجود (ADR-31) لطيّ الأحداث الخام إلى جداول مؤشرات لكل نطاق (لا مسح خام في زمن القراءة). **(4) طبقة قراءة موحّدة** خدمة `analyticsApi`+RPCs تخدم المستويات الثلاثة من التجميعات وتُخفي المصدر؛ الدوال الحالية (`get_analytics_summary`·`get_advanced_analytics`·`admin_dashboard`) تُدمَج تدريجياً بلا إعادة كتابة. **القرارات الثلاثة المعتمدة (توصية المالك — الأقل مخاطرة):** (أ) الأحداث الخام تُحفظ + **تقسيم شهري** + احتفاظ ~90 يوماً للخام مع إبقاء التجميعات للأبد؛ (ب) إدخال أحداث العميل عبر **RPC مباشر `track_event`** الآن (Edge Function لاحقاً عند كبر الحجم)؛ (ج) أحداث الفوترة الحالية **تبقى كما هي** ويُوحَّد التوحيد في طبقة القراءة (بلا ترحيل، بلا مخاطر). **الحوكمة:** `event_type` مُنمَّط `module.action` snake_case + allowlist مركزية (منع أحداث عشوائية)؛ أمان anon (rate-limit + التحقق من النوع + لا PII حسّاسة في props)؛ RLS لكل نطاق (المطعم يرى أحداثه·المشرف الكل·العميل بلا قراءة). **الخطة على مراحل (نمط PCR):** **M0** (هذه — توثيق ADR فقط، بلا كود) · **M1** Event Store + عقد المنتِج (بلا مستهلكين، غير كاسر) · **M2** منتجون مرجعيون (QR scan·product_viewed·cart events — إثبات المسار) · **M3** التجميع + إظهار مؤشرات مفيدة في لوحة Super Admin **من البيانات الموجودة** (إعادة استخدام `platform_daily_metrics`؛ إضافة الجديد فقط: استخدام الميزات من PCR + funnel العميل) · لاحقاً: لوحات المطعم/العميل ومنتجون إضافيون. **صفر كود في M0** — يبدأ الكود في M1 بعد موافقة منفصلة.

- **[ADR-43] تنفيذ الهوية البصرية المعتمدة عبر المنتج (Brand Implementation) (أغسطس 2026):** تحويل «نظام هوية SIMSIM النهائي» إلى تنفيذ متّسق عبر كل السطوح **دون إعادة تصميم أي عنصر من الهوية** — اعتُمدت أصول المالك كما هي (Single Source of Truth)، والقرار: الكود يتبع الهوية لا العكس. **القيم المعتمدة:** البرتقالي `#FF6B35`→**`#FF6A00`** (SIMSIM Orange)، الأسود `#0F1117`→**`#0B0B0F`** (SIMSIM Black)، درجة hover مشتقّة `#E05D00` (ليست لوناً جديداً)، والخطوط `Cairo`→**`Tajawal`** (عربي) + **`Poppins`** (لاتيني/الـWordmark فقط)، مع مواءمة السلّم المحايد. **رمز الـS:** الرمز الرسمي كملف متجّه `public/simsim-s.svg` (fill-rule evenodd — S برتقالي بداخله S أبيض داخلي)؛ منه اشتُقّ favicon (مربّع `#0B0B0F` + S برتقالي = أيقونة «Black») وog-image (بطاقة سوداء + الرمز + wordmark أبيض)؛ والـWordmark صار `simsim` صغيرة (جزء عادي + جزء برتقالي) بدل `SIMSIM`. **النطاق والتنفيذ التدريجي (بموافقة منفصلة لكل مرحلة + بناء + تحقّق بصري Playwright):** **(الموقع التسويقي)** كان مبنياً على Tokens (`--ss-*` في `landing.css`) فكفى تصحيح القيم مركزياً + إضافة Poppins وتوسيع أوزان Tajawal(800/900) في `@import` بـ`index.css` + ربط الرمز في Navbar/Footer. **(التطبيق — D1 ألوان)** اللوحة inline-styled بالكامل (لا Tokens): استبدال آلي للقيم عبر 38 ملفاً (`src/pages/*`·`components/*`·`features/menu/*`) + تصحيح `index.css :root`. **(D2 شعار)** استبدال حرف «S» النصّي بالرمز الرسمي في AppShell·Login·Register·ForgotPassword·ResetPassword·Onboarding·MenuBranding. **(D3 خطوط)** `Cairo→Tajawal` عبر ~40 ملفاً؛ `typography.js` ثابت `CAIRO`→`HEADING`. **(لوحة الأدمن — الخيار «أ»)** الحفاظ على الثيم الداكن المميّز (ADR-27/41) مع استبدال الأكسنت البنفسجي بالبرتقالي المعتمد: `theme.js` `ACCENT` #7C3AED→#FF6A00 و`ACCENT_SOFT`، ودرجات `#A78BFA→#FF8A3D`·`#C4B5FD→#FFB27F`، + `Cairo→Tajawal` + إضافة الرمز الرسمي لشعار AdminShell/AdminLogin. **(Brand Voice)** نبرة رسائل دافئة متوازنة (قسم 28 من الهوية): حذف «يرجى» → أمر مباشر، إرشاد للفعل في الأخطاء المجرّدة («تعذّر الحفظ، جرّب ثانية»·«حصل خطأ، جرّب مرة ثانية»)، ولمسة إنسانية للفراغات («ما لقينا نتائج مطابقة»·«ما في عملاء بعد») — دون المساس بالرسائل السليمة أصلاً. **ضمانات:** تعديلات بصرية/نصّية بحتة — **بلا أي تغيير في منطق العمل أو قاعدة البيانات أو استدعاءات الـAPI، وبلا لمس SQL**؛ البناء `Build (Vite)` ينجح في كل مرحلة. `#FFF0EB` (primary-light) و`#FF9F6B` (تدرّج فاتح) أُبقيا. الـ`@import` ما زال يُبقي تحميل Cairo (غير مستخدم كخط فعلي بعد الآن — قابل للإزالة لاحقاً). **الدمج:** PR #236 (squash) — 71 ملفاً (+718/−728). **مؤجَّل (Suggestions لقرار منفصل):** توحيد توكنات Radius/Spacing (مخاطرة عالية — 600+ قيمة inline)، Component Library مشترك للوحة، جعل الأرقام/الأسعار بخط Poppins (توكن `numbers`)، تحسين بصري لحالات المنتج (Empty/Loading/Error).

- **[ADR-44] Customer Order Journey — PHASE 1 Hotfix (إعادة إحياء مسار الطلب المعطوب) (أغسطس 2026):** بعد تدقيق معماري شامل لرحلة الطلب (وثيقة "Customer Order Journey — E2E" المُسلَّمة من المالك) كشف أن مسار الطلب **متوقّف كلياً في الإنتاج** منذ نشر النسخة الحالية من `create_order` (155 طلباً قديماً، صفر طلب ناجح بالعقد الحالي)، نُفِّذت PHASE 1 فقط (إعادة الحياة + إغلاق الثغرتين الحرجتين) بموافقة صريحة من المالك على القرار D-08 (A2)، على أن تبقى المراحل 2–10 (السلة/الدفع/Idempotency/آلة الحالة/التتبّع اللحظي/التصلّب الأمني/الأداء/الاختبار) معلّقة لموافقات منفصلة لاحقة كما تنصّ الوثيقة. **الأعطال الثلاثة المُصلَحة (كلها VERIFIED فعلياً بمعاملة rollback + مستشار أمان Supabase قبل/بعد):** **(B1)** `create_order` كانت تفشل بخطأ `55000` في كل طلب **بلا كوبون** — المتغيّر `v_coupon record` يُقرأ (`v_coupon.id`) بعد شرط `if v_coupon.id is not null` بلا إسناد سابق إن لم يُرسَل كوبون. **الإصلاح (D-08/A2):** متغيّر صريح `v_coupon_id uuid` يُسنَد فقط داخل كتلة معالجة الكوبون، ويُستخدم بدل `v_coupon.id` في الشرط النهائي — بلا أي تغيير في منطق التسعير/التحقّق (13 فحصاً + إعادة التسعير الخادمية بقيت كما هي حرفياً). **(B2)** المشغّل `trg_broadcast_order_status` كان يستدعي `realtime.broadcast(...)` **غير الموجودة** أصلاً في هذه النسخة من Supabase (الموجود `realtime.send`) فيفشل **كل** `UPDATE` على `orders` بخطأ `42883` — أي أن دورة حياة الطلب (قبول/تحضير/جاهز/تسليم/إلغاء) كانت معطّلة بالكامل. **الإصلاح:** استبدال الاستدعاء بـ`realtime.send(payload, event, topic, private)` (ترتيب معاملات مختلف) + `coalesce(order_access_token,'')` لتفادي `NULL` على الطلبات القديمة + تغليف الاستدعاء بـ`exception when others then null` بحيث **لا يمكن لفشل البثّ مستقبلاً أن يُعطّل تحديث الطلب مرة أخرى**. **(B3)** سياسة `orders_cancel_public` (`FOR UPDATE TO PUBLIC USING (status='pending') WITH CHECK (status='cancelled')`) + منحة `UPDATE`/`INSERT`/`DELETE` لدور `anon` على `orders` كانتا تسمحان بإلغاء جماعي مجهول لكل طلبات المنصّة بطلب HTTP واحد (بلا حاجة لصلاحية SELECT)، وسياسة `orders_insert_public` كانت تسمح بإدخال طلبات مزوَّرة بحالة/سعر حرّين (مُتحقَّق أن لا كود في المستودع يستخدم أياً من السياستين — الإدخال/الإلغاء الشرعيان يمرّان حصراً عبر RPC بصلاحيات `SECURITY DEFINER` لا تتأثر بسحب المنح). **الإصلاح:** `DROP POLICY orders_cancel_public/orders_insert_public` + `REVOKE INSERT, UPDATE, DELETE ON orders FROM anon`، **بعد** نقل إلغاء الزبون (`useActiveOrders.js: cancelOrderByCustomer`) من `UPDATE` مباشر إلى الدالة الآمنة المنشورة مسبقاً `cancel_order_by_customer(id, access_token)` (كانت منشورة وغير مستخدَمة) — `useCheckout.js` أصبح يحفظ `access_token` الراجع من `create_order` ضمن الطلب في `localStorage`، و`OrderCardActive.jsx` يُخفي زرّ الإلغاء للطلبات القديمة المحفوظة بلا رمز (بدل فشل صامت). **التحقّق:** كل تغيير SQL اختُبر أولاً داخل معاملة تنتهي بـ`ROLLBACK` مضمون على قاعدة الإنتاج الحيّة (نفس أسلوب ADR-25/ADR-29) قبل التنفيذ الفعلي، ثم أُعيد الاختبار بعد التنفيذ بمحاكاة دور `anon` الحقيقي (`SET ROLE anon`) لإثبات رفض `INSERT`/`UPDATE` المباشرين، ومستشار أمان Supabase أظهر **صفر تحذير جديد** (نفس مستوى ADR-29). 300/300 اختبار محلي و`npm run build` ناجحان بلا تغيير. **الملفات:** `sql/order_journey_hotfix.sql` (جديد، MIG-001..004 موثّقة برجوعها) · `useActiveOrders.js` · `useCheckout.js` · `OrderCardActive.jsx`. **الحالة:** الإنشاء والانتقالات وإلغاء الزبون **تعمل الآن فعلياً** على الإنتاج؛ الرحلة الكاملة (السلة/الدفع/الاختبار الآلي/التتبّع اللحظي بالرمز/آلة الحالة المفروضة قاعدياً) **تبقى محكومة بموافقات D-09..D-12 لكل مرحلة تالية على حدة** كما وثّقتها خطة التنفيذ الأصلية.

- **[ADR-45] Customer Order Journey — PHASE 2 (عزل السلة بالفرع) (أغسطس 2026):** تنفيذ TASK-CART-001/002/003 بموافقة صريحة من المالك على القرار **D-10 = الخيار B** (رسالة صريحة بخيارين بدل مفتاح تخزين منفصل لكل فرع). **لا تغيير في قاعدة البيانات — كله واجهة.** **(TASK-CART-001 عزل السلة)** حمولة `localStorage['simsim_cart_<slug>']` أصبحت تحمل `branchId` (دالة خالصة جديدة `readStoredCart` في `cartHelpers.js`، قابلة للاختبار بمعزل عن React، على نمط `searchUtils.js`/`pricing.js` القائم): سلة محفوظة لفرع مختلف **وبها أصناف** لا تُحمَّل صامتة بعد الآن — بدل ذلك تُطرح كنزاع (`branchConflict`) والسلة النشطة تبقى فارغة حتى يقرر الزبون عبر `ConfirmDialog` موحّد (نفس مكوّن تأكيد إلغاء الطلب): **«فرّغ وابدأ من هنا»** (يمسح سلة الفرع القديم نهائياً) أو **«ارجع للفرع السابق»** (تنقّل لـ`/menu/<slug>?branch=<الفرع المحفوظ>`، بإسقاط رمز طاولة QR إن وُجد عمداً — القرار قرار المنيو العادي للفرع لا جلسة QR الحالية). سلال **قديمة بلا `branchId`** (محفوظة قبل هذا التغيير) تُعامَل بتوافق خلفي كأنها لنفس الفرع فلا يُفاجَأ أي زبون قائم. الهوية المعتمدة للفرع هي `branch.id` **المُحلّل فعلياً** (بعد اكتمال `useMenuData`) لا معامل `?branch=` الخام مباشرة — يتفادى نزاعاً وهمياً أثناء التحميل الأول. **(TASK-CART-002 مزامنة التبويبات)** مستمع `window.addEventListener('storage', ...)` في `useCart.js` يعيد تفسير الحمولة عبر نفس `readStoredCart` عند أي تعديل من تبويب آخر لنفس المفتاح. **(TASK-CART-003 فحص الصلاحية قبل الدفع)** دالة خالصة ثانية `validateCartAgainstProducts` تطابق كل سطر سلة بـ`products` **المحمَّلة أصلاً** (مُصفّاة سلفاً بـ`is_available=true` من `useMenuData` — فغياب المعرّف من القائمة يعني حذفاً أو عدم توفّراً بغضّ النظر عن السبب، **صفر استعلامات إضافية**): `CartDrawer.jsx` يُعلِّم السطر المتأثر بصرياً (تعتيم + رسالة «لم يعد متوفراً — أزِله للمتابعة» + تعطيل ✎/+ مع إبقاء 🗑) أو يعرض السعر الجديد صراحة عند تغيّره، ويُعطَّل زرّ «تأكيد الطلب» بالكامل مع رسالة تشرح السبب طالما بقي صنف غير متوفر في السلة — بدل رفض خادمي غامض (`product is unavailable for this branch`) بعد الضغط. **الفحص واجهي بحت** (تجربة مستخدم لا حدود أمان) — الخادم (`create_order`) يبقى المرجع النهائي الوحيد للسعر والتوفّر كما هو منذ التدقيق الأصلي. **التحقّق:** 11 اختباراً جديداً في `cartHelpers.test.js` (UT-CART-009 ومكافئات TTL/التوافق الخلفي/كشف الحذف/تغيّر السعر) — 311/311 محلياً و`npm run build` ناجحان. **الملفات:** `src/features/menu/hooks/cartHelpers.js` (جديد) + اختباره · `useCart.js` · `PublicMenu.jsx` · `CartDrawer.jsx` · `i18n.js`. **الحالة:** PHASE 3 وما بعدها تبقى بانتظار موافقات D-11/D-12 كما في ADR-44.

- **[ADR-46] Customer Order Journey — PHASE 3 (الدفع/Checkout) (أغسطس 2026):** تنفيذ TASK-CHK-001..004 — لا تحتاج قرار مالك جديد (§18 من الوثيقة الأصلية). **لا تغيير في قاعدة البيانات — كله واجهة.** **(TASK-CHK-001 مهلة زمنية)** استدعاء `create_order`/`create_order_from_table_qr` في `useCheckout.js` أصبح مُغلَّفاً بـ`withTimeout` **الموجودة أصلاً** في `src/lib/asyncTimeout.js` (15 ثانية) — يمنع سبيرنر بلا نهاية على شبكة معلَّقة، ويعرض رسالة تنصّ صراحة أن **الطلب لم يُنشأ**. **نطاق مقصود أضيق من التوصية الكاملة في الوثيقة:** لم يُضَف زرّ «إعادة محاولة» بمفتاح Idempotency لأن TASK-CHK-001 نفسها موسومة بالاعتماد على `TASK-ORD-002` (PHASE 4، لم تُنفَّذ بعد) — إعادة المحاولة اليوم تبقى يدوية (زرّ التأكيد العادي يعمل من جديد بعد المهلة) دون ضمانة عدم التكرار؛ سيُضاف الزرّ المخصّص فور تنفيذ PHASE 4. **(TASK-CHK-002 رسائل عربية)** `src/features/menu/orderErrors.js` (جديد، دالة خالصة `mapOrderError`) يحوّل كل رسائل التحقّق الـ13 الموثّقة في §6.3 من الوثيقة الأصلية (نصّ Postgres الخام) إلى عربي/إنجليزي — بديل احتياطي عام لأي رسالة غير مُدرَجة، بدل عرض رسالة الخادم الخام كما كان يحدث. **(TASK-CHK-003 توحيد الخصم)** دالة خالصة جديدة `computeCouponDiscount` في `lib/pricing.js` (مصدر التسعير الموحّد — ADR-1) تطابق منطق `create_order` حرفياً: نفس التقريب (`round(x,2)`) وتطبيق `max_discount_amount` وقصّ الخصم عند الإجمالي — كانت `useCoupon.js` تتجاهل الاثنين فيُعرض للزبون مبلغ أقل مما سيرفضه الخادم فعلياً (سبب الطريق المسدود الموثَّق في §6.5). أُضيف أيضاً فحص `usage_limit` عند تطبيق الكوبون بدل اكتشافه لاحقاً عند التأكيد فقط. **(TASK-CHK-004 حل طريق `price_changed` المسدود)** كان الرد يُظهر توست عابر «تم تحديث السعر» **بلا أي تغيير في السلة** فيتكرّر الرفض إلى ما لا نهاية إن كان سبب الفارق مقيماً (كوبون بسقف خصم — بالضبط ما أصلحه TASK-CHK-003 أعلاه لغالب الحالات). الآن: `useCheckout.js` يحتفظ برد الخادم (`{oldTotal, newTotal}`) في حالة `priceChangeInfo`، و`CartDrawer.jsx` يعرضه صراحة (السعر القديم مشطوب ← الجديد) مع زرّ مخصّص **«حدّث وتابع»** يُعيد الإرسال بالسعر الصحيح الذي أعاده الخادم فعلاً — **بلا إعادة إرسال تلقائية صامتة** (قرار متعمَّد: تغيّر السعر يُعرض على الزبون قبل أي التزام مالي جديد، لا يُخفى خلف إعادة محاولة آلية). زرّ التأكيد العادي يتعطّل تماماً طالما `priceChangeInfo` قائمة، فلا يمكن تجاوز الرسالة بالخطأ. **التحقّق:** 14 اختباراً جديداً (`pricing.test.js`: تطابق الخصم مع الخادم بما فيه أمثلة §6.4/§6.5 الحقيقية من الوثيقة · `orderErrors.test.js`: كل رسائل §6.3 الـ13 مُترجَمة + رسالة احتياطية) — 325/325 محلياً و`npm run build` ناجحان. **الملفات:** `src/features/menu/orderErrors.js` (جديد) + اختباره · `lib/pricing.js` + اختباره · `useCoupon.js` · `useCheckout.js` · `CartDrawer.jsx` · `PublicMenu.jsx` · `i18n.js`. **الحالة:** PHASE 4 (Idempotency وترقيم الطلب الذرّي) لا تحتاج قرار مالك جديد أيضاً — بقية المراحل تنتظر D-09/D-11/D-12.

- **[ADR-47] Customer Order Journey — PHASE 4 (Idempotency + ترقيم ذرّي) (أغسطس 2026):** تنفيذ TASK-ORD-002/003 على قاعدة الإنتاج مباشرة — لا تحتاج قرار مالك جديد. **(TASK-ORD-002 Idempotency)** عمود `orders.idempotency_key uuid` + فهرس فريد جزئي `WHERE idempotency_key IS NOT NULL` (يسمح بأي عدد من `NULL` للطلبات القديمة/بلا مفتاح) + معامل جديد `p_idempotency_key uuid DEFAULT NULL` في نهاية توقيعَي `create_order` و`create_order_from_table_qr` (توافق خلفي كامل — أي استدعاء قديم بلا المعامل يستمر يعمل بلا Idempotency كما كان). أول بيان فعلي داخل `create_order`: لو المفتاح مُرسَل وموجود مسبقاً لنفس `restaurant_id`، **يُعاد الطلب القائم فوراً بلا تكرار أي فحص أو إدخال** (`price_changed=false` دائماً في هذا المسار — الطلب موجود أصلاً). **الواجهة:** `useCart.js` يولّد `crypto.randomUUID()` مرة واحدة عند أول تحوّل للسلة من فارغة إلى بها أصناف (لا عند كل ضغطة)، يحفظه في `localStorage['simsim_idem_<slug>_<branchId>']` (مفتاح منفصل تماماً عن مفتاح السلة نفسه)، وتلقائياً يُبطَل (يُمسَح فيتولّد غيره لاحقاً) بمجرد أن تصبح السلة فارغة — سواء بعد نجاح الطلب أو أي سبب آخر (نزاع فرع، تفريغ يدوي). `useCheckout.js` يمرّره في كل محاولة إنشاء — **نفس المفتاح حتى مع إعادة المحاولة بعد `price_changed`** (طبيعي وآمن: أول محاولة لم تُدرِج شيئاً، فالمحاولة الثانية بنفس المفتاح تنشئ الطلب بلا أي تعارض). **(TASK-ORD-003 ترقيم ذرّي)** `generate_order_number()` كانت تحسب `COUNT(*)+1` **بلا أي قفل** — سباق تزامن حقيقي وارد عند أول ذروة. استُبدلت بـ`pg_advisory_xact_lock(hashtext(restaurant_id)::bigint)` (يسلسل إدخالات نفس المطعم فقط، يتحرّر تلقائياً عند COMMIT/ROLLBACK) ثم `max(الرقم المُستخرَج من order_number)+1` بدل `COUNT`+1 — يمنع أيضاً **إعادة استخدام رقم بعد حذف طلب** (كانت مشكلة إضافية موثَّقة في §6.8 من الوثيقة الأصلية). أُضيف أيضاً `SET search_path = public` (لم يكن مضبوطاً) و**قيد تفرّد صريح** `UNIQUE (restaurant_id, order_number)` كخط دفاع أخير حتى لو تعطّل القفل الاستشاري لأي سبب — الإدخال يفشل بصوت عالٍ (`unique_violation`) بدل تكرار صامت. **تحقّق ما قبل التنفيذ:** صفر تعارضات موجودة في 155 صفاً (فحص مباشر قبل إضافة القيد). **تحقّق بعد التنفيذ (rollback مضمون على الإنتاج):** IT-ORD-019 — استدعاءان بنفس مفتاح Idempotency ⇒ نفس `id` مُعاد، وصفّ واحد فقط في القاعدة؛ IT-NUM-001 (مبسّط، 5 إدخالات متتالية داخل نفس المعاملة) ⇒ 5 أرقام متتالية فريدة بلا تصادم. مستشار أمان Supabase: **صفر تحذير جديد**. **الملفات:** `sql/order_idempotency.sql` (جديد) · `sql/order_number_atomic.sql` (جديد) · `cartHelpers.js` (+`idempotencyStorageKey`) · `useCart.js` · `useCheckout.js` · `PublicMenu.jsx`. **التحقّق:** 1 اختبار جديد لصيغة المفتاح + 326/326 محلياً و`npm run build` ناجحان. **الحالة:** PHASE 5 فما فوق تحتاج D-09 (سلوك تراجع المطعم) لآلة الحالة، وD-11/D-12 لبقية المراحل.

- **[ADR-48] Customer Order Journey — PHASE 6 جزئي (التتبّع اللحظي والقراءة الآمنة) (أغسطس 2026):** تنفيذ TASK-ORD-004 + TASK-TRK-001 + TASK-TRK-002 — لا تحتاج قرار مالك جديد؛ **TASK-TRK-004 (قناة `restaurant-orders`) لم يُنفَّذ عمداً** لأنه الوحيد من مهام PHASE 6 المرهون بـD-12 (إصلاحها أم إزالتها) — لم يُتَّخذ بعد. **لا تغيير في قاعدة البيانات** — `get_orders_status_secure`/`cancel_order_by_customer` كانتا منشورتين وجاهزتين منذ التدقيق الأصلي، والمطلوب فقط أن تستعملهما الواجهة. **(TASK-ORD-004 القراءة الآمنة)** `useActiveOrders.js` يستبدل `get_orders_status(uuid[])` (بلا إثبات ملكية — أي UUID صحيح يُرجع بياناته) بـ`get_orders_status_secure([{id, access_token}])` لكل الطلبات التي تحمل `accessToken` (كل الطلبات الجديدة منذ PHASE 1)؛ الطلبات القديمة **المحفوظة محلياً بلا رمز** (قبل هذا الفرع) تبقى مؤقتاً على المسار القديم حتى تنتهي مهلة `localStorage` الخاصة بها (12 ساعة) — توافق خلفي صريح بلا كسر أي جلسة قائمة. تحقّق الإلغاء (`cancelOrderByCustomer`) يستخدم نفس القراءة الآمنة الآن أيضاً. **(TASK-TRK-001 تصحيح اسم القناة)** كانت القناة `order-status:<id>` **لا تطابق أصلاً** نمط سياسة `realtime.messages` (`order-status:%:%`) فتُرفَض دائماً بصمت — البثّ اللحظي للزبون **لم يعمل قط** حتى بعد إصلاح B2 في PHASE 1 (كان B2 يمنع فشل التحديث، لكن الاشتراك نفسه كان مرفوضاً من الأساس). أصبحت `order-status:<id>:<accessToken>` مطابقة تماماً؛ الطلبات بلا رمز لا تشترك بقناة إطلاقاً (بدل قناة ستُرفَض حتماً) وتبقى على الاستطلاع. **(TASK-TRK-002 استطلاع ذكي)** الفاصل الزمني الثابت 5 ثوانٍ استُبدل بجدولة ذاتية التكيّف: يتوقّف تماماً بينما التبويب مخفي (`visibilitychange`، يستأنف فوراً عند العودة)، ويتباطأ تدريجياً **5ث ← 15ث ← 30ث** بمجرد وصول أول حدث بثّ لحظي حقيقي لأي طلب (إشارة أن الـRealtime يعمل فعلياً لهذا الزبون) — الطلبات بلا قناة (بلا رمز) تُبقي الاستطلاع على 5ث دائماً لأنها لا تملك مصدراً بديلاً. مؤشّر **«آخر تحديث»** جديد أعلى قائمة «قيد التنفيذ الآن» في `OrdersScreen.jsx` (دالة خالصة `formatLastSynced` في `orderTracking.js` جديد — الآن/بالثواني/بالدقائق، عدّاد حيّ معزول بمكوّن صغير كي لا يُعيد رسم الشاشة كلها كل ثانية). **التحقّق:** 5 اختبارات جديدة لـ`formatLastSynced` — 331/331 محلياً و`npm run build` ناجحان. **الملفات:** `src/features/menu/orderTracking.js` (جديد) + اختباره · `useActiveOrders.js` · `OrdersScreen.jsx` · `PublicMenu.jsx` · `i18n.js`. **الحالة:** PHASE 5 (آلة الحالة) تنتظر D-09؛ TASK-TRK-004 من PHASE 6 ينتظر D-12؛ PHASE 9/10 تنتظر D-11.

- **[ADR-49] Customer Order Journey — PHASE 9 جزئي: الحرّاس الثابتة (TASK-TST-001) + إغلاق انحراف SEC-003 التوثيقي (أغسطس 2026):** بعد أن أوضح المالك عدم معرفته باعتماد الكاشير على زر «تراجع» (D-09 معلّق فعلياً)، تابعتُ بالمهمة الوحيدة المتبقية من الخطة التي **لا تحتاج أي قرار مالك إطلاقاً وبلا مخاطرة على الإنتاج**: TASK-TST-001 — الحرّاس الآلية التي تمنع تكرار B1/B2/SEC-001/SEC-002/SEC-003 (السبب الجذري الحقيقي للأزمة الأصلية كان انحراف صامت بين الإنتاج والمستودع، لا خطأ برمجي عابر). **لا تغيير في قاعدة البيانات في أي جزء من هذه المرحلة.** **(تمهيد ضروري)** أثناء بناء `GUARD-SQL-004` اكتُشف أن 4 دوال حيّة في الإنتاج (`get_orders_status_secure`، `cancel_order_by_customer`، `can_read_order_status`، و`get_orders_status` — هذه الأخيرة GAP-SEC-003 نفسها) **لم يكن لها ملف `sql/` مقابل إطلاقاً** رغم أن الكود يستدعيها — نفس فئة الانحراف الموثّقة أصلاً في §R-05 من التدقيق الأصلي. وثّقتها حرفياً (نسخ مطابق تماماً بلا أي تغيير منطقي، مُتحقَّق بـ`pg_get_functiondef` مباشرة من الإنتاج) في `sql/order_status_reads.sql` الجديد، مع تعليق صريح على `get_orders_status` يوسمها GAP-SEC-003 ويوضح أنها تبقى مؤقتاً (تُسحب في TASK-SEC-003 لاحقاً بعد انتقال كل العملاء — **لم تُسحب الآن**). كما وسمتُ `sql/create_order_rpc.sql` (توقيع 15-arg قديم من ADR-25، كان يحمل تعليقاً مضلِّلاً موثَّقاً كـHIGH priority منذ التدقيق الأصلي) وموضع سياسة `orders_insert_public` القديمة في `sql/table_qr_system.sql` بتعليقين توضيحيين فقط (بلا تعديل أي منطق SQL فيهما) يشيران للتعريف الحيّ الحالي ولحقيقة إسقاط السياسة لاحقاً في PHASE 1 — الملفان يبقيان سجلّاً تاريخياً صحيحاً بدل أن يُقرآ خطأً كواقع حالي. **(الحرّاس نفسها)** `src/lib/orderJourneyGuards.test.js` جديد على نمط `adminGate.test.js` القائم حرفياً (فحص Offline لملفات `sql/` بلا شبكة، ضمن `npm test` الحالي بلا تعديل CI): **GUARD-SQL-001** كل دالة `SECURITY DEFINER` في نطاق دوال رحلة الطلب المعروفة تتحقّق من `order_access_token` أو مُستثناة بالاسم صراحة مع تبرير مكتوب (استثناءان فقط: `create_order`/`create_order_from_table_qr` تُنشئان الرمز ولا تتحقّقان من رمز قائم، و`get_orders_status` GAP-SEC-003 موثَّق). **GUARD-SQL-002** لا سياسة `CREATE POLICY` على `orders` في `sql/` تمنح `INSERT/UPDATE/DELETE` لـ`anon`/`PUBLIC` فعلياً (مع استثناء صريح واحد بالاسم لسياسة أُسقطت لاحقاً وتاريخها موثَّق أعلاه). **GUARD-SQL-003** كل استدعاء `realtime.*` في `sql/` ضمن الدوال الأربع الموجودة فعلاً في مخطّط realtime (`send`/`send_binary`/`broadcast_changes`/`topic`). **GUARD-SQL-004** كل RPC معروفة في رحلة الطلب (`create_order`، `create_order_from_table_qr`، `cancel_order_by_customer`، `get_orders_status`، `get_orders_status_secure`، `can_read_order_status`، `resolve_table_qr`) لها تعريف فعلي في `sql/` — **نطاق مقصود** بقائمة دوال رحلة الطلب المعروفة فقط، لا مسح تلقائي لكل استدعاءات `rpc()` في المستودع (٥٠+ دالة عبر أنظمة أخرى — ولاء/فوترة/إدارة — لم تُدقَّق في هذه الرحلة ولها معايير حوكمة مختلفة؛ **Suggestion لقرار منفصل:** تعميم نسخة تلقائية كاملة لاحقاً). **GUARD-STM-001 لم يُنفَّذ عمداً** — هدفه مطابقة ثابت `STATUS` في `Orders.jsx` مع `sql/order_state_machine.sql`، والملف الثاني غير موجود أصلاً لأن PHASE 5 (آلة الحالة) لا تزال معلّقة على D-09؛ سيُضاف تلقائياً عند تنفيذ تلك المرحلة. **تحقّق الحرّاس أنفسهم (إلزامي حسب التوثيق الأصلي — «يجب إثبات أن كل حارس يحمرّ فعلاً»):** جُرِّبت الأربعة يدوياً بإعادة إدخال كل عطل مؤقتاً (حذف استثناء `create_order` من GUARD-SQL-001، إضافة سياسة `INSERT ... TO anon` وهمية في ملف مؤقت لـGUARD-SQL-002، إضافة استدعاء `realtime.broadcast(...)` حقيقي (لا داخل تعليق) لـGUARD-SQL-003، إضافة اسم دالة وهمي لقائمة GUARD-SQL-004) — **الأربعة احمرّت فعلياً** كما هو متوقّع، ثم أُعيد كل شيء لحالته الصحيحة والمجموعة عادت خضراء بالكامل؛ لا أثر للتجربة في المستودع النهائي. **التحقّق:** 357/357 اختبار محلي (26 اختباراً جديداً للحرّاس) و`npm run build` ناجحان. **الملفات:** `src/lib/orderJourneyGuards.test.js` (جديد) · `sql/order_status_reads.sql` (جديد) · `sql/create_order_rpc.sql` (تعليق توضيحي فقط) · `sql/table_qr_system.sql` (تعليق توضيحي فقط). **الحالة:** هذه آخر مهمة يمكن تنفيذها بلا قرار مالك. **PHASE 5 متوقفة فعلياً بانتظار D-09** (المالك صرَّح أنه غير متأكد ويحتاج سؤال المطعم/الفريق) — لا مزيد من التقدّم ممكن على الخطة حتى يُحسم D-09 أو D-11 أو D-12.

- **[ADR-50] Customer Order Journey — PHASE 5: آلة الحالة مفروضة في قاعدة البيانات (D-09 = B1) (أغسطس 2026):** المالك حسم **D-09 صراحة: الخيار B1** — التراجع مسموح **لخطوة واحدة للخلف فقط، خلال 60 ثانية من آخر تحديث فعلي** على الطلب. نُفِّذ TASK-STM-002 بالكامل على الإنتاج مباشرة. **(القيد الأساسي)** `orders.status` أصبح `NOT NULL` + `CHECK (status IN ('pending','preparing','ready','completed','cancelled'))` — صفر صفوف مخالفة قبل التنفيذ (155 صفاً: 28 `cancelled` + 127 `completed` فقط، تحقّق مباشر). **(المشغّل)** `enforce_order_transition()` (`BEFORE UPDATE OF status` — لا يتأثر أي تحديث لا يمسّ `status`، مثل `toggleItemUnavailable` الذي يعدّل `items`/`subtotal`/`tax`/`total` فقط) يفرض مصفوفة §4.2 كاملة: **الانتقالات الأمامية** (`pending→preparing`، `pending→cancelled`، `preparing→ready`، `preparing→cancelled`، `ready→completed`، `ready→cancelled`) مسموحة دائماً؛ **التراجع** (`preparing→pending`، `ready→preparing`، `completed→ready`) مسموح **فقط** إن كان `now() - old.updated_at <= 60 ثانية`، وإلا `raise exception 'invalid_order_transition: ...'`؛ أي انتقال آخر (تخطي مرحلة، تحريك حالة نهائية) مرفوض فوراً بنفس رمز الخطأ — **إلغاء طلب مكتمل أصبح مستحيلاً فعلياً على مستوى القاعدة**، لا مجرد اتفاق ضمني بالواجهة. **(اكتشاف واجب الإصلاح أثناء العمل)** أثناء اختبار PHASE 5 تبيّن أن `CREATE OR REPLACE FUNCTION` بمعامل إضافي (كما فُعل مرتين في PHASE 4 لإضافة `p_idempotency_key`) **لا يستبدل** الدالة في Postgres — يُنشئ Overload جديداً بجانب القديمة لاختلاف قائمة المعاملات، فبقيت نسختان حيّتان من `create_order` (11/12 معامل) و`create_order_from_table_qr` (7/8 معاملات) منذ PHASE 4 بصمت. أُصلح فوراً بإسقاط النسختين القديمتين صراحة (`DROP FUNCTION` بالتوقيع الدقيق)، وثُبِّت التصحيح في `sql/order_idempotency.sql` نفسه (تعليق تحذيري + أوامر الإسقاط) حتى لا يتكرر عند إعادة تنفيذ الملف على قاعدة جديدة — هذا بالضبط نوع الانحراف الصامت الذي صُمِّمت الحرّاس الآلية لمنعه، ولم يكن مرتبطاً مباشرة بـD-09 لكن إصلاحه كان ضرورياً قبل المتابعة (تحقّق سلامة، لا ميزة جديدة). **(الواجهة)** `Orders.jsx`: دالة خالصة جديدة `transitionErrorMessage` تحوّل رفض `invalid_order_transition` الخادمي إلى رسالة عربية واحدة مفهومة («⚠️ لا يمكن تنفيذ هذا التغيير — إما أن الانتقال غير مسموح أو انتهت مهلة التراجع») بدل نص Postgres الخام — مُطبَّقة في `showUndo`/`advanceOrder`/`performCancel`/`toggleItemUnavailable`. توست التراجع (`showUndo`) مدّته أصبحت 60 ثانية (كانت 5) لتطابق نافذة القرار فعلياً بدل اختفاء الزرّ بعد 5 ثوانٍ فقط بينما القاعدة تسمح بـ60 — **القاعدة (المشغّل) هي الحدّ الفعلي دائماً**، حتى لو بقي التوست ظاهراً بعدها. `performCancel` لم تكن تتحقّق من نتيجة `UPDATE` إطلاقاً (فشل صامت) — أصبحت تتحقّق وتعرض الرسالة عند الرفض. **لم يُلمَس أي سلوك آخر** (لا أزرار جديدة، لا صلاحيات جديدة، لا تغيير في من يستطيع رؤية أي زرّ — طبقاً لتعليمات المالك الصريحة). **(GUARD-STM-001)** أُضيف كما وُعِد في ADR-49: حارس جديد في `orderJourneyGuards.test.js` يقارن ثابت `STATUS` في `Orders.jsx` (أزرار «تقديم الحالة») بمصفوفة `enforce_order_transition` في `sql/order_state_machine.sql` — يمنع أن يُقدَّم للمطعم زرّ يطلب خادمياً انتقالاً ممنوعاً. **التحقّق:** كل خلايا §4.2 اختُبرت فعلياً داخل معاملة `ROLLBACK` على الإنتاج (تقدّم طبيعي، رفض تخطي مرحلة، تراجع ناجح خلال النافذة، تراجع مرفوض بعد تعطيل مؤقت لمشغّل `updated_at` لمحاكاة مرور 5 دقائق ثم إعادة تفعيله، رفض إلغاء طلب مكتمل) — **كلها كما هو متوقّع**. الحرّاس الأربعة الجدد (GUARD-STM-001) جُرِّبوا يدوياً بإفساد `next` في `STATUS` (`pending→completed`) وثبت أنهم يحمرّون فعلاً، ثم أُعيد كل شيء. 361/361 اختبار محلي (30 اختباراً في ملف الحرّاس، 3 جديدة لـGUARD-STM-001) و`npm run build` ناجحان. مستشار أمان Supabase: صفر أخطاء، وتحذيرات `SECURITY DEFINER` الاعتيادية أصبحت **نصف عددها السابق** بعد إسقاط الـOverloads المكرّرة (تحسين جانبي غير مقصود). **الملفات:** `sql/order_state_machine.sql` (جديد) · `sql/order_idempotency.sql` (تصحيح توثيقي) · `src/pages/Orders.jsx` · `src/lib/orderJourneyGuards.test.js`. **الحالة:** PHASE 6 المتبقي (TASK-TRK-004) ينتظر D-12؛ PHASE 9 المتبقي (بيئة تكامل حقيقية) وPHASE 10 (E2E) ينتظران D-11؛ PHASE 7/8 تحتاجان نقاشاً صريحاً مع المالك قبل البدء (مخاطر تشغيلية حقيقية موثّقة في الوثيقة الأصلية، ليست مجرد تنفيذ آلي).

- **[ADR-51] Customer Order Journey — D-11 (بيئة اختبار التكامل): أُجِّل نهائياً بقرار المالك (أغسطس 2026):** بعد ADR-50، اقترح المالك استخدام مشروع Supabase القائم `simsim-menu-staging` (`rgqsetckcigkgsyobyjg`) كبيئة لاختبارات التكامل (`IT-ORD-*`/`RLS-*`) التي تحتاج قاعدة بيانات حقيقية. **فحص مباشر قبل أي تنفيذ كشف أنه ليس نسخة فارغة قابلة لتلقّي ملفات `sql/`:** له نظام `orders`/`create_order` **مستقل ومتباعد فعلياً** عن الإنتاج — توقيع `create_order` مختلف (`p_idempotency_key text` لا `uuid`)، ترقيم طلبات بصيغة `STG-<رقم>` عبر `sequence` منفصل تماماً عن آلية القفل الاستشاري المبنية في PHASE 4، لا وجود لجدول `restaurant_tables` (نظام QR غير مبني هناك أصلاً)، وجداول لا أثر لها في الإنتاج إطلاقاً (`customer_identities`، `customer_restaurant_memberships`، `guest_realtime_sessions`، `marketing_pages`...). كما يحمل بيانات فعلية حديثة (4 طلبات، مطعمان، آخر نشاط 17 أغسطس) — أي **مشروع نشط لمبادرة موازية** لا صلة معروفة لها بهذا التدقيق ولا معرفة بمالكها أو الفريق العامل عليه. طُبِّقت القاعدة 6 من `CLAUDE.md` (توقّف وأخبر المالك قبل أي تعديل قد يؤثر على جزء آخر من النظام) — **لم يُطبَّق أو يُعدَّل أي ملف `sql/` على هذا المشروع، ولم تُزرَع أي بيانات اختبار فيه.** بعد عرض الخيارات (مشروع Supabase جديد معزول تماماً / التنسيق مع مالك `simsim-menu-staging` / Postgres محلي في CI / تأجيل)، اختار المالك الخيار الموصى به: **إنشاء مشروع Supabase جديد معزول**. محاولة الإنشاء الفعلية (`simsim-order-journey-test`، نفس منطقة الإنتاج `ap-southeast-1`، تكلفة **0$/شهر مؤكَّدة قبل الإنشاء**) اصطدمت بحدّ حساب حقيقي: **حساب Supabase وصل الحد الأقصى للمشاريع المجانية النشطة (2)** ولا يمكن تجاوزه برمجياً — يتطلب إيقاف/حذف/ترقية أحد المشاريع الثلاثة القائمة (`simsim` الإنتاج، `madar` غير نشط منذ يوليو، `simsim-menu-staging`)، وهو **قرار مالي/تشغيلي لا يجوز اتخاذه بالنيابة عن المالك** (نفس القاعدة 6). عُرضت الخيارات مجدداً؛ **قرار المالك النهائي: تأجيل D-11 نهائياً والاكتفاء بما أُنجز.** **الأثر:** `TASK-TST-002` (بيئة تكامل حقيقية لاختبارات `IT-ORD-*`/`RLS-*`/`IT-STM-*`/`IT-NUM-*`/`IT-RT-*`) وكل سيناريوهات `TEST-E2E-001..012` في PHASE 10 **تبقى غير منفَّذة بقرار صريح، لا نسياناً** — الحرّاس الثابتة (ADR-49) والتحقّق اليدوي داخل معاملات `ROLLBACK` على الإنتاج (مُوثَّق في كل ADR من 44 إلى 50) يبقيان الدليل الوحيد المتاح على سلامة كل مرحلة، وهو أضعف من تغطية اختبار تكامل آلية مستمرة في CI. **لا تغيير في أي كود أو قاعدة بيانات في هذا القرار** — توثيق فقط. **الحالة:** D-11 مُغلَق (Ended: أُجِّل نهائياً). المتبقي من الخطة: `TASK-TRK-004` (D-12) وPHASE 7/8 (تحتاجان نقاشاً صريحاً منفصلاً مع المالك حول مخاطرهما التشغيلية الموثَّقة أصلاً في §21/§25 من الوثيقة الأصلية) — كلاهما بانتظار طلب المالك.

- **[ADR-52] Phase 3 — Payment Gateway: Moyasar (أغسطس 2026):** بقرار المالك الصريح، **Moyasar** هو بوابة الدفع الرسمية لـPhase 3. القرار مُثبَّت ومُقفَّل — لا يُعاد فتح نقاش اختيار البوابة إلا بقرار ADR جديد يُلغي هذا. **التفاصيل الكاملة:** `docs/architecture/ADR-003-PAYMENT-GATEWAY-MOYASAR.md` (Status: ACCEPTED).

  **[تحديث — التنفيذ الكامل اكتمل واندمج، أغسطس 2026]:** عبر سلسلة مهام بموافقات منفصلة لكل خطوة (Task 3.1 قفل القرار → 3.2 `MoyasarAdapter` → 3.3 ربط `paymentService` → 3.4 `payment-webhook` → 3.5 عمود/فهرس `orders.payment_transaction_id` على الإنتاج → 3.6/3.6A-3.6D تنسيق الدفع أولاً + واجهة الزبون الكاملة)، اكتمل **تكامل Moyasar الفعلي بالكامل** ودُمج في `main` عبر **PR #326** (Base=`main`، Squash Merge، Merge Commit `bbef458ed5268b0532f904b08b8c837cd8bcb17e`، مدموج 2026-08-28). **حالة كل عنصر (بأدلة مباشرة من المستودع، لا افتراض):**
  - **الدفع أولاً (Payment-First Checkout):** IMPLEMENTED — مسار كامل: `CartDrawer.jsx` (اختيار "بطاقة") ← `useCheckout.js` (`startPaymentFirstCheckout`، لقطة مجمَّدة) ← `PaymentFirstCheckoutEntry.jsx` ← `payment-first-checkout` Edge Function ← إعادة توجيه Moyasar ← `PaymentFirstCallbackLanding.jsx` ← إنشاء الطلب.
  - **التسعير الجاف (Pricing Dry-Run):** IMPLEMENTED — `sql/order_dry_run_pricing.sql` يوسّع `create_order` بمعامل `p_dry_run` يعيد استخدام نفس منطق التسعير/التحقّق الحيّ بلا تكرار.
  - **لقطة الدفع (Checkout Snapshot):** IMPLEMENTED — تُبنى في `src/payments/checkoutBinding.js` (`buildCheckoutSnapshot`) وتُخزَّن مع الدفعة عبر `sql/order_payment_reference.sql`.
  - **البصمة/الربط (Fingerprint/Binding):** IMPLEMENTED — `computeCheckoutFingerprint` (`checkoutBinding.js`) تُحسَب عند بدء الدفع وتُعاد مطابقتها إلزامياً عند إنشاء الطلب فعلياً (`src/payments/services/checkoutOrchestration.js`)؛ عدم التطابق يُرفض صراحةً (`snapshot_fingerprint_mismatch`) بلا كشف السبب للعميل.
  - **بدء الدفع (Payment Initiation) / `startCharge`:** IMPLEMENTED — Task 3.2/3.3، `paymentService` مربوط فعلياً بـ`MoyasarAdapter` (`src/payments/adapters/moyasar.js`).
  - **محوّل Moyasar (Adapter):** IMPLEMENTED — يطابق العقد المجرَّد `PaymentAdapter` من ADR-34 (`createCharge`/`verifyPayment`/`parseWebhook`/`refundPayment`/`mapStatus`)، 13 اختبار وحدة مخصّص (Task 3.2).
  - **Idempotency:** IMPLEMENTED — `payment_transactions.idempotency_key` (قيد تفرّد، `sql/payment_transactions_idempotency_key_unique.sql`، Task 3.3) + `orders.idempotency_key` (ADR-47) على نفس لقطة الدفع.
  - **معالجة الـCallback:** IMPLEMENTED — `PaymentFirstCallbackLanding.jsx` يقرأ حالة الدفع عبر `get_payment_status_by_idempotency_key`.
  - **التحقّق من الدفع:** IMPLEMENTED + STAGING VERIFIED — `sql/payment_status_reads.sql` (`get_payment_status_by_idempotency_key`)، تحقَّقت فعلياً على `simsim-menu-staging`.
  - **إنشاء الطلب:** IMPLEMENTED — `create-order-from-payment` Edge Function ← `createOrderFromSuccessfulPayment` ← `create_order` RPC (نفس الدالة الآمنة من ADR-25)، منشورة `ACTIVE` على staging (50 اختبار وحدة).
  - **تأكيد الطلب للزبون:** IMPLEMENTED — `PaymentFirstOrderCreation.jsx` (بطاقة تأكيد مرئية فعلياً؛ خلل توقيت تنقّل كان يُخفيها أُصلح أثناء التنفيذ).
  - **QR / غير-QR:** IMPLEMENTED — كلا المسارين مدعومان في لقطة الدفع (`table_qr_token` مقابل `restaurant_slug`+`branch_id`).
  - **بيانات العميل:** IMPLEMENTED — الاسم/الجوال/الملاحظات ضمن اللقطة المجمَّدة عند بدء الدفع، لا تُعاد كتابتها بعد التجميد.
  - **التوصيل / الاستلام:** IMPLEMENTED — `delivery_address` (توصيل) و`table_number` (محلي/QR) ضمن اللقطة حسب `type`.
  - **تعدّد الفروع:** IMPLEMENTED — `restaurant_slug`+`branch_id` إجباريان للمسار غير-QR، يطابق نموذج الفروع المستقلة من ADR-22.
  - **الأخطاء وإعادة المحاولة:** IMPLEMENTED — حالات `RETRYABLE_ERROR`/`FAILED`/`REQUIRES_RECONCILIATION` مع رسائل عربية موحّدة (`mapPaymentFirstRejectionReason`، `src/payments/paymentFirstErrors.js`).
  - **التنظيف (Cleanup):** IMPLEMENTED جزئياً — إلغاء العميل اليدوي (`cancelPaymentFirstCheckout()`) يُصفّر حالة الدفع فوراً في الواجهة؛ حالة `payment_expired` من Moyasar مُعرَّفة (`moyasar.js`). **لا** مهمة دورية (cron) على الخادم لتنظيف محاولات دفع متروكة/منتهية — NOT IMPLEMENTED، لم تُطلَب في نطاق هذه المرحلة.
  - **الضوابط الأمنية:** IMPLEMENTED — كل الدوال الخادمية `SECURITY DEFINER` مبوّبة؛ `payment-webhook` بـ`verify_jwt:false` (متوقَّع لِوجهة Webhook خارجية) مع تحقّق توقيع صريح داخل الدالة؛ `payment-first-checkout` بـ`verify_jwt:true`.
  - **السلطة الخادمية (Server-Side Authority):** IMPLEMENTED — السعر النهائي يُعاد احتسابه دائماً خادمياً (`p_dry_run` ثم `create_order` الفعلي)، لا يُعتمَد على أي رقم قادم من العميل.
  - **سلوك الـWebhook:** IMPLEMENTED (كود) — `payment-webhook` Edge Function (22 اختبار وحدة، Task 3.4) منشورة `ACTIVE` على staging. **التحقّق الحيّ بتوقيع Moyasar حقيقي DEFERRED** — السرّ `PAYMENT_MOYASAR_WEBHOOK_SECRET` غير مهيَّأ على staging؛ الغياب تحقَّق منه بأسلوب سلوكي آمن (طلب حقيقي يكشف فقط أي مسار كود مُنفَّذ، بلا قراءة/طباعة أي قيمة سرّ).
  - **الاختبارات:** VERIFIED — **1100/1100 ناجح** (61 ملف اختبار)، أُعيد التشغيل فعلياً (`npx vitest run`) عند كتابة هذا التحديث، صفر فشل وصفر تخطّي.
  - **تحقّق Staging:** IMPLEMENTED + PARTIALLY VERIFIED — 3 دوال Edge منشورة `ACTIVE` على `simsim-menu-staging` (`rgqsetckcigkgsyobyjg`): `create-order-from-payment`، `payment-webhook`، `payment-first-checkout`. **مشروع الإنتاج (`gpwwnuuicywsvmmhxngs`) لم يُلمَس بأي نشر من هذه السلسلة** باستثناء ترحيل `orders.payment_transaction_id`/الفهرس المرتبط (Task 3.5، مُعتمَد مسبقاً كأساس لهذا العمل). **معاملة Moyasar حقيقية كاملة DEFERRED** — الأسرار الثلاثة (`PUBLIC_APP_BASE_URL`، `PAYMENT_MOYASAR_WEBHOOK_SECRET`، `PAYMENT_MOYASAR_SECRET_KEY`) غير مُهيَّأة على staging (سبب: تهيئة/تكوين خارجي غير متاح حالياً — لم تُقرأ أو تُطبَع أي قيمة سرّ في أي مرحلة). **تنفيذ الكود والنشر ≠ التحقّق الفعلي من معاملة دفع حقيقية عبر Moyasar** — الأول مكتمل، الثاني لا يزال مؤجَّلاً بانتظار تهيئة خارجية.

  **GitHub:** PR #326 (Base=`main`)، Squash Merge، Merge Commit `bbef458ed5268b0532f904b08b8c837cd8bcb17e`. فحوصات CI الخمسة على الكوميت المدموج كلها `success`: `Build (Vite)` · `build` · `report-build-status` · `deploy` (GitHub Pages) · `Supabase Preview`.
  **النشر:** الإنتاج (مشروع Vercel `simsim`) — **SUCCESS**. `simsim-marketing-ssr-staging` — **SUCCESS**. كلاهما نتيجة الدمج التلقائي في `main` (لا نشر يدوي منفصل؛ لا مساس بمشروع Supabase الإنتاجي).
  **التوثيق التفصيلي الكامل** (لا يُكرَّر هنا): `reports/TASK_3_6D_PAYMENT_FIRST_FINAL_IMPLEMENTATION_DOCUMENTATION.md` (35 قسماً) + سلسلة تقارير `reports/TASK_3_6D_*` الفردية — هذا القسم ملخّص حالة معماري يُحيل إليها، لا بديل عنها.

- **[ADR-53] إصلاح انهيار Dashboard البصري — حظر CSP لـ`<style>` الداخلية (أغسطس 2026):** **المشكلة المُكتشفة:** Dashboard صاحب المطعم (`src/pages/Dashboard.jsx`) كانت تظهر بلا أي تنسيق بصري على الإنتاج — محتوى مدفوع لليمين بالكامل، مساحات فارغة كبيرة، بطاقات KPI غير منظمة، أزرار «معاينة المنيو»/«QR Code»/«مشاركة المنيو» بمظهر متصفح خام، تخطيط جوال مكسور. **Root Cause الفعلي (بعد تصحيح تدقيق سابق):** تدقيق أوّلي (`reports/DASHBOARD_UI_REGRESSION_AUDIT_REPORT.md`) استنتج خطأً أن الصفحة لا تملك أي CSS إطلاقاً (اعتماداً على فحص ملفات `.css` فقط، بلا فحص `<style>` مضمّنة). **الفحص الأعمق أثناء هذه المهمة كشف الحقيقة:** PR #241 (`feat: rebuild restaurant dashboard 2.0`، مدموج 2026-08-17) كتب فعلياً CSS كاملاً واحترافياً (71 class، تصميم شبكي/مرن، نقطتا استجابة `1050px`/`640px`، RTL صحيح) لكن حقنه عبر `<style>{CSS}</style>` (نص JS مضمَّن في نفس المكوّن) بدل ملف مستقل. **`vercel.json` يفرض** `Content-Security-Policy: style-src 'self' https://fonts.googleapis.com` (بلا `'unsafe-inline'`/nonce/hash — إعداد أمني متعمَّد من Phase 1، انظر `reports/PHASE_1_CSP_FIX_REPORT.md`، **لم يُمَس** في هذا الإصلاح) — وهذه السياسة **تحظر أي عنصر `<style>` بمحتوى نصّي مضمَّن** بينما تسمح بملفات CSS مُحمَّلة من نفس الأصل (`'self'`) عبر `<link>`. لذلك كانت الـ71 كلاس تُعرَّف فعلياً في الكود، لكن المتصفح **يرفض تطبيقها بصمت** على الإنتاج (Vercel يُرسل الترويسة؛ `vite dev`/`preview` المحليان لا يرسلانها، فالخلل **لا يظهر في التطوير المحلي إطلاقاً** — تفسير إضافي لعدم اكتشافه). بقية صفحات صاحب المطعم (`AppShell.jsx`, `Orders.jsx`, ...) تستخدم `style={{}}` (خاصية Inline عبر CSSOM لا عنصر `<style>`) وهي **غير خاضعة** لقيد `style-src` بنفس الطريقة، فلم تتأثر. **الإصلاح:** نُقل نص الـCSS بالكامل (بلا حذف أي قاعدة) من الثابت `CSS` داخل `Dashboard.jsx` إلى ملف حقيقي `src/pages/Dashboard.css` مستورَد عادياً (`import './Dashboard.css'`) — Vite يبنيه كملف CSS منفصل (`dist/assets/Dashboard-*.css`، مؤكَّد بعد `npm run build`) يُحمَّل عبر `<link>` من `'self'`، متوافق مع الـCSP الحالية **بلا أي تعديل على `vercel.json`**. **تحسين اتساق مصاحب:** استبدال لوني `#f26622`→`#FF6A00` و`#139a68`→`#10B981` (لتطابق `--primary`/`--success` الفعليين في `src/index.css`)، وألوان شارات الحالة (`badge.warning/info/success/neutral/danger`) بالقيم **المطابقة حرفياً** لنفس مفاتيح الحالة في `src/pages/Orders.jsx` بدل قيم مقاربة مختلفة. **صفر تغيير في المنطق/الاستعلامات/التنقّل** — الفرق الكامل في `Dashboard.jsx`: سطر `import` واحد + حذف الثابت `CSS`/وسم `<style>` + استبدال قيمتي لون، بلا لمس لأي معالج حدث أو استعلام Supabase. **التحقّق:** `npm run build` ناجح (تأكيد إصدار `dist/assets/Dashboard-*.css` منفصلاً) + **1100/1100 اختبار ناجح** (بلا تعديل أي اختبار). **Visual QA:** تعذّر Visual QA حقيقي بمتصفح (Playwright غير قابل للتشغيل على بيئة Termux/Android — `Unsupported platform: android`) وتعذّر الوصول لجلسة owner موثَّقة (لا ملف `.env`/بيانات Supabase محلية) — **AUTHENTICATED VISUAL QA BLOCKED**، موثَّقة صراحة لا مُدَّعاة كنجاح. التحقّق الفعلي المتاح: تحليل بنيوي لمخرجات البناء (تأكيد أن الملف يُحمَّل كـ`<link>` من نفس الأصل لا كنص مضمَّن) + سلوك CSP الموثَّق رسمياً (W3C CSP spec). **الملفات المعدَّلة:** `src/pages/Dashboard.jsx` (8 أسطر: +2/-6) · `src/pages/Dashboard.css` (جديد). **لم يُعدَّل:** أي جدول/دالة/RLS في Supabase، أي Edge Function، أي كود Payment-First، `vercel.json`/متغيّرات البيئة/التوجيه، أي صفحة أخرى. **تصحيح توثيقي:** أُضيفت ملاحظة تصحيح أعلى `reports/DASHBOARD_UI_REGRESSION_AUDIT_REPORT.md` نفسه (لا حذف للتقرير الأصلي) توضّح أن الـCSS كان مكتوباً فعلاً، والمشكلة في آلية التسليم لا الغياب. **Mobile/Desktop QA:** كلاهما ضمن نفس قيد "AUTHENTICATED VISUAL QA BLOCKED" أعلاه (لا فرق بين الاثنين بلا متصفح حقيقي) — الاستجابة (`1050px`/`640px`) موجودة في الـCSS المنقول ذاته بلا تعديل، فقط لم تُتحقَّق بصرياً بمتصفح فعلي. **مشاكل متبقية:** التحقّق البصري الحقيقي (Authenticated + Desktop + Mobile + RTL فعلي) يبقى مفتوحاً بانتظار بيئة متصفح/جلسة owner متاحة — Suggestion صريح للمالك. **Out of Scope مكتشف:** لا شيء جديد في نطاق هذه المهمة (فحص محصور بـDashboard.jsx/CSS فقط، لم يُفتَح أي ملف آخر بحثاً عن أخطاء).

  **Git:** فرع `fix/dashboard-ui-regression` من `main`. **Commit:** `<TBD — يُستكمَل بعد الـcommit>`. **PR:** `<TBD>`. **حالة الدمج:** لم يُدمَج بعد — بانتظار مراجعة/موافقة المالك (Build (Vite) هو الفحص الإلزامي على `main` حسب الـruleset الموثَّق سابقاً في §12).

## 4) واجهة AppShell
```jsx
<AppShell
  active="settings"                  // مفتاح الرابط المفعّل (من nav.js)
  title="⚙️ الإعدادات"                // عنوان التوب-بار
  actions={<button>...</button>}     // اختياري: عناصر يسار التوب-بار
  badges={{ orders: 3 }}             // اختياري: أرقام بجانب روابط التنقل
>
  {/* محتوى الصفحة — يدير تمريره الداخلي بنفسه */}
</AppShell>
```
- AppShell يوفّر: السايدبار الكامل، التوب-بار (زر القائمة + العنوان + actions)، التجاوب (لابتوب: سايدبار ثابت / موبايل+تابلت: منزلق)، وحالة `sidebarOpen`.
- الصفحة تركّز على محتواها فقط.

## 5) نقاط التحوّل (useBreakpoint)
`isMobile < 768` · `isTablet 768–1024` · `isDesktop ≥ 1024`. حيّة (تتحدّث مع resize).

## 5-ب) نظام صلاحيات الموظفين 🔐 (مكتمل)
- **قاعدة البيانات:** جدول `restaurant_members` (user_id, restaurant_id, username, allowed_pages, branch_scope, is_active) + دوال RLS: `is_restaurant_owner`, `has_restaurant_access`, `member_branch_scope`.
- **الدخول:** صفحة `StaffLogin` على `/staff-login/:slug` (اسم مستخدم + كلمة مرور).
- **الحماية طبقتان:** RLS يفلتر الطلبات بالفرع (`orders_access`) + الواجهة (AppShell يفلتر التنقل، App.jsx يحمي المسارات).
- **تصنيف الصفحات:**
  - خاصة بالفرع (تُفلتر بفرع الموظف عبر `branch_scope`): الطلبات، التحليلات، العملاء.
  - مشتركة (تتطلب نطاق "كل الفروع"): المنيو، الفروع، الولاء+التقييمات، QR.
  - صاحب المطعم فقط: الرئيسية، الإعدادات، الموظفون، العروض والكوبونات.

## 6) الديون التقنية (Technical Debt) — أولوية
1. **[منجز ✅] تكرار السايدبار/التوب-بار** → حُلّ بـ AppShell (كل صفحات اللوحة تستخدمه).
2. **[منجز ✅] منطق التسعير/الضريبة مكرّر** → وُحّد في `lib/pricing.js` (وأُزيلت دالة `recalc` الميتة في Orders التي كانت تخالف ADR-1 بإضافة الضريبة فوق السعر).
3. **[مُدار] الاختبارات** → 114 اختبار وحدة (مسار الفلوس (`src/lib/pricing.test.js`) + حارس بوابة المشرف الآلي (`src/lib/adminGate.test.js` — يفحص كل دوال `admin_*`، ADR-28) عبر Vitest + فحص build — الكل إلزامي في CI قبل أي دمج. لا اختبارات E2E بعد.
4. **[مُدار] تعدّد نسخ الملفات** → يُدار باتفاقية مصدر الحقيقة + هذا الملف.

## 7) حالة تطبيق AppShell (Rollout) — مكتمل ✅
| الصفحة | AppShell | تجاوب | ملاحظات |
|---|---|---|---|
| Settings | ✅ | ✅ | نموذج مرجعي |
| Dashboard | ✅ | ✅ | |
| Analytics | ✅ | ✅ | |
| Orders | ✅ | ✅ | كانبان |
| Menu | ✅ | ✅ | |
| Customers | ✅ | ✅ | |
| Branches | ✅ | ✅ | ADR-22 |
| QRCode | ✅ | ✅ | |
| Loyalty | ✅ | ✅ | |
| Staff | ✅ | ✅ | |
| Tables | ✅ | ✅ | ADR-12 |

## 8) ملفات SQL (نُفّذت في Supabase)
| الملف | الغرض |
|---|---|
| `orders_privacy_phase1.sql` | إغلاق تسريب خصوصية الطلبات (RPCs) — ADR-9 |
| `menu_colors_migration.sql` | ألوان المنيو (price_color, description_color) — ADR-7 |
| `menu_i18n_phase1.sql` | ترجمة المنيو (أعمدة `_en`) — ADR-6 |
| `staff_permissions_phase1.sql` | جدول الموظفين + RLS — ADR-8 |
| `staff_permissions_phase1b.sql` | نطاق الفرع (branch_scope) — أُعيد إحياؤه بـ`create_branches_v2.sql` — ADR-22 |
| `sql/get_restaurant_rating.sql` | تقييم المطعم للمنيو العام (متوسط + عدد فقط) — نُفِّذ عبر موصّل Supabase ✅ |
| `opening_hours_migration.sql` | أوقات العمل — ADR-4 |
| `orders_cancel_reason.sql` · `reviews_table.sql` · `loyalty_tables.sql` · `get_orders_status_rpc.sql` | ميزات سابقة |
| `sql/restaurant_tables.sql` | إدارة الطاولات — ADR-12 — نُفِّذ يدوياً في Supabase ✅ |
| `sql/product_recommendations.sql` | محرك الاقتراحات الذكي — ADR-13 — ⚠️ لم يُنفَّذ بعد، يحتاج تشغيلاً يدوياً في Supabase |
| `sql/menu_card_modularity.sql` | مرونة بطاقة هوية المطعم (5 مفاتيح إظهار/إخفاء) — ADR-14 — ⚠️ لم يُنفَّذ بعد، يحتاج تشغيلاً يدوياً في Supabase |
| `sql/remove_branches_feature.sql` | حذف جدول `branches` القديم (ADR-21) — نُفِّذ ✅، ثم عُكس بالكامل ببنية جديدة عبر ما يلي |
| `sql/create_branches_v2.sql` | إعادة بناء `branches` (نموذج عميق، `is_primary`) + `branch_id` إجباري على categories/products/orders + اختياري على banners/coupons/reviews + استعادة `branch_scope` — ADR-22 — نُفِّذ ✅ |
| `sql/branch_operational_settings.sql` | `delivery_enabled`/`delivery_fee` (وراثة اختيارية) + `takeaway_enabled` + `is_paused` على `branches` — ADR-23 — نُفِّذ ✅ |
| `sql/cart_wide_recommendations_branch_scope.sql` | `branch_id` إجباري على `cart_wide_recommendations` + قيد فريد لكل فرع — ADR-23 — نُفِّذ ✅ |
| `sql/active_orders_count_per_branch.sql` | `get_active_orders_count` تدعم `p_branch_id` اختياري — ADR-23 — نُفِّذ ✅ |
| `sql/create_order_rpc.sql` | دالة `create_order` الآمنة (`SECURITY DEFINER`) — إصلاح حادثة "الزبون لا يقدر ينشئ طلباً" — ADR-25 — نُفِّذ ✅ |
| `sql/order_status_broadcast.sql` | Realtime Broadcast لتتبّع حالة الطلب عند الزبون (قناة لكل طلب) — ADR-25 — نُفِّذ ✅ (بعد حذف سطر `alter table realtime.messages enable row level security` غير المسموح — الجدول مملوك لـSupabase) |
| `sql/restaurant_orders_broadcast.sql` | Realtime Broadcast لعدّاد الطلبات النشطة بالمنيو العام (قناة لكل مطعم) — ADR-25 — نُفِّذ ✅ |
| `sql/get_customers_summary.sql` | تجميع العملاء خادمياً (صف لكل عميل) — M1/ADR-26 — نُفِّذ ✅ |
| `sql/get_customers_insights.sql` | رؤى صفحة العملاء (أكثر منتج + اتجاه المتوسط الشهري) — M1/ADR-26 — نُفِّذ ✅ |
| `sql/get_analytics_summary.sql` | تجميع صفحة التحليلات (استدعاء واحد) — M1/ADR-26 — نُفِّذ ✅ |
| `sql/get_dashboard_summary.sql` | تجميع لوحة التحكم (الجزء التحليلي) — M1/ADR-26 — نُفِّذ ✅ |
| `sql/orders_indexes.sql` | فهرس مركّب `(restaurant_id, created_at desc)` لتسريع التقارير — M0 — نُفِّذ ✅ |
| `sql/platform_admin_foundation.sql` | أساس Super Admin: platform_admins/audit/feature_flags + دوال + RLS — ADR-27 — نُفِّذ ✅ |
| `sql/admin_list_restaurants.sql` | أول Admin Service: قائمة المطاعم (DEFINER مبوّبة) — ADR-27/M1.4؛ ثم ترقيم/بحث خادمي + عدّ لكل صفحة + إعادة `platform_suspended` — ADR-28 — نُفِّذ ✅ |
| `sql/billing_foundation.sql` | جداول الفوترة: subscriptions/invoices/payments + RLS (قراءة مشرف فقط) — ADR-29/M4.1 — نُفِّذ ✅ |
| `sql/billing_plans.sql` | جدول الباقات `plans` + ربط `subscriptions.plan_id` — ADR-29/M4.1 — نُفِّذ ✅ |
| `sql/billing_functions.sql` | 9 دوال فوترة `admin_*` (باقات/اشتراكات/فواتير، بوابة+Audit) — ADR-29/M4.1 — نُفِّذ ✅ |
| `sql/billing_owner_read.sql` | سياستا RLS قراءة فقط للمالك (subscriptions/invoices) — ADR-29/M4.2 — نُفِّذ ✅ |
| `sql/platform_roles_rbac.sql` | RBAC مرن: `platform_roles` + ربط `platform_admins.role_id` + CRUD مع حماية super_admin — ADR-30 — نُفِّذ ✅ |
| `sql/platform_events_ledger.sql` | دفتر أحداث append-only (`subscription_events`/`invoice_events`) + Triggers + backfill — ADR-31/F1.1 — نُفِّذ ✅ |
| `sql/platform_metrics_rollup.sql` | تجميع مسبق (`platform_daily_metrics`/`restaurant_stats`) + Health Score + refresh + `pg_cron` كل 10د — ADR-31/F1.2 — نُفِّذ ✅ |
| `sql/admin_dashboard.sql` | `admin_dashboard()` لوحة القرار (KPIs/سلاسل/تنبيهات/نمو) — ADR-31/F3 — نُفِّذ ✅ |
| `sql/admin_get_restaurant.sql` | Restaurant 360 (مقاييس/فروع/اشتراك/فواتير/نشاط، CTE موحّد) — ADR-28/ADR-31/F4 — نُفِّذ ✅ |
| `sql/admin_audit_viewer.sql` | `admin_list_audit_logs`+`admin_audit_actions` لعارض التدقيق — ADR-31/F5 — نُفِّذ ✅ |
| `sql/admin_growth.sql` | `admin_growth()` تحليلات MRR/Churn من دفتر الأحداث — ADR-32 — نُفِّذ ✅ |
| `sql/admin_feature_flags.sql` | 5 دوال إدارة المزايا (`manage_flags`، بوابة+Audit) — ADR-32 — نُفِّذ ✅ |
| `sql/admin_restaurant_operational.sql` | «الحالة التشغيلية»: جاهزية المنيو+الإعداد (قراءة فقط، بلا PII) — ADR-32 — نُفِّذ ✅ |
| `sql/admin_restaurant_features.sql` | المزايا الفعّالة لمطعم (effective+overridden) — ADR-32 — نُفِّذ ✅ |
| `sql/announcements.sql` | جدول الإعلانات + 3 دوال إدارة (`manage_announcements`، بوابة+Audit) — ADR-33/م1 — نُفِّذ ✅ |
| `sql/announcement_reads.sql` | حالة «مقروء» + دالتا مطعم آمنتان (تحلّان المطعم عبر `auth.uid`) — ADR-33/م2 — نُفِّذ ✅ |
| `sql/payments_gateway_foundation.sql` | أساس الدفع: providers/transactions/webhook_events (خامل، RLS إداري) — ADR-34 — نُفِّذ ✅ |
| `sql/payment_transactions_idempotency_key_unique.sql` | قيد تفرّد `idempotency_key` على `payment_transactions` — Task 3.3/ADR-52 — نُفِّذ على **staging فقط** ✅ (الإنتاج لم يُلمَس) |
| `sql/order_dry_run_pricing.sql` | توسعة `create_order` بمعامل `p_dry_run` (تسعير جاف بلا إدراج، بلا تكرار منطق) — Task 3.6/ADR-52 — نُفِّذ على **staging فقط** ✅ (الإنتاج لم يُلمَس) |
| `sql/order_payment_reference.sql` | ربط `orders`↔`payment_transactions` (لقطة الدفع + `p_payment_transaction_id`) — Task 3.6/ADR-52 — نُفِّذ على **staging فقط** ✅ (الإنتاج لم يُلمَس) |
| `sql/payment_status_reads.sql` | `get_payment_status_by_idempotency_key` — قراءة آمنة لحالة الدفع للـCallback — Task 3.6D/ADR-52 — نُفِّذ على **staging فقط** ✅ (staging-verified) |
| `sql/loyalty_tables.sql` | توثيق `loyalty_programs`+`loyalty_redemptions` (كانا في Supabase فقط) — ADR-37/خطوة 1 — منشور مسبقاً ✅ |
| `sql/reviews_table.sql` | توثيق `reviews` (مع ثغرة `reviews_insert_public` الموثّقة) — ADR-37/خطوة 1 — منشور مسبقاً ✅ |
| `sql/get_customer_loyalty.sql` | دالة رصيد الزبون — وُثّقت (خطوة 1) ثم أُعيدت لتقرأ من الدفتر — ADR-37/خطوة 6ج — نُفِّذ ✅ |
| `sql/loyalty_ledger.sql` | مخطط دفتر النقاط: accounts+transactions+Triggers+دوال داخلية+RLS — ADR-37/خطوة 2 — نُفِّذ ✅ (مع تشديد صلاحيات anon/authenticated) |
| `sql/loyalty_backfill.sql` | البذر التاريخي `loyalty_backfill()` (صافي الأصناف، idempotent) — ADR-37/خطوة 3 — نُفِّذ ✅ (57 حساب/979 نقطة، تحقّق تطابق 0 اختلاف) |
| `sql/loyalty_earn_trigger.sql` | Trigger كسب/عكس النقاط على `orders` (غير كاسر، idempotent، حاجز عكس) — ADR-37/خطوة 4 — نُفِّذ ✅ (تحقّق rollback + عدم كسر) |
| `sql/loyalty_rewards.sql` | كتالوج المكافآت `loyalty_rewards` (6 أنواع) + FK reward_id + RLS + بذر المكافأة الحالية — ADR-37/خطوة 5 — نُفِّذ ✅ (idempotent، 0 تنبيه أمني) |
| `sql/loyalty_redeem_reward.sql` | دالة `redeem_reward` (DEFINER، بوابة+تحقّق رصيد، خصم عبر الدفتر) — ADR-37/خطوة 6أ — نُفِّذ ✅ (تحقّق rollback بمحاكاة جلسة المالك) |
| `sql/loyalty_account_name.sql` | عمود `customer_name` على الحسابات (يُزامَن من Trigger الكسب) + بذرة أسماء — ADR-37/خطوة 6ب — نُفِّذ ✅ |
| `sql/submit_review.sql` | نزاهة التقييمات: دالة `submit_review` DEFINER + قيد فريد + أعمدة reply/status خاملة + إسقاط `reviews_insert_public` — ADR-37/خطوة 7 — نُفِّذ ✅ |
| `sql/loyalty_tiers.sql` | مستويات العضوية: جدول `loyalty_tiers` + بذر 5 + `tier_id` + `loyalty_compute_tier` + تحديث Triggers (مستوى+مضاعف) — ADR-38/P2.1أ — نُفِّذ ✅ |
| `sql/loyalty_recompute_tiers.sql` | إعادة تصنيف المستويات بعد تعديل/حذف مستوى (DEFINER، بوابة) — ADR-38/P2.1ب — نُفِّذ ✅ |
| `sql/get_customer_loyalty.sql` | (محدّث) يُرجع المستوى الحالي+التالي لكرت الزبون — ADR-38/P2.1ج — نُفِّذ ✅ |
| `sql/get_loyalty_dashboard.sql` | تجميع لوحة تحكم الولاء (jsonb، INVOKER) — ADR-38/ب — نُفِّذ ✅ |
| `sql/get_reviews_summary.sql` | تجميع إحصائيات التقييمات (توزيع/شكاوى/حالات/اتجاه، jsonb، INVOKER) — ADR-38/ج — نُفِّذ ✅ |
| `sql/loyalty_earning_rules.sql` | قواعد الكسب: أعمدة (حد أدنى/فروع/مستثناة/حملة) + Trigger الكسب المحدّث — ADR-38/هـ-1 — نُفِّذ ✅ |
| `sql/loyalty_reward_limits.sql` | حدود المكافآت في `redeem_reward` (per_customer_limit+period) — ADR-38/هـ-2 — نُفِّذ ✅ |
| `sql/loyalty_expiry.sql` | صلاحية النقاط: عمود+trigger+`loyalty_expire_points`+`get_customer_loyalty`(expiry_months)+pg_cron — ADR-38/و — نُفِّذ ✅ |
| `sql/get_advanced_analytics.sql` | تحليلات متقدمة: `complaint_category` + دالة (تأثير الولاء/رضا/عودة/أداء الفروع/أسباب الشكاوى) — ADR-39/P3.1 — نُفِّذ ✅ |
| `sql/loyalty_campaigns.sql` | الحملات الموسمية: جدول+ترحيل+Trigger (الأعلى يفوز، ختم campaign_id)+`get_campaigns_impact`+حذف الأعمدة القديمة — ADR-39/P3.3 — نُفِّذ ✅ |
| `sql/order_journey_hotfix.sql` | PHASE 1 من Customer Order Journey: إصلاح `create_order`(B1)+`broadcast_order_status`(B2)+إسقاط `orders_cancel_public`/`orders_insert_public`+REVOKE من `anon`(B3) — ADR-44 — نُفِّذ ✅ |
| `sql/order_idempotency.sql` | PHASE 4/TASK-ORD-002: عمود `idempotency_key`+فهرس فريد جزئي+معامل جديد في `create_order`/`create_order_from_table_qr` — ADR-47 — نُفِّذ ✅ |
| `sql/order_number_atomic.sql` | PHASE 4/TASK-ORD-003: ترقيم ذرّي بقفل استشاري بدل `COUNT(*)+1`+قيد تفرّد `(restaurant_id, order_number)` — ADR-47 — نُفِّذ ✅ |
| `sql/order_status_reads.sql` | توثيق (لا تنفيذ جديد): `get_orders_status_secure`/`cancel_order_by_customer`/`can_read_order_status`/`get_orders_status` كانت حيّة بلا ملف مقابل — ADR-49 — منشورة مسبقاً ✅ |
| `sql/order_state_machine.sql` | PHASE 5/TASK-STM-002: `CHECK`+`NOT NULL` على `orders.status` + مشغّل `enforce_order_transition` (مصفوفة §4.2 كاملة + نافذة تراجع 60 ثانية — D-09=B1) — ADR-50 — نُفِّذ ✅ |

## 9) حقائق تشغيلية 🔑
- **وصول قاعدة البيانات:** موصّل Supabase الرسمي (MCP) مفعّل لدى الفريق التقني — SQL يُنفَّذ مباشرة **بعد موافقة المالك على كل نص**. ملفات SQL الجديدة تُحفظ في `sql/` بالمستودع للتوثيق.
- **المفتاح العام** `sb_publishable_` (anon) آمن للعلن — الحماية الحقيقية عبر RLS.
- **تأكيد الإيميل معطّل** في Supabase — ضروري لدخول الموظفين بالإيميل الوهمي (ADR-8). لا تفعّله.
- **الكاش:** اختبر التحديثات بنافذة خفية (Incognito) لتفادي JavaScript قديم.
- **الرفع الناقص خطر:** التعريف والاستدعاء يصلان معاً — يُفضّل رفع الملف كاملاً عند العمل من الجوال.
- **Supabase Auth Rate Limits (تحقق مباشر 2026-08-22 — R-4 مغلق):**
  | الإعداد | القيمة المُفعَّلة |
  |---------|-----------------|
  | Token refreshes | 150 طلب / 5 دقائق |
  | Token verifications (OTP / Magic Link) | 30 طلب / 5 دقائق |
  | Anonymous users | 30 طلب / ساعة |
  | Sign-ups and sign-ins | 30 طلب / 5 دقائق |
  | Web3 sign-ups and sign-ins | 30 طلب / 5 دقائق |
  | IP Address Forwarding | **معطّل (OFF) — لا تُفعِّله** |
  - القيم لم تُعدَّل — مُثبَّتة كما هي في Dashboard. تقرير التحقق: `reports/TASK_1_4_RATE_LIMITING_VERIFICATION_REPORT.md`

## 10) خريطة الطريق
| البند | الحالة / الأولوية |
|---|---|
| 🎯 لوحة Super Admin (تحكّم مركزي بكل المطاعم مع حفظ خصوصيتها) | ✅ الأساس + Phase 2 منجزة (ADR-27→33، PR #176–#182) |
| توحيد نظام تصميم Super Admin + إتاحة الوصول (تُوكِنز/مكوّنات مشتركة/جداول متجاوبة/Skeleton/أيقونات موحّدة) — ADR-41 | ✅ منجزة (PR #193) — مؤشر «آخر تحديث» خارج Overview يحتاج تعديل SQL منفصل |
| طبقة توسّع المنصّة F1–F5 (دفتر أحداث/تجميع/لوحة قرار/Restaurant 360/تدقيق) — ADR-31 | ✅ منجزة |
| Phase 2 أدوات المشرف: المزايا · النمو · «عرض كمطعم» · بحث ⌘K — ADR-32 | ✅ منجزة (PR #177–#180) |
| الإعلانات (منصّة + جرس المطعم) — ADR-33 | ✅ منجزة (PR #181–#182) |
| أساس نظام الدفع (خامل، محايد للمزوّد) — ADR-34 | ✅ منجز (PR #183) — ربط مزوّد فعلي أُنجز لاحقاً، انظر الصف التالي |
| **Phase 3 — الدفع الإلكتروني الفعلي (Payment-First Checkout, Moyasar)** — ADR-52 | ✅ **مُنجزة ومدموجة**: التنفيذ الكامل + 1100/1100 اختبار + دمج `main` (PR #326، Squash، `bbef458`) + نشر إنتاج/staging **SUCCESS** + توثيق. ⏳ **مؤجَّل:** التحقّق الحيّ بمعاملة Moyasar حقيقية (تهيئة أسرار خارجية غير متاحة حالياً على staging؛ الإنتاج لم يُلمَس) |
| طبقة التكامل (البوابة الموحّدة، خاملة) — ADR-35 | ✅ منجز (PR #184) — ربط مزوّدين فعليين لاحقاً |
| إعادة التنظيم لطبقات احترافية (LAYERS/Observability/Config/Data-Access) — ADR-36 | ✅ منجز (PR #186–#189) — ترحيل بقية صفحات المطعم تدريجياً |
| خطة أساس المينيو: فصل الحزم (~137KB بدل 783) · 22 اختبار فلوس في CI · تفكيك PublicMenu (1,974←~290) · Skeleton + صور كسولة · إضافة سريعة · اقتراحات السلة | ✅ منجزة 6/6 |
| الهندسة البصرية الجديدة للمينيو (ADR-10): هيرو/ورقة/تبويبات نصية/☰/ولاء/تقييم ★ + جولات ضبط المالك | ✅ منجزة |
| قائمتا الصدارة (يعجب زبائننا + الأكثر طلباً) + إعادة تصميم «طلباتي» مرحلة 1 — ADR-11 | ✅ منجزة (PR #69–#76) |
| مرحلة 2 لطلباتي: سجلّ كامل + إنفاق مدى الحياة + مستوى عضوية + فاتورة PDF | مؤجّلة (تحتاج بنية حساب عميل) |
| إدارة الطاولات + Dropdown في السلة — ADR-12 | ✅ منجزة (PR #89–#90) |
| محرك الاقتراحات الذكي + إعادة تصميم السلة — ADR-13 | ✅ منجزة (PR #96–#97) — ⚠️ ينتظر تنفيذ SQL |
| مرونة بطاقة هوية المطعم (Modularity) — ADR-14 | ✅ منجزة (PR #107) — ⚠️ ينتظر تنفيذ SQL |
| Sticky Header بأسلوب Morph Animation لبطاقة الهوية — ADR-15 | ✅ منجزة (PR #109) |
| رحلة طلب الزبون E2E — PHASE 1 (إصلاح B1/B2/B3) — ADR-44 | ✅ منجزة (إنتاج) |
| رحلة طلب الزبون E2E — PHASE 2 (عزل السلة بالفرع) — ADR-45 | ✅ منجزة |
| رحلة طلب الزبون E2E — PHASE 3 (الدفع/Checkout) — ADR-46 | ✅ منجزة |
| رحلة طلب الزبون E2E — PHASE 4 (Idempotency + ترقيم ذرّي) — ADR-47 | ✅ منجزة (إنتاج) |
| رحلة طلب الزبون E2E — PHASE 6 جزئي (تتبّع لحظي + قراءة آمنة) — ADR-48 | ✅ منجزة — TASK-TRK-004 ينتظر D-12 |
| رحلة طلب الزبون E2E — PHASE 9 جزئي (الحرّاس الثابتة) — ADR-49 | ✅ منجزة |
| رحلة طلب الزبون E2E — PHASE 5 (آلة الحالة، D-09=B1) — ADR-50 | ✅ منجزة (إنتاج) |
| رحلة طلب الزبون E2E — PHASE 6 المتبقي (TASK-TRK-004) / PHASE 7-8 | ⏳ D-12 / نقاش مخاطر مباشر مع المالك |
| رحلة طلب الزبون E2E — PHASE 9-10 (بيئة تكامل + E2E حقيقي) — ADR-51 | ⛔ أُجِّلت نهائياً بقرار المالك (D-11) — راجع ADR-51 |
| 🔐 إخراج `.env` من تتبّع Git ونقل القيم لإعدادات Vercel | Suggestion مفتوح (أمان) |
| ترجمة الأصناف/الأقسام المتبقية (إدخال من صاحب المطعم) | مستمرة |
| 🔮 تطبيق التوصيل (سِمسِم كجهة توصيل للمطاعم المفعّلة للخيار) | مستقبلية |
| توحيد التسعير `lib/pricing.js` | ✅ منجز (PR #49) |
| إضافة `package-lock.json` + `.gitignore` | ✅ منجز (PR #49) |

## 11) الرؤية (كما حدّدها المؤسس)
1. **صاحب المطعم:** يسجّل، يدير مطعمه، QR، طلبات، موظفون. ✅
2. **صاحب سِمسِم:** تحكّم مركزي بكل المطاعم مع حفظ خصوصيتها. 🎯 التالي
3. **تطبيق توصيل:** سِمسِم جهة التوصيل للمطاعم التي تفعّل الخيار. 🔮
