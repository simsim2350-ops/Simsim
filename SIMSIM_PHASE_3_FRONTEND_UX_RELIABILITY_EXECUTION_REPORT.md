# SIMSIM — Phase 3: Global Frontend UX Reliability & Interaction Hardening

**النطاق:** موثوقية التفاعل الأمامي عبر `menu-next` (منيو العميل) و`src/` (لوحة تحكم المطعم/المنصّة) — إعادة تركيب/remount، فقدان تركيز، مفاتيح React، النماذج، لوحة المفاتيح على الجوال، المودال/الدرج، اللمس/التمرير، غير المتزامن، Realtime، الاستجابة/RTL، الأداء ذو الصلة فقط.
**المرجع:** قُرئت `SIMSIM_GLOBAL_QA_DISCOVERY_REPORT.md`، `SIMSIM_PHASE_1_SECURITY_EXECUTION_REPORT.md`، `SIMSIM_PHASE_2_ORDER_INTEGRITY_EXECUTION_REPORT.md` كسياق تاريخي — **لم يُفترَض أنها تعكس الكود الحالي بدقة، وقد كُشف بالفعل عن انحراف حقيقي (القسم 3، ملاحظة حرجة).**

---

## 1. Executive Summary

**السبب الجذري الوحيد المؤكَّد والمُصلَح في هذه المرحلة**: مكوّنات React مُعرَّفة داخل جسم دالة الرسم للمكوّن الأب (`const XCard = (...) => {...}` بدل تعريف مستقل) — تُجبر React على معاملتها كنوع مكوّن جديد كل مرة يُعاد فيها رسم الأب، فيُفكَّك DOM القديم ويُركَّب من جديد بالكامل. وُجد هذا النمط في **7 مواضع في `src/` (لوحة التحكم)، صفر في `menu-next`**. من بين هذه السبعة، **موضع واحد فقط له أثر وظيفي حقيقي ومؤكَّد**: `OrderCard` في `src/pages/Orders.jsx` — يتتبع إيماءة سحب لمسية عبر `useRef`، بينما الأب يُعاد رسمه كل 20 ثانية (مؤقّت) وعند كل حدث Realtime، ما يعني أن إعادة التركيب في منتصف إيماءة سحب فعلية تُصفِّر حالة السحب وقد تُسقطها بصمت — **هذا أُصلح** (رفع المكوّن لمستوى الوحدة، تحويل كل الإغلاقات لـ props صريحة).

**المواضع الستة الأخرى** (`Tables.jsx`, `Branches.jsx`, `Loyalty.jsx`, `Analytics.jsx`×6, `Onboarding.jsx`, `AdminShell.jsx`) **فُحصت واحدًا واحدًا ولا تحتوي أي `useState`/`useRef`/حقل إدخال داخلها**، وآباؤها لا يُعاد رسمهم بمؤقّت/Realtime (تفاعل مستخدم فقط) — **لا خطر فقدان حالة أو تركيز فيها، فقط إعادة تركيب مُهدِرة بصريًا**. طُبِّقت قاعدة التعليمات صراحةً ("لا تُصلح كل إعادة رسم") فلم تُلمَس، وهذا موثَّق كخيار واعٍ، لا إغفال.

**اكتشاف مهم غير متوقَّع**: الفرع المحلي الذي يعمل عليه هذا المستودع (`test/deployment-update-visual-smoke-test`) **متأخر 23 commit عن `origin/main`** — من بينها بالضبط commit إصلاح التمرير/اللمس العام الذي جرى التحقق منه على الإنتاج مسبقًا في هذه الجلسة نفسها (`3f75fc4 fix(menu-next): lock body scroll on every overlay, contain scroll chaining`). **هذا ليس تراجعًا في الإنتاج** (مؤكَّد: `origin/main` يحتوي الإصلاح، والإنتاج يُبنى من `main`) — لكنه يعني أن هذا الفحص المحلي وحده كان سيُظهر نتيجة "الإصلاح مفقود" لو اعتمدتُ على الفرع المحلي فقط دون التحقق من `origin/main` — تفصيل هذا في القسم 11. **لم يُلمَس هذا الملف — ليس اكتشافًا يخصّ هذه المرحلة إصلاحه.**

**التحقق من الانحدار**: `npx vite build` نجح بلا أخطاء، و**كامل مجموعة الاختبارات (64 ملفًا، 1201 اختبار) نجحت بالكامل** بعد التعديل.

---

## 2. Scope

كما حدَّد المالك: `menu-next` بالكامل (منيو، سلة، دفع، تتبع طلب، تقييمات) + `src/` (لوحة التحكم: نماذج، مودالات، أدراج، بحث، فلاتر، تبويبات، تنقّل جوال، تخطيطات متجاوبة). **لم يُعَد تصميم أي هوية بصرية أو منتج، لم يتغيّر أي منطق عمل** خارج السبب الجذري المُصلَح.

---

## 3. Frontend Architecture Map

```
menu-next (Next.js 16 App Router)
├── حالة عالمية: CartContext + BannerContext فقط (useCallback/useMemo مُطبَّقة بثبات)
├── لا مكتبة نماذج (react-hook-form/formik) — كل نموذج useState يدوي
├── useBodyScrollLock (hook) — قفل تمرير الخلفية عند فتح أي overlay
├── CSS: overscroll-behavior: contain على كل حاويات overlay الداخلية (origin/main)
└── 0 حالة لمكوّن مُعرَّف داخل render (تأكيد مباشر عبر grep شامل)

src/ (Vite/React، Zustand، React.StrictMode مُفعَّل)
├── AppShell (13 صفحة تستورده) — تخطيط مشترك
├── Orders.jsx — لوحة كانبان حيّة (Realtime + مؤقّت 20 ثانية)
├── 7 مواضع "مكوّن داخل render" — القسم 5
└── لا Context للحالة — Zustand فقط (authStore)
```

---

## 4. Interaction Architecture

- **النماذج**: كل حقول الإدخال (`CustomerInfoForm.tsx`, `Login.jsx`, نماذج `src/pages/*`) مُتحكَّم بها (controlled) بحالة top-level، لا مكوّنات فرعية مُعرَّفة حول أي حقل نصي.
- **المودال/الدرج**: `useBodyScrollLock` + `overscroll-behavior: contain` (طبقتان — JS وCSS).
- **Realtime**: `postgres_changes` (Dashboard) + `realtime.send` مخصص (العميل)، كلاهما يستبدل الحالة الكاملة (`payload.new`) وليس تراكميًا — لا خطر بيانات قديمة تُراكَم فوق أحدث.
- **غير المتزامن**: `submittingRef` (useRef، ليس state) يحرس كل عملية إرسال حساسة — مؤكَّد Phase 2، أُعيد التحقق من وجوده في `CheckoutForm.tsx` هذه المرحلة.

---

## 5. Render / Remount Audit

بحث شامل (`grep`) عن نمط `const [A-Z]... = (...) =>` أو `function [A-Z]...` **داخل** جسم أي مكوّن، عبر **كل** ملفات `src/**/*.jsx` و`menu-next/**/*.tsx`:

| # | الملف | المكوّن الداخلي | يحوي useState/useRef/input؟ | تكرار رسم الأب |
|---|---|---|---|---|
| 1 | `src/pages/Orders.jsx` | `OrderCard` | **نعم (`useRef` لتتبع اللمس)** | **مؤقّت 20 ثانية + Realtime — متكرر جدًا** |
| 2 | `src/pages/Tables.jsx` | `TableCard`, `ActionButton` | لا | تفاعل مستخدم فقط |
| 3 | `src/pages/Branches.jsx` | `BranchCard` | لا | تفاعل مستخدم فقط |
| 4 | `src/pages/Loyalty.jsx` | `TierBadge` | لا | تفاعل مستخدم فقط |
| 5 | `src/pages/Analytics.jsx` | 6 مكوّنات (`Icon`,`Card`,`CardHead`,`Empty`,`ErrorState`,`Skeleton`,`GrowthBadge`) | لا لأي منها | تفاعل مستخدم فقط |
| 6 | `src/pages/Onboarding.jsx` | `Progress` | لا | تفاعل مستخدم فقط |
| 7 | `src/admin/AdminShell.jsx` | `NavList`, `Sidebar` | لا | تفاعل مستخدم فقط |
| — | `menu-next/**` | **لا يوجد أي موضع** | — | — |

**التصنيف**: #1 = **CONFIRMED BUG (P1) → FIXED**. #2–#7 = **CONFIRMED UX RELIABILITY RISK (P3، نفس النمط، أثر بصري بحت)** — لم تُصلَح عمدًا (القسم 21).

---

## 6. Input Focus Audit

اختبار الفحص المطلوب طُبِّق على كل نموذج إدخال حقيقي في المستودع (وليس فقط عيّنة):

| النموذج | مُتحكَّم به؟ | مكوّن داخلي حوله؟ | مفتاح ديناميكي؟ | النتيجة |
|---|---|---|---|---|
| `Login.jsx` (email/password) | ✅ | لا | لا | **VERIFIED — لا خطر** |
| `menu-next/components/checkout/CustomerInfoForm.tsx` | ✅ | لا | لا | **VERIFIED — لا خطر** |
| `CheckoutForm.tsx` (الحقل الرئيسي) | ✅ | لا | لا | **VERIFIED — لا خطر** |
| `src/pages/Tables.jsx` حقل البحث | ✅، **في نفس نطاق `Orders()`/`Tables()` الذي يحوي مكوّنًا داخليًا** | البحث نفسه ليس داخل `TableCard` | لا | **VERIFIED — التركيز لا يُفقَد** (حقل البحث خارج شجرة `TableCard` المُعاد تركيبها، فلا يتأثر تركيزه هو نفسه — لكن الكروت تحته تومض/تُعاد تركيبها بصريًا عند كل حرف، القسم 5 #2) |
| `Orders.jsx` حقلا `th.warn`/`th.late` (رقمي) | ✅ | **لم يكونا** داخل `OrderCard` أصلًا (في جزء آخر من الصفحة) | لا | **VERIFIED — غير متأثرين** |
| `MarketingSettings.jsx`/`marketingSectionRegistry.jsx` (روابط ديناميكية) | ✅ | لا (لكن `key={index}`، القسم 8) | **جزئيًا — القسم 8** | راجع القسم 8 |

**لا نمط "فقدان تركيز عند الكتابة" كلاسيكي وُجد في أي حقل نصي حقيقي في المستودع.** المشكلة الوحيدة المؤكَّدة (`OrderCard`) كانت في عنصر يتتبع **لمسًا**، وليس حقل نص.

---

## 7. State / Rerender Audit

- `CartContext`/`BannerContext` (menu-next): `useCallback` لكل الإجراءات، `useMemo` للقيم المشتقة (`subtotal`, `count`) — **VERIFIED**، لا خطر cascade.
- `Orders.jsx`: بعد الإصلاح، `OrderCard` يُعاد رسمه (وليس تركيبه) بشكل طبيعي فقط عند تغيّر props (`order`, `now`, `th`) — سلوك React قياسي وصحيح.
- لم تُوجَد حلقة مزامنة حالة (state sync loop) أو effect يُحدِّث state يُطلق نفس الـ effect مجددًا في أي من الملفات المفحوصة.

---

## 8. Form Audit

- **كل النماذج مُتحكَّم بها بالكامل**، لا حقول uncontrolled، لا تحقق يتسبب في إعادة تركيب.
- **نمط مفتاح React إشكالي حقيقي وُجد**: `key={index}` على صفوف `<input>` **قابلة للحذف/الإضافة** في `src/admin/features/marketing/MarketingSettings.jsx:28` و`marketingSectionRegistry.jsx:58,74` (أدوات تحرير CMS للمنصّة، إدارة فقط). حذف عنصر من المنتصف بينما عنصر آخر بعده مُركَّز عليه قد يُسبب سلوكًا غير متوقَّع في أي حالة محلية مرتبطة بالعقدة (نادر عمليًا هنا لأن الحقول مُتحكَّم بها بالكامل، فالبيانات نفسها لا تُخطئ، لكن موضع التركيز قد ينتقل بشكل غير بديهي). **CONFIRMED UX RELIABILITY RISK (P3)** — سطح إداري منخفض الاستخدام، الإصلاح الصحيح (معرّف ثابت لكل عنصر) يتطلب تعديل شكل البيانات، **لم يُنفَّذ في هذه المرحلة** (خارج الحد الأدنى الآمن).
- كل المفاتيح الأخرى المركَّبة (`` `${id}-${i}` ``) في قوائم غير قابلة لإعادة الترتيب أو بلا حقول إدخال — **VERIFIED، لا خطر**.

---

## 9. Mobile Keyboard Audit

- `env(safe-area-inset-bottom)` مطبَّق بثبات (11+ موضعًا، مؤكَّد Discovery، لم يتغيّر).
- لا استخدام لـ `100vh` الثابت وُجد في مسارات checkout/modal الحرجة (بحث سريع لم يُظهر نمطًا خطرًا).
- **لم يُجرَ اختبار فعلي على جهاز/محاكي جوال حقيقي بلوحة مفاتيح مفتوحة في هذه الجلسة** — التقييم مبني على مراجعة الكود/CSS فقط.
- **التصنيف: UNKNOWN — REQUIRES FURTHER TESTING** (لجزئية السلوك الفعلي للوحة المفاتيح تحديدًا فقط؛ بنية الـ CSS نفسها VERIFIED سليمة).

---

## 10. Modal / Drawer Audit

- `useBodyScrollLock` (menu-next) يُستدعى من `CategoryNav.tsx` (الدرج الرئيسي)؛ باقي الـ overlays (سلة، خيارات منتج، حساسية، بحث، عروض) تعتمد على `overscroll-behavior: contain` + بنية CSS خاصة بكل overlay — **مزيج مقصود، وليس تناقضًا** (مؤكَّد أنه اجتاز اختبار إنتاج مباشر سابق في هذه الجلسة نفسها لكل الأنواع الستة).
- لا "مودال داخل مودال" حقيقي وُجد في المسارات المفحوصة.
- **التصنيف: VERIFIED** (بالاستناد لأدلة كود + تحقق إنتاجي سابق موثَّق، وليس إعادة اختبار من الصفر هذه المرحلة — القسم 11).

---

## 11. Scroll / Touch Audit

**هذا القسم يحتوي أهم ملاحظة إجرائية في هذا التقرير.**

فحص أولي لملف `menu-next/app/globals.css` في هذا الفرع المحلي أظهر **صفر** نتيجة لـ `overscroll-behavior` — نتيجة مثيرة للقلق ظاهريًا تتعارض تمامًا مع ما وثَّقه تقرير Discovery. تحقيق مباشر كشف السبب الحقيقي:

```
git rev-list --count HEAD..origin/main   → 23
git log HEAD..origin/main -- menu-next/app/globals.css
  → 3f75fc4 fix(menu-next): lock body scroll on every overlay, contain scroll chaining
    (ضمن PR #414، مدموج في origin/main)
```

**الفرع المحلي الحالي متأخر 23 commit عن `origin/main`، ومن ضمنها بالضبط commit إصلاح التمرير/اللمس الذي تحقّقتُ منه على الإنتاج الحي مسبقًا في هذه الجلسة (6 أنواع overlay، كلها PASS، بلا أي تسرّب تمرير للخلفية).** الإنتاج يُبنى من `origin/main` (مؤكَّد Phase 1)، لذا **الإصلاح حاضر وفعّال في الإنتاج الحقيقي اليوم** — هذا **ليس** تراجعًا، بل فجوة في مزامنة نسخة العمل المحلية فقط.

**لماذا هذا مهم رغم ذلك**: التحقق من `src/pages/Orders.jsx` (الملف الذي عدَّلته في هذه المرحلة) أظهر أنه **غير متأثر** بهذه الفجوة (`git log HEAD..origin/main -- src/pages/Orders.jsx` أعاد نتيجة فارغة) — لذا إصلاح Phase 3 مبني على نفس المحتوى الموجود في `origin/main` لهذا الملف تحديدًا، **آمن**. لكن ملفين من مجموعة الـ7 التي حدَّدتها في القسم 5 (`Branches.jsx`, `Onboarding.jsx`) **يختلفان بالفعل عن `origin/main`** — حسنًا أنني قررتُ عدم لمسهما أصلًا (القسم 5)، لكن هذا يعني أن أي عمل مستقبلي على هذا الفرع المحلي يجب أن يبدأ بمزامنته مع `origin/main` أولًا لتفادي نتائج فحص مضلِّلة أو تعارضات دمج لاحقة.

**التصنيف**:
- سلوك التمرير/اللمس نفسه في الإنتاج: **VERIFIED** (دليل من هذه الجلسة سابقًا + تأكيد وجود الإصلاح في `origin/main`).
- حالة الفرع المحلي: **ملاحظة إجرائية، ليست UX bug** — موصى بمزامنة الفرع (القسم 21، 23).

---

## 12. Async / Network Interaction Audit

- `submittingRef` (ref، وليس state) يحرس كل نقطة إرسال حساسة في `CheckoutForm.tsx` — مؤكَّد Phase 2، أُعيد التحقق من وجوده حيًّا في الكود الحالي هذه المرحلة (لم يتغيّر).
- لا نمط طلب مكرَّر أو سباق تحديث state بعد فك تركيب المكوّن (`unmounted component update`) وُجد في المسارات المفحوصة.
- **VERIFIED** — يعتمد على حماية Phase 2 الخادمية (idempotency) كخط دفاع نهائي بغض النظر عن سلوك الواجهة.

---

## 13. Realtime UI Audit

- **`src/pages/Orders.jsx` هو السطح الأهم (كما طلبت التعليمات صراحةً) — وهو الملف الذي أُصلح جذريًا هذه المرحلة.**
- كل معالج حدث Realtime يستبدل الحالة الكاملة للطلب (`payload.new`)، لا يُراكِم دلتا جزئية — **لا خطر بيانات قديمة تكتب فوق أحدث**.
- **قبل الإصلاح**: أي حدث Realtime كان يُعيد رسم `Orders()`، ما يُعيد تركيب كل `OrderCard` المرئية — لو كان المستخدم يسحب كرتًا لحظة وصول الحدث، تُفقَد حالة السحب. **بعد الإصلاح**: `OrderCard` مكوّن مستقر الهوية، يُحدَّث بالـ props العادية فقط، لا إعادة تركيب.
- لا فلتر/تحديد/تمرير يُعاد تصفيره عند وصول حدث Realtime في أي مكوّن آخر فُحص.

---

## 14. Responsive / RTL Audit

- `dir="rtl"` على `<html>` (menu-next)، `direction:'rtl'` صريح في تخطيطات `src/admin/AdminShell.jsx` وغيرها — **VERIFIED**.
- لا مشكلة نص عربي/طول ديناميكي مكسور وُجدت في المسارات المفحوصة (استخدام `overflow:hidden; textOverflow:ellipsis; whiteSpace:nowrap` متّسق عبر الكروت).
- **لم يُجرَ اختبار بصري فعلي على شاشات متعددة الأحجام في هذه الجلسة** — التقييم مبني على مراجعة الكود.

---

## 15. Performance Findings

- **لا مشكلة أداء جديدة أُدخلت** — التحقق: حجم `dist/assets/Orders-*.js` بعد الإصلاح (43.85 kB) ضمن النطاق الطبيعي لملف بحجمه، البناء نجح بلا تحذيرات حجم غير معتادة.
- الإصلاح في القسم 5/13 **يُحسِّن** الأداء كأثر جانبي إيجابي (تقليل إعادة تركيب DOM غير الضرورية لكل الكروت المرئية عند كل نبضة مؤقّت/حدث)، لكن هذا لم يكن الهدف الأساسي ولم يُقَس رقميًا (خارج نطاق "الأداء فقط حيث له صلة").
- لا قوائم كبيرة غير مُحسَّنة أو استعلامات Supabase متكررة جديدة وُجدت أثناء هذا الفحص.

---

## 16. Root Cause Analysis

```
السبب الجذري:
  مكوّنات React مُعرَّفة داخل جسم دالة رسم المكوّن الأب
  (بدل التعريف على مستوى الوحدة/الملف)

  → هوية المكوّن (type) تتغيّر في كل إعادة رسم للأب
  → React يُفكِّك التركيب القديم بالكامل ويُركِّب من جديد
  → أي حالة/مرجع (useState/useRef) داخل ذلك المكوّن تُصفَّر

الأعراض المُلاحَظة (مُجمَّعة حسب هذا السبب الواحد):
  • Orders.jsx/OrderCard: احتمال إسقاط إيماءة سحب لمسية جارية
    (تكرار الرسم: مؤقّت 20 ثانية + Realtime — عالي التكرار)  → P1، مُصلَح
  • Tables.jsx/Branches.jsx/Loyalty.jsx/Analytics.jsx×6/
    Onboarding.jsx/AdminShell.jsx: وميض/إعادة تركيب بصرية بحتة
    عند أي تفاعل مستخدم يُعيد رسم الصفحة الأب (لا حالة داخلية لتُفقَد)
    (تكرار الرسم: تفاعل مستخدم فقط — منخفض)  → P3، موثَّق فقط

لماذا سبب واحد فقط عولج فعليًا: التعليمات صريحة — "أصلح فقط ما يُنتج
سلوكًا خاطئًا أو فقدان حالة/تركيز أو عدم استقرار تفاعل أو تدهور أداء
حقيقي." الموضع الوحيد المستوفي لهذا المعيار بدليل مباشر هو OrderCard.
```

---

## 17. Findings Register

| ID | Classification | Severity | Area | Root Cause | Evidence | Status | Fix |
|---|---|---|---|---|---|---|---|
| FE-1 | CONFIRMED BUG | **P1** | `src/pages/Orders.jsx` — `OrderCard` | مكوّن مُعرَّف داخل render، أب يُعاد رسمه كل 20 ثانية + Realtime | `useRef` لتتبع لمس + موضع الإعادة الرسم — تحليل كود مباشر | ✅ **FIXED** | رفع `OrderCard` لمستوى الوحدة، props صريحة بدل إغلاقات |
| FE-2 | CONFIRMED UX RELIABILITY RISK | P3 | 6 ملفات أخرى في `src/` (القسم 5) | نفس السبب الجذري | لا `useState`/`useRef`/input داخلها، أب يُعاد رسمه بتفاعل مستخدم فقط | موثَّق، **لم يُصلَح** | لا حاجة — لا خطر حالة/تركيز مؤكَّد |
| FE-3 | CONFIRMED UX RELIABILITY RISK | P3 | `MarketingSettings.jsx`, `marketingSectionRegistry.jsx` | `key={index}` على صفوف إدخال قابلة للحذف/الإضافة | قراءة كود مباشرة | موثَّق، **لم يُصلَح** | يتطلب معرّف ثابت لكل عنصر (تعديل شكل بيانات) |
| FE-4 | NOT A BUG (ملاحظة إجرائية) | — | الفرع المحلي `test/deployment-update-visual-smoke-test` | متأخر 23 commit عن `origin/main` | `git rev-list --count HEAD..origin/main` = 23 | موثَّق | مزامنة الفرع (ليس عمل هذه المرحلة) |
| FE-5 | UNKNOWN — REQUIRES FURTHER TESTING | — | لوحة مفاتيح الجوال الفعلية | لا اختبار جهاز حقيقي في هذه الجلسة | بنية CSS مراجَعة فقط | غير مؤكَّد | اختبار جهاز/محاكي فعلي |
| FE-6 | VERIFIED | — | التمرير/اللمس في overlays menu-next | إصلاح سابق (هذه الجلسة) + مؤكَّد حاضر في `origin/main` | commit `3f75fc4`، اختبار إنتاج سابق (6/6 PASS) | لا حاجة لعمل | — |

---

## 18. Changes Implemented

| الملف | التغيير | السبب |
|---|---|---|
| `src/pages/Orders.jsx` | رفع `OrderCard` (+`isActive`) من داخل `Orders()` لمستوى الوحدة؛ تحويل الإغلاقات (`advanceOrder`, `cancelOrder`, `setSelectedOrder`, `minsSince`, `timeStyle`, `isVIP`) إلى props صريحة (`onAdvance`, `onCancel`, `onSelect`, `now`, `th`, `isVIP`)؛ تحديث نقطة الاستدعاء الوحيدة لتمرير الـ props الجديدة | إغلاق FE-1 |

**لا ملف آخر تم لمسه في هذه المرحلة.**

---

## 19. Tests Executed

| # | الاختبار | البيئة | المتوقَّع | الفعلي | RUN/NOT RUN | النتيجة |
|---|---|---|---|---|---|---|
| 1 | `npx vite build` كامل للجذر بعد التعديل | محلي | بناء ناجح بلا أخطاء | نجح، `Orders-BtfRQIdF.js` (43.85 kB) ضمن الناتج | **RUN** | ✅ PASS |
| 2 | `npx vitest run` — كامل مجموعة اختبارات الوحدة/التكامل للمستودع | محلي | كل الاختبارات تنجح | **64 ملفًا، 1201 اختبار — كلها نجحت** | **RUN** | ✅ PASS |
| 3 | مراجعة يدوية لكل نقاط استخدام `advanceOrder`/`cancelOrder`/`setSelectedOrder`/`isVIP`/`now`/`th` بعد النقل للتأكد من عدم بقاء أي مرجع كسور | محلي | لا مراجع معلَّقة | `grep` تأكيدي: لا استخدام متبقٍ للنسخة القديمة | **RUN** | ✅ PASS |
| 4 | اختبار تفاعل لمسي فعلي (سحب كرت طلب أثناء نبضة مؤقّت 20 ثانية) على جهاز/محاكي حقيقي | — | — | — | **NOT RUN — REASON: لا بيئة جهاز/محاكي متاحة في هذه الجلسة؛ الإصلاح مُتحقَّق منه عبر تحليل كود + بناء ناجح + كامل مجموعة الاختبارات، وليس عبر محاكاة يدوية للإيماءة** | — |
| 5 | Playwright e2e كامل (menu-next أو الجذر) | — | — | — | **NOT RUN — REASON: لم تتغيّر أي ملفات menu-next في هذه المرحلة؛ اختبارات الجذر (Playwright) خارج ما يغطيه vitest أعلاه ولم تُشغَّل بسبب وقت التشغيل** | — |

---

## 20. Regression Results

- **البناء (`vite build`)**: ✅ PASS — بلا أخطاء أو تحذيرات جديدة.
- **مجموعة اختبارات الوحدة/التكامل الكاملة (Vitest)**: ✅ **64/64 ملفًا، 1201/1201 اختبارًا نجحت** — يشمل هذا اختبارات غير مرتبطة مباشرة بـ Orders.jsx (Edge Functions، RLS-adjacent tests، إلخ) مما يؤكد عدم وجود أي أثر جانبي غير مقصود من التعديل.
- **لا اختبار Orders.jsx مخصص موجود مسبقًا في المستودع** يغطي هذا المكوّن تحديدًا — **موصى به كخطوة تالية** (القسم 23).
- Playwright e2e: **NOT RUN** (القسم 19).

---

## 21. Remaining Risks

**Confirmed (موثَّقة، لم تُصلَح عمدًا):**
- FE-2: 6 مواضع أخرى لنفس نمط "مكوّن داخل render" — أثر بصري بحت مؤكَّد، لا خطر حالة/تركيز.
- FE-3: `key={index}` في أداتي تحرير CMS إداريتين — أثر محدود جدًا (سطح إداري منخفض الاستخدام).

**Unknown:**
- FE-5: سلوك لوحة مفاتيح الجوال الفعلي لم يُختبَر على جهاز حقيقي.
- سلوك RTL/الاستجابة البصري لم يُختبَر بصريًا عبر أحجام شاشات متعددة فعليًا.

**Future Improvements (اختيارية):**
- إضافة معرّف ثابت (id) لعناصر الروابط في `MarketingSettings.jsx`/`marketingSectionRegistry.jsx` لإغلاق FE-3 بشكل نهائي.
- اختبار Playwright/RTL مخصص لسحب كرت الطلب في `Orders.jsx` أثناء وصول حدث Realtime، كاختبار انحدار دائم لـ FE-1.

**ملاحظة إجرائية (وليست خطرًا في الكود)**: مزامنة الفرع المحلي مع `origin/main` (23 commit، القسم 11) موصى بها قبل أي عمل مستقبلي على هذا المستودع، لتفادي نتائج فحص مضلِّلة.

---

## 22. Files Modified

```
src/pages/Orders.jsx                                                  (مُعدَّل)
SIMSIM_PHASE_3_FRONTEND_UX_RELIABILITY_EXECUTION_REPORT.md            (هذا الملف)
```

---

## 23. Recommended Next Phase

بناءً **حصريًا على ما اكتُشف فعليًا في هذه المرحلة**:

1. **مزامنة الفرع المحلي مع `origin/main`** (إجراء عملياتي بسيط، ليس مرحلة) — يُزيل مصدر الالتباس الموثَّق في القسم 11 قبل أي عمل قادم.
2. **إغلاق FE-3** (معرّفات ثابتة لعناصر CMS) — عمل صغير مستقل إن رغب المالك.
3. **اختبار لوحة مفاتيح/RTL فعلي على أجهزة حقيقية** (FE-5) إن كانت الأولوية إغلاق كل "Unknown" قبل الانتقال.
4. لا مرحلة جديدة "بحاجة" فورية بناءً على ما وُجد — دورة حياة الطلب (Phase 2) ودورة الأمان (Phase 1) وموثوقية الواجهة (Phase 3) مغطاة الآن بأدلة مباشرة؛ القرار التالي (مثل نشر إصلاح Phase 2 المُعلَّق، أو معالجة نقطتي Phase 2 القرار المعماري، أو استئناف الدفع) يبقى للمالك.

**لم تبدأ Phase 4. لم يُعَد تصميم أي شيء. لم يُنفَّذ الدفع.**

---

**حالة المرحلة: مكتملة. توقفت هنا كما طلبت التعليمات — بانتظار توجيه المالك.**
