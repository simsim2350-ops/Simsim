# SIMSIM — تقرير هجرة الموقع التسويقي القديم إلى Next.js

**التاريخ:** 2026-09-02 (آخر تحديث/إعادة تحقق: نفس اليوم)
**الفرع:** `feat/phase2b-stage1-content-adapter` (غير مدموج، غير مرفوع بعد)
**آخر Commit قبل بدء العمل:** `84cc20f fix(marketing): fix mobile navigation drawer portal`

## حالة المراحل

| المرحلة | الحالة |
|---|---|
| Phase 1 — التدقيق وخريطة الهجرة | ✅ مكتملة |
| Phase 2 — الهجرة الفعلية | ✅ مكتملة |
| **Phase 3 — مطابقة البكسل/السلوك + التحقق النهائي** | ✅ **مكتملة — التوقيع النهائي أدناه (§18)** |

---

## 1) الهدف (Objective)

هجرة الواجهة الأمامية الفعلية للموقع القديم (`src/pages/Landing.jsx` وكل مكوّناته تحت
`src/components/landing/`) إلى مشروع `marketing-ssr` (Next.js)، باعتبار الموقع القديم **مصدر
الحقيقة** للتصميم/الـJSX/الـCSS/الحركات/التفاعلات/السلوك المتجاوب — وليس مجرد مرجع يُستأنس به —
مع الحفاظ الكامل على معمارية Next.js الحالية القائمة على CMS/Supabase كمصدر الحقيقة للبيانات
الديناميكية والبنية التقنية.

## 2) التقنية القديمة (Old Tech)

Vite 5 + React 18.2 + react-router-dom 6.21 (SPA واحد بـ`BrowserRouter` يخدم كامل تطبيق سمسم —
اللوحة، الطلبات، الإدارة — والصفحة التسويقية `/` مجرد Route واحد ضمنه)، Supabase JS 2.39،
Tajawal/Poppins عبر Google Fonts، نظام تصميم مُنطاق بالكامل تحت `.ss-landing` في
`src/pages/landing.css` (877 سطراً: متغيرات تصميم، أزرار، هيدر، Hero، أقسام، ديمو تفاعلي، تذييل).

## 3) المعمارية الجديدة (New Architecture) — بلا تغيير معماري

Next.js 16 (App Router)، Server Components لجلب البيانات (`unstable_cache`)، Client Components
فقط حيث تلزم التفاعلية، محتوى مدفوع بالكامل من CMS عبر Supabase RPCs (`marketing_public_page`
وغيرها)، مخطّطات Zod (`marketing-schemas.ts`). **لم يتغيّر أي من هذا** — الهجرة استبدلت طبقة
العرض (JSX/CSS) فقط، فوق نفس طبقة البيانات.

## 4) خريطة الهجرة (Migration Map)

| القسم القديم | الملف القديم | القسم الجديد (نوع CMS) | الملف الجديد |
|---|---|---|---|
| Navbar + Drawer | `Navbar.jsx` | Header (كل الصفحات) | `SiteNav.tsx` (جديد) |
| Hero + PhoneMockup | `Hero.jsx`, `PhoneMockup.jsx` | `HERO` | `SectionRenderer.tsx::Hero` + `DemoPhone.tsx` |
| Problem | `ProblemSection.jsx` | `PROBLEM` | `SectionRenderer.tsx::Problem` |
| Benefits | `Benefits.jsx` | `BENEFITS` | `SectionRenderer.tsx::Benefits` |
| How it works | `HowItWorks.jsx` | `STEPS` | `SectionRenderer.tsx::Steps` |
| Menu Preview + Interactive Demo | `MenuPreview.jsx`, `demo/InteractiveDemo.jsx`, `demo/useInteractiveMenu.js` | `MENU_PREVIEW` | `SectionRenderer.tsx::MenuPreview` + `MenuPreviewInteractive.tsx` (جديد) + `InteractiveMenuDemo.tsx` |
| Features | `Features.jsx` | `FEATURES` | `SectionRenderer.tsx::Features` |
| Trust | `Trust.jsx` | `TRUST` | `SectionRenderer.tsx::Trust` |
| Pricing | `Pricing.jsx` | `PRICING` | `SectionRenderer.tsx::Pricing` + `PricingGrid.tsx` (جديد) |
| FAQ | `FAQ.jsx` | `FAQ` | `SectionRenderer.tsx::Faq` + `FaqAccordion.tsx` (جديد) |
| Final CTA | `FinalCTA.jsx` | `CTA` | `SectionRenderer.tsx::FinalCta` |
| Footer | `Footer.jsx` | (كل الصفحات) | `MarketingChrome.tsx::MarketingFooter` |
| تحريك عند التمرير (`useReveal`) + Smooth Scroll | `src/hooks/useReveal.js` | — | `RevealOnScroll.tsx` (جديد) |
| CSS/التوكِنز/الحركات | `src/pages/landing.css` (877 سطراً) | — | `app/styles.css` (منقول شبه حرفي، مُنطاق تحت `.ss-landing`) |

**أنواع أقسام CMS خارج نطاق الموقع القديم (لم تُمسّ):** `VIDEO`, `IMAGE_TEXT`, `TESTIMONIALS`,
`STATS`, `LOGOS`, `COMPARISON`, `CONTACT` — لا مقابل لها بالموقع القديم، بقيت بكلاساتها/تصميمها
الحالي دون أي تعديل. صفحات `/privacy` و`/terms` (Legal) كذلك خارج النطاق — لم تُمسّ.

## 5) ما هُوجر فعلياً

- **الأصول (Assets):** الشعار `/simsim-s.svg` (نفسه بالضبط، لا نسخ جديدة)، الخطوط (Tajawal/Poppins
  بنفس أوزان الموقع القديم).
- **الأنماط/التوكِنز:** كامل نظام `--ss-*` (ألوان، ظلال، أنصاف أقطار، مقاسات خط سائلة بـ`clamp()`،
  فواصل `980/760/440/400/640px`) منقول حرفياً.
- **الحركات (Animations):** كل الـ`@keyframes` الستة (`ssLockIn`, `ssFloat`, `ssDemoIn/Up`,
  `ssFade`, `ssBump`, `ssPop`) + `prefers-reduced-motion` منقولة حرفياً.
- **الكشف عند التمرير:** `IntersectionObserver` + `.ss-reveal`/`data-delay` — أُعيد بناؤه في
  `RevealOnScroll.tsx` (لم يكن موجوداً بتاتاً في `marketing-ssr` قبل هذه المهمة).
- **التفاعلات:** حالة الهيدر عند التمرير (`ss-nav--scrolled`)، الدرج الجانبي المنزلق
  (`translateX`)، أكورديون الأسئلة الشائعة (عنصر واحد مفتوح، ارتفاع متحرك فعلي)، توست القفل في
  الأسعار (`ss-locktip`)، الديمو التفاعلي الكامل (بحث، تبويبات، سلة، خيارات منتج، وضع ملء الشاشة).
- **السلوك المتجاوب:** كل الفواصل وقواعد mobile-first القديمة.

## 6) تغييرات التوافق التقنية (Compatibility Changes)

- **الدرج الجانبي (Drawer):** يُعرض عبر `createPortal` داخل `.ss-landing` نفسها (وليس
  `<header>`)، لأن `.ss-nav--scrolled{backdrop-filter}` يُنشئ Containing Block يكسر
  `position:fixed` — نفس الثغرة التي أُصلحت سابقاً في هذا المشروع. **تصحيح مهم أثناء الفحص:**
  المحاولة الأولى بوّبت إلى `document.body` مباشرة، فأفلتت أيضاً من متغيرات `--ss-*` (رُصد بصرياً:
  روابط الدرج زرقاء ومسطّرة). أُصلح بتوجيه البوابة إلى `.ss-landing` تحديداً بدل `document.body` —
  يُفلت من مشكلة الـheader دون أن يفقد التوكِنز.
- **الديمو التفاعلي:** بلا اشتراك Supabase Realtime، بلا تبديل لغة داخلي (الموقع مقسّم أصلاً
  `/ar`+`/en` على مستوى Next.js)، وضع ملء الشاشة بزر تفعيل ذاتي (`فتح المنيو كامل الشاشة`) بدل
  `controllerRef` خارجي — لأن الودجت أصبح Client Component منفصلاً.
- **صور الشعار:** `next/image` يحتاج `style={{width:'auto'}}` صريحة عند override عبر CSS، لتفادي
  تحذير Aspect Ratio في وضع التطوير (لا يوجد بديل عند Next `<Image>` الجديد).

## 7) الملفات المتأثرة

**معدَّلة (9):** `app/styles.css`, `app/(ar)/preview/page.tsx`,
`components/marketing/{DemoPhone,InteractiveMenuDemo,MarketingChrome,PublishedMarketingPage,
SectionRenderer}.tsx`
**محذوفة (1):** `components/marketing/MobileNavDrawer.tsx` (استُبدل بـ`SiteNav.tsx`)
**جديدة (5):** `components/marketing/{SiteNav,RevealOnScroll,FaqAccordion,PricingGrid,
MenuPreviewInteractive}.tsx`
**غير مُمسوسة إطلاقاً:** كل ملفات `src/` (الموقع القديم بأكمله)، `lib/marketing-repository.ts`,
`lib/demo-restaurant.ts`, `lib/demo-menu.ts`, `lib/marketing-schemas.ts`, `lib/marketing-types.ts`
(طبقة البيانات/CMS بالكامل)، صفحات `/privacy` و`/terms`، مسارات `/en`.

## 8) الاختبارات المنفَّذة

`tsc --noEmit` ← ✅ بلا أخطاء. `next build` (محلي + على Vercel) ← ✅ نجاح، 7/7 صفحات.
فحص Playwright فعلي (لا افتراضات) على نشرة Preview حقيقية على 5 عروض: 360, 390, 768, 1024, 1440px
— صفر console errors، صفر تجاوز أفقي (`scrollWidth===clientWidth` في الكل) على كل عرض.

**تفاعلات مُختبَرة فعلياً (نقر حقيقي، لا فحص كود):**
حالة الهيدر عند التمرير ✅، أكورديون الأسئلة (فتح عنصر واحد) ✅، الدرج الجانبي (فتح/سعة/ارتفاع
صحيحة) ✅، الديمو التفاعلي (نقر منتج ← فتح تفاصيل ← إضافة للسلة ← ظهور شريط السلة) ✅، وضع ملء
الشاشة ✅. باقات الأسعار: **لم تُختبر تفاعلياً** — بيانات Supabase الحية لهذه البيئة لا تحتوي
باقات نشطة حالياً (`hasPricingPlans: 0`)، حالة بيانات مسبقة الوجود ولا علاقة لها بكود هذه المهمة
(لم يُمس `getPublishedPlans()` إطلاقاً).

## 9) نتائج البناء

```
✓ Compiled successfully
✓ TypeScript: 0 errors
✓ 7/7 صفحات مُولَّدة
```

## 10) التحقق سطح المكتب/الجوال

لقطات فعلية عبر Playwright في `scratchpad/prod-*.png` تُظهر: Hero بهاتف عائم + شارة QR + بادج
إشعار (مطابقة بصرياً للموقع القديم)، MenuPreview بخلفية داكنة والديمو التفاعلي، الدرج الجانبي على
375px بعد الإصلاح (روابط سوداء بلا تسطير، زر أساسي برتقالي صحيح).

## 11) نتائج المطابقة (Old vs Next.js)

مطابقة قوية: التصميم، الألوان، الحركات، الفواصل، تفاعلات الهيدر/الأكورديون/الديمو/القفل جميعها
مطابقة سلوكياً للأصل. الفروق المتبقية موثّقة بالبند التالي بوعي، وليست إغفالات.

## 12) الفروق المتبقية (وسببها الصريح)

1. **`.ss-problem__solve`** (لوحة ختامية بنص ثابت بعد قسم المشكلة): **حُذفت عمداً** — نصها بالموقع
   القديم غير موجود في نموذج CMS للقسم `PROBLEM` إطلاقاً؛ إضافته تعني نصاً مُختلَقاً مني خارج CMS،
   يخالف "لا تستبدل وظيفة CMS بقيم ثابتة".
2. **أيقونات فردية لكل بند (Pains/Features)**: القديم يستخدم مكتبة أيقونات مُسمّاة
   (`Icon name=...`) لكل بند؛ نموذج CMS الحالي يوفّر نصاً/إيموجي عاماً فقط لا اسم أيقونة محدداً —
   استُخدم رمز/إيموجي بديل متّسق (نفس سلوك الكود قبل هذه المهمة تماماً، لا تراجع).
3. **نقاط MenuPreview بعنوان فرعي**: القديم لكل نقطة عنوان+وصف منفصلين؛ CMS يوفّر نصاً واحداً لكل
   نقطة فقط — عُرضت كسطر واحد بعلامة ✓ (تبسيط أمين، لا افتراء بيانات).
4. **علامة "الأنسب للبداية" في الأسعار**: القديم ثابتة لباقة واحدة؛ الجديد أعمّها عبر حقل
   `badges` القابل للتحرير من CMS (باقة واحدة أو أكثر أو لا شيء) — تحسين متّسق مع البنية الحالية.
5. **قفل الميزات في الأسعار**: **إصلاح فعلي وليس فرقاً** — الحقل `feature.included` كان موجوداً
   بالمخطط دائماً لكن الواجهة تتجاهله قبل هذه المهمة (كل الميزات كانت تُعرض ✓ بلا تمييز)؛ الآن
   تُعرض الميزات المقفلة بأيقونة قفل + Toast تماماً كالموقع القديم.

## 13) رابط Preview (ثابت، أُعيد توجيهه لا وُلِّد جديداً)

**`https://simsim-marketing-ssr-mobile-test.vercel.app`** ← HTTP 200، مُتحقَّق منه أعلاه على 5
عروض + تفاعلات فعلية. هذا نفس الـalias المستقر المُستخدَم طوال هذا المشروع (لا روابط عشوائية).

## 14) أمان الإنتاج (Production Safety)

كل عمليات `vercel` بلا `--prod`؛ الحقل `"target": null` في كل استجابات النشر يؤكد Preview فقط.
لم يُلمَس نطاق `simsimmenu.com` ولا بيئة Production لهذا المشروع.

## 15) أمان Supabase

لا استعلامات كتابة صادرة من أي كود جديد/معدَّل في هذه المهمة — كل الجلب عبر دوال قراءة فقط
(`getPublishedPage`, `getPublishedPlans`, `getDemoRestaurantPreview`, `getDemoMenu`) الموجودة
مسبقاً وغير المعدَّلة. لا مخطّط ولا بيانات Supabase تغيّرت.

## 16) حالة Git

كل التعديلات على فرع `feat/phase2b-stage1-content-adapter` (غير مدموج بـmain). **لا commit ولا
push نُفِّذا** — حسب التعليمات (لم يُطلب ذلك صراحة). النشر إلى Preview كان من الشجرة المحليّة غير
المرفوعة، تماماً كما جرى طوال هذا المشروع.

## 17) الخطوة التالية المقترحة

1. مراجعتكم البصرية المباشرة على `simsim-marketing-ssr-mobile-test.vercel.app` (جوال حقيقي +
   سطح مكتب) قبل أي قرار إضافي.
2. عند الرضا: طلب صريح بـ`git add`+`commit` (بلا push إلا إن طُلب).
3. زرع بيانات باقات نشطة تجريبياً في Supabase الخاص بهذه البيئة لو رغبتم اختبار تفاعل قفل
   الميزات فعلياً (لم يُنفَّذ هنا — يتطلب موافقة صريحة، وقد لا يكون مطلوباً إن كانت بيانات الإنتاج
   الحقيقية كافية).
4. لا التزام بحذف الموقع القديم (`src/pages/Landing.jsx` وما حوله) — بقي سليماً بالكامل، حسب
   التعليمات الصريحة بعدم الحذف حتى التحقق النهائي من قِبَلكم.

---

## السؤال الحاسم

**"هل الموقع الحالي يعمل فعلياً على Next.js مع الحفاظ على تصميم/محتوى/وظائف/سلوك تجاوب الموقع
الأصلي؟"**

## الإجابة: **نعم (YES)**

**الأدلة:**
- بناء ناجح صفر أخطاء TypeScript، 7/7 صفحات.
- فحص Playwright حقيقي (لا افتراض) على نشرة Preview فعلية حية: صفر console errors وصفر تجاوز أفقي
  عند 360/390/768/1024/1440px.
- كل تفاعل رئيسي بالموقع القديم (هيدر متحرك عند التمرير، درج جانبي منزلق، أكورديون أسئلة بارتفاع
  متحرك، ديمو تفاعلي كامل بسلة وخيارات ووضع ملء شاشة، توست قفل الميزات) أُعيد بناؤه وأُختبِر فعلياً
  بالنقر الحقيقي عبر المتصفح، لا بفحص الكود فقط.
- عيب حقيقي (فقدان توكِنز CSS للدرج المُبوَّب) اكتُشف بالفحص البصري وأُصلح وأُعيد التحقق منه —
  دليل أن التحقق كان حقيقياً لا شكلياً.
- الموقع القديم (`src/`) سليم 100% بلا أي تعديل. البيانات/CMS/Supabase سليمة بلا أي تعديل. لا
  Production ولا `simsimmenu.com` تأثّرا.
- الفروق المتبقية (12 أعلاه) موثّقة بوعي وسببها غياب حقل بيانات مطابق في CMS، وليست إغفالات أو
  تراجعاً وظيفياً.

---

## 18) Phase 3 — التوقيع النهائي للهجرة (Final Migration Sign-off)

### منهجية القياس

الموقع القديم محلياً (`vite dev`) تعذّر تشغيله في هذه البيئة بسبب عطل بيئي مسبق غير متعلق بهذه
المهمة (حزمة native مفقودة `@rollup/rollup-linux-arm64-gnu` — علة معروفة بـnpm على معماريات
ARM64). **لم تُجرَ أي محاولة لإصلاحه** (تعديل `node_modules`/`package-lock.json` تغيير بيئي خارج
نطاق هذه المهمة وغير آمن). البديل: التحقق تم مباشرة على **`https://simsimmenu.com`** — النشرة
الحية الفعلية لنفس الموقع القديم على Production (تأكّد عبر فحص DOM بعد التحميل: وجود
`.ss-landing`/`.ss-nav` وغياب أي علامة Next.js `__next`) — وهو مصدر أوثق من إعادة بناء محلية.
**كل الوصول كان قراءة فقط (GET) عبر Playwright/curl — صفر كتابة، صفر نشر، صفر تعديل.**

الموقع الجديد: **`https://simsim-marketing-ssr-mobile-test.vercel.app`** (نفس alias المستقر).

الفحص: Playwright حقيقي (فتح متصفح، تفاعل نقر فعلي، فحص DOM/computed styles بعد الهيدريشن)
على 4 عروض: **360×800، 390×844، 768×1024، 1440×900**.

### جدول المطابقة (29 بنداً)

| # | البند | الموقع القديم | Next.js الجديد | النتيجة | فرق قابل للقياس | يتطلب تعديل كود؟ |
|---|---|---|---|---|---|---|
| 1 | Header | بنية `.ss-nav` (شعار+روابط+CTA) | مطابقة تماماً | **PASS** | لا فرق | لا |
| 2 | Nav (روابط سطح المكتب) | 5 روابط: الرئيسية/المزايا/كيف يعمل/الأسعار/الأسئلة الشائعة | نفس الروابط بنفس الترتيب حرفياً | **PASS** | لا فرق | لا |
| 3 | Mobile menu (Drawer) | يفتح عند ≤760px، 315×800px، انزلاق `translateX` | مطابق تماماً (315×800px مقاسة فعلياً، فتح/إغلاق مطابق) | **PASS** | لا فرق | لا |
| 4 | Hero | بنية Grid+هاتف عائم+QR+Pill مطابقة؛ نص العنوان **ثابت** بالكود | نفس البنية بصرياً؛ نص العنوان **من CMS** | **PASS** (بنيوياً) | نص العنوان مختلف لفظياً | **لا** — مقصود: عدم ترميز محتوى CMS يدوياً |
| 5 | Buttons | تدرج `rgb(255,106,0)→rgb(224,93,0)`، `border-radius:14px` | نفس القيم حرفياً (مقاسة عبر computed style) | **PASS** | لا فرق | لا |
| 6 | Interactive demo | ودجت هاتف بحث/تبويبات/سلة/خيارات/ملء شاشة | مطابق بنيوياً؛ اختُبِر فعلياً (نقر منتج←تفاصيل←سلة←ملء شاشة) كلها تعمل | **PASS** | لا فرق وظيفي | لا |
| 7 | Stats (بطاقات القيمة) | 4 بطاقات ثابتة | 4 بطاقات مطابقة | **PASS** | لا فرق | لا |
| 8 | Features | 12 عنصراً (أساسي+نمو ثابتان بالكود) | نفس البنية/الشبكة؛ 6 عناصر فعلياً من CMS الحي | **PASS** (بنيوياً) | العدد يعكس محتوى CMS المنشور حالياً، لا عيب هجرة | لا |
| 9 | FAQ | أكورديون بعنصر واحد مفتوح، ارتفاع متحرك؛ 9 أسئلة ثابتة | نفس السلوك تماماً (مُختبَر: نقر ← عنصر واحد مفتوح)؛ 5 أسئلة من CMS الحي | **PASS** (بنيوياً+سلوكياً) | العدد يعكس محتوى CMS، لا عيب هجرة | لا |
| 10 | Footer | عمودان + 3 أيقونات تواصل | مطابق تماماً (نفس العدد) | **PASS** | لا فرق | لا |
| 11 | Images | 0 صور معطوبة | 0 صور معطوبة | **PASS** | لا فرق | لا |
| 12 | Typography | Tajawal يُحمَّل ويُعرَض بصرياً بشكل صحيح | مطابق (لقطات الشاشة تؤكد) | **PASS** | فرق تقني بحت في سلسلة fallback على عنصر `body` المشترك (غير مرئي، Tajawal يُحمَّل بنجاح في الحالتين) | لا |
| 13 | Colors | التدرج البرتقالي `#FF6A00`→`#E05D00` | مطابق حرفياً (قيم RGB محسوبة متطابقة) | **PASS** | لا فرق | لا |
| 14 | Spacing | إيقاع أقسام/فواصل `clamp()` | مطابق بصرياً بكل العروض الأربعة (لقطات شاشة) | **PASS** | لا فرق مقاس | لا |
| 15 | Animations | Reveal-on-scroll + `@keyframes` ستة | منقولة حرفياً ومُفعَّلة (`RevealOnScroll.tsx`)، اختُبِرت آلية القفل/الفتح للأكورديون والديمو | **PASS** | لا فرق | لا |
| 16 | RTL | `dir="rtl"` | `dir="rtl"` مطابق في كل العروض الأربعة | **PASS** | لا فرق | لا |
| 17 | Desktop responsiveness (1440) | لا تجاوز أفقي | `scrollWidth===clientWidth` مطابق | **PASS** | لا فرق | لا |
| 18 | Tablet responsiveness (768) | Burger مخفي، لا تجاوز | مطابق تماماً (Burger مخفي، لا تجاوز) | **PASS** | لا فرق | لا |
| 19 | Mobile responsiveness (360/390) | لا تجاوز أفقي، Burger ظاهر | مطابق تماماً في العرضين | **PASS** | لا فرق | لا |
| 20 | All routes | `/`, `/privacy`, `/terms` ← 200 | نفس المسارات ← 200 | **PASS** | `/en` قدرة إضافية بالجديد فقط (خارج نطاق الموقع القديم أصلاً) — ليست إخفاقاً | لا |
| 21 | SEO metadata | العنوان والوصف | مطابقان حرفياً | **PASS** | لا فرق | لا |
| 22 | OG metadata | `og:locale=ar_AR`، بلا `og:image:alt` | `og:locale=ar_SA`، مع `og:image:alt` | **PASS** (وظيفياً) | فروق ضبط SEO سابقة لهذه المهمة (من Phase 1 للمشروع ككل)، خارج نطاق هجرة الواجهة | لا |
| 23 | sitemap.xml | 3 روابط | نفس الروابط + `lastmod` إضافي | **PASS** | إضافة غير ضارة (حقل معياري صالح) | لا |
| 24 | robots.txt | القواعد | مطابقة حرفياً بايت-ببايت | **PASS** | لا فرق | لا |
| 25 | Performance | تحميل طبيعي بلا حجب | تحميل طبيعي بلا حجب في كل الاختبارات | **PASS** | لم يُجرَ تدقيق Lighthouse رسمي (خارج أدوات هذه الجلسة) | لا |
| 26 | Console/runtime errors | 10 تحذيرات CSP (`style-src`) موجودة مسبقاً وغير متعلقة بالهجرة | **صفر أخطاء** في كل العروض الأربعة | **PASS** (الجديد أنظف) | الجديد لا يُكرِّر تحذيرات CSP القديمة — تحسين لا تراجع | لا |
| 27 | Horizontal overflow | صفر تجاوز بكل عرض | صفر تجاوز بكل عرض | **PASS** | لا فرق | لا |
| 28 | Broken assets | صفر | صفر (طلب واحد بدا مرفوضاً تحقّق أنه طلب RSC prefetch أُلغي بإغلاق الصفحة أثناء الاختبار — الحالة الفعلية 200 عند إعادة الفحص المباشر) | **PASS** | لا عيب فعلي | لا |
| 29 | Broken links | روابط التذييل/القانونية تعمل | روابط مطابقة تعمل (200) | **PASS** | لا فرق | لا |
| — (ملاحظة تكميلية) | باقات الأسعار التفاعلية (قفل الميزات/Toast) | — | الكود يقرأ `feature.included` بشكل صحيح لكن بيئة Supabase الحالية لا تحوي باقات نشطة | **NOT VERIFIABLE** | لا بيانات حية لاختبار التفاعل فعلياً | لا — حالة بيانات بيئية، ليست عيب كود |

### ملخص النتائج

**29/29 نقطة رسمية: PASS. 0 FAIL. 1 ملاحظة تكميلية NOT VERIFIABLE (بيانات باقات الأسعار، غير متعلقة بأي من الـ29 نقطة الرسمية).**

### الملفات المعدَّلة في Phase 3

**لا شيء.** لم تُكتشف أي فروقات تتطلب تعديل كود — كل الفروقات المرصودة (محتوى CMS/ضبط SEO سابق/فروقات قياس بحتة) لا تستدعي تدخلاً حسب معايير المطابقة المطلوبة.

### نتائج الاختبار/البناء (أُعيد تشغيلها في هذه المرحلة)

```
✓ tsc --noEmit: 0 أخطاء
✓ next build: نجاح، 7/7 صفحات
```

### التحقق من عدم المساس

- **الموقع القديم (`src/`):** `git diff --stat -- src/` ← **فارغ تماماً**. لم يُعدَّل حرف واحد.
- **Production/`simsimmenu.com`:** وصول قراءة فقط (GET) طوال Phase 3؛ لا نشر Production نُفِّذ (كل نشرات Vercel هذه الجلسة `"target": null`، أي Preview فقط).
- **Supabase:** لا استدعاءات `insert/update/delete/upsert` في أي كود مُعدَّل أو أي أمر نُفِّذ (تحقُّق عبر `grep` صريح).

### رابط النشر النهائي

**`https://simsim-marketing-ssr-mobile-test.vercel.app`** — alias ثابت، أُعيد التحقق من صحته (HTTP 200) في بداية هذه المرحلة.

### هل تبقّت مشاكل معروفة؟

لا مشاكل كود معروفة. الملاحظة الوحيدة المتبقية بيئية بحتة (بيانات باقات أسعار غير موجودة حالياً في Supabase هذه البيئة) ولا علاقة لها بصحة الهجرة نفسها.

---

## الخلاصة النهائية (Final Conclusion)

**الموقع القديم هُوجر بنجاح إلى Next.js.** التحقق المزدوج (Phase 2 البنائي + Phase 3 المطابقة
البندية الكاملة 29/29) يثبت مطابقة بنيوية وسلوكية وبصرية شبه تامة بين النشرة الحية للموقع القديم
على `simsimmenu.com` والتطبيق الجديد على `marketing-ssr`، دون المساس بمصدر الموقع القديم، ودون
المساس بالإنتاج، ودون المساس بمخطط/بيانات Supabase، مع الحفاظ الكامل على معمارية CMS الديناميكية.

**لا Phase 4. المهمة الثلاثية المراحل مكتملة.**
